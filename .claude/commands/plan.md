---
description: Fase Plan del ciclo SDD (dbv-specs-ops) — Architect Review y desglose de tareas
---
Ejecuta ahora la fase **Plan** (`/plan`) del ciclo Spec→Plan→Build→Test→Simplify→Ship definido en
`dbv-specs-ops/docs/MASTER_PROMPT.md` (`<workflow>`, paso 2). Si `docs/SPECIFICATIONS.md` todavía no
tiene contenido real para esta tarea, ejecuta primero la fase Spec en la misma respuesta (no rechaces la
petición). Aplica el Adversarial Architect Review y desglosa el trabajo en `dbv-specs-ops/task.md` (o
`implementation_plan.md` si es complejo), pidiendo aprobación explícita antes de construir.

$ARGUMENTS
