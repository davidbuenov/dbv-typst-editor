# 🧠 Memory & Context

> **Frontera de uso (Memory vs. Tasks):**
> - `task.md` → progreso **operativo**: checklist de tareas, Snapshot de Contexto (el paso exacto siguiente), y estado de la sesión.
> - `memory.md` → contexto **cualitativo y temático**: conocimiento persistente, decisiones técnicas profundas, lecciones, y el área del producto en foco (no el paso específico).
> Si hay info que sirva para los dos, prioriza: datos con fecha/paso exacto → `task.md`; razonamiento/por-qué/lecciones → `memory.md`.
>
> *Instrucción para la IA: Consulta este archivo al inicio de cada sesión para recuperar el hilo técnico. Actualiza las secciones correspondientes cuando el workflow lo indique (triggers en `/plan`, `/build`, `/test` y gate en `/ship`).*

## 🎯 Contexto Activo

- **Ubicación:** `d:/Programacion/github-davidbuenov/dbv-typst-editor` (renombrado el 2026-09-05 desde `dbv-academic-writer`; si esa ruta no existe, probar el nombre antiguo).
- **Estado actual del desarrollo:** `/spec` y `/plan` CERRADAS y congeladas (`docs/SPECIFICATIONS.md` v1.1, `docs/ARCHITECTURE.md` v1.0, `docs/TYPST_ECOSYSTEM_RESEARCH.md` verificado contra binario real). **`/build` COMPLETADA (2026-09-05): los 10 slices del MVP v0.1 construidos, verificados y commiteados, un commit por slice.** El bucle de valor completo funciona de punta a punta y **el usuario lo validó a mano**: crear proyecto desde plantilla → escribir con resaltado Typst → ver el PDF actualizarse solo → guardar → exportar PDF.
- **Verificación viva:** 77 tests Rust · 8 comprobaciones del frontend sin navegador · 8 del sidecar contra el binario real · 20 del catálogo de plantillas. Todo en `npm test` + `npm run verify:frontend|typst|templates`.
- **Foco inmediato (próxima sesión):** **fase `/test`.** No hay que replanificar nada. El punto de retorno operativo, con la lista exacta de lo que `/test` debe cubrir, está en `task.md` → "CÓMO RETOMAR ESTE PROYECTO".
- **Bloqueante de infraestructura, no de código:** el repositorio **no tiene remoto configurado**, así que `ci.yml` y `release-linux.yml` (Slice 10) no se han ejecutado nunca y están verificados solo por lectura. Es lo primero que conviene resolver: coste casi nulo y hace que todo lo demás se compruebe solo.
- **Alcance cerrado de v0.1:** RF-01..RF-10 y RF-12 hechos. RF-11 (Project Archive `.dbvt`) y las 4 plantillas restantes siguen diferidos a v0.2 por decisión del usuario.

## 🏗️ Log de Decisiones Técnicas (ADR Ligero)

*Registro de por qué se tomaron ciertas rutas (ej. cambios en librerías, arquitectura o patrones).*

- **2026-09-05 — El directorio del repositorio se renombra de `dbv-academic-writer` a `dbv-typst-editor`,** cerrando la ambigüedad detectada el 2026-09-04 (entrada siguiente). Ya no queda ningún sitio donde el proyecto se llame "academic writer": `package.json`, `Cargo.toml`, `tauri.conf.json` y el identificador de la aplicación decían `dbv-typst-editor` desde el primer commit, así que el nombre del directorio era el único resto del prompt inicial. El cambio no afecta a git (sus rutas son relativas) ni a la documentación SDD (vive dentro del repo).
- **2026-09-04 — Nombre del proyecto:** El nombre oficial es "DBV Typst Editor" (no "DBV Academic Writer", que queda como tagline/eslogan: *"Academic and technical writing made simple. Powered by Typst."*). Confirmado explícitamente por el usuario tras detectar ambigüedad entre el nombre del directorio del repositorio (`dbv-academic-writer`) y el nombre pedido en el prompt inicial.
- **2026-09-04 — Editor de código: CodeMirror 6 en vez de Monaco Editor.** Motivo: Monaco (~2-5MB, motor completo de VS Code) contradice la filosofía "ligero, offline-first, arranque instantáneo" que es la propuesta de valor de toda la familia DBV Markdown Reader / DBV Typst Editor; CodeMirror 6 es ~200-400KB, modular, ESM-first, y es la base que usa el propio editor web oficial de Typst (app.typst.io), lo que maximiza la probabilidad de un modo de lenguaje Typst mantenido a largo plazo. Ver justificación completa en `docs/ARCHITECTURE.md` §7.1.
- **2026-09-04 — Compilador Typst embebido como crates Rust (no CLI sidecar).** ⚠️ **REVERTIDA el mismo día** — ver entrada más abajo ("TYPST CLI INTEGRATION"). Motivo original (histórico, ya no vigente): coherencia con el patrón "single self-contained binary" ya usado por DBV Markdown Reader, evitar desalineación de versión, compilar a memoria sin pasar por el sistema de ficheros. Se mantiene esta entrada por trazabilidad del razonamiento, no como decisión activa.
- **2026-09-04 — Preview en vivo por SVG página a página, PDF real solo en guardado/exportación.** Motivo: SVG es vectorial, permite refresco incremental sin cargar un visor PDF.js completo, y reutiliza casi literalmente el mecanismo de watch→debounce→re-render ya validado en DBV Markdown Reader (`lib.rs:457-503` + `app.js:552-568`). Ver `docs/ARCHITECTURE.md` §7.3.
- **2026-09-04 — Introducción de Vite como bundler del frontend.** Rompe conscientemente el patrón "sin bundler, IIFE + vendor scripts" documentado como obligatorio en `docs/NATIVE_DESKTOP_APPS.md` §3 para DBV Markdown Reader — necesario porque CodeMirror 6 es ES Modules-first. Se mantiene `withGlobalTauri: true` para los plugins Tauri consumidos vía `window.__TAURI__`; a validar en `/build` si el resto del frontend se mantiene híbrido (vendorizado) o migra también a ESM real.
- **2026-09-04 — Spec Addendum del usuario: cambio de filosofía de producto.** DBV Typst Editor deja de plantearse como "editor de código para Typst" y pasa a ser una herramienta orientada a documento/proyecto, "para Typst lo que Obsidian es para Markdown" — el usuario piensa en "crear un TFG", no en sintaxis. Consecuencias registradas en `docs/SPECIFICATIONS.md` §2 y §4 (modelo de Proyecto, no fichero suelto) y `docs/ARCHITECTURE.md` §7.5-7.13 (lanzador, asistente de creación, marketplace de plantillas, asistentes de inserción, outline, modos de escritura, gestión de imágenes, Project Archive).
- **2026-09-04 — Editor de código: CodeMirror 6 re-evaluado y reconfirmado (no Monaco), pese a que el Addendum pedía explícitamente evaluar Monaco como opción principal.** Motivo del refuerzo: **Obsidian —la referencia explícita de la nueva filosofía de producto— está construido internamente sobre CodeMirror 6**, no sobre Monaco; Monaco se percibe como "VS Code embebido", justo la sensación de "editor de código" que el Addendum pide evitar. Única ventaja real de Monaco tras la re-evaluación: minimapa nativo más maduro (CM6 lo cubre vía extensión de comunidad, no descarta la decisión). Multi-cursor y rendimiento en documentos largos, los otros dos motivos citados por el Addendum, resultan ser no-diferenciadores (CM6 ya los cubre nativamente a esta escala). Ver tabla de re-evaluación completa en `docs/ARCHITECTURE.md` §7.1.
- **2026-09-04 — Modelo de Proyecto (no fichero `.typ` suelto) como unidad de trabajo.** Estructura `main.typ` + `refs.bib` + `chapters/` + `images/` + `assets/` + `settings/` (manifiesto propio de DBV con metadatos que Typst no conoce: plantilla de origen, valores del asistente). Un `.typ` suelto se trata como proyecto de un solo fichero para mantener compatibilidad. Ver `docs/ARCHITECTURE.md` §7.5.
- **2026-09-04 — Marketplace de plantillas comunitarias apoyado en el registro oficial de Typst (`@preview`/Typst Universe), no en un backend propio.** Decisión inicial acertada, refinada tras investigación posterior (ver entradas más abajo): la descarga-y-caché ocurre dentro del proceso CLI, no vía llamada directa a una API Rust. Mitigación de riesgo de cadena de suministro: whitelist curada por DBV antes de abrir al registro completo sin filtrar. Ver `docs/ARCHITECTURE.md` §7.6.3 y riesgo en §6.
- **2026-09-04 — Project Archive `.dbvt` como ZIP (crate `zip` en Rust) con `manifest.json` propio.** Requiere sanitización obligatoria anti *zip-slip* en el comando de importación (rechazar cualquier ruta de entrada que resuelva fuera del directorio de proyecto destino) antes de escribir nada a disco. Ver `docs/ARCHITECTURE.md` §7.12 y riesgo en §6.
- **2026-09-04 — Alcance del MVP ampliado tras el Addendum:** lanzador orientado a tareas, asistente de creación de proyecto y Project Archive pasan de "futuro" a MVP (de RF-01..RF-11 a RF-01..RF-12); marketplace de plantillas, outline estructural, asistentes de inserción, gestión de imágenes por arrastre y bibliografía visual quedan explícitamente en Beta por decisión del propio usuario. Ver roadmap reconciliado en `docs/SPECIFICATIONS.md` §11.
- **2026-09-04 — Clarificación del usuario: Paquetes y Plantillas de Typst Universe son dos ecosistemas de producto distintos (no fusionar en un único "marketplace").** Investigación técnica confirmó que, a nivel de **datos**, comparten una única fuente (`index.json` público, `packages.typst.org`) — una plantilla es solo un paquete con sección `[template]` en su manifiesto — pero la separación pedida por el usuario vive correctamente en la capa de producto/UX: **Package Explorer** (§7.6.2, acción "Añadir al proyecto") y **Template Explorer** (§7.6.3, acción "Crear Proyecto") son vistas distintas sobre la misma capa de sincronización de datos. Se añade detección automática de "Paquetes usados" (escaneo de `#import`) como funcionalidad nueva. Ver `docs/TYPST_ECOSYSTEM_RESEARCH.md` (informe completo).
- **2026-09-04 — "TYPST CLI INTEGRATION": integración con Typst vía el CLI oficial vendorizado como sidecar de Tauri, en vez de embeber las crates Rust del compilador — REVIERTE la decisión anterior del mismo día.** Instrucción explícita del usuario, con alcance concreto (creación de proyecto, resolución de paquetes, compilación, preview, exportación) más un terminal avanzado opcional para usuarios avanzados. Consecuencias en cascada documentadas y propagadas en `docs/ARCHITECTURE.md` §7.2: la detección de "Paquetes usados" pasa de "parser `typst::syntax::parse` embebido" a "escaneo de texto ligero" (para no mezclar integración CLI con una crate embebida suelta); la resolución/caché de paquetes ya no se invoca vía `typst-kit::PackageStorage` como API Rust, ocurre dentro del proceso hijo `typst`; el outline estructural usa `typst query heading` en vez de `typst-ide` directamente. Los binarios de release oficiales de Typst usan naming por *target triple*, compatible con la convención de sidecar de Tauri — ver investigación completa en `docs/TYPST_ECOSYSTEM_RESEARCH.md` §1.
- **2026-09-04 — Investigación dedicada del ecosistema Typst realizada a petición explícita del usuario, antes de pasar a `/plan`.** Resultado documentado en el nuevo fichero `docs/TYPST_ECOSYSTEM_RESEARCH.md` (CLI, sistema de paquetes, Typst Universe, registros oficiales, oportunidades de integración). Hallazgo más relevante para futuras sesiones: **no existe comando CLI de "actualizar paquete"** — el Package Explorer debe reescribir el string de versión del `#import` directamente, no delegar en un subcomando.
- **2026-09-04 — Posicionamiento oficial de producto fijado por el usuario tras revisar el informe de investigación: "el entorno de escritorio más accesible para el ecosistema Typst", explícitamente NO "un editor de código con soporte Typst".** Se formaliza como dos principios arquitectónicos guía en `docs/ARCHITECTURE.md` §0.1: (1) reparto de responsabilidades — Typst aporta infraestructura (compilador, gestión de paquetes, plantillas, caché), DBV aporta experiencia (proyectos, flujos académicos, exploración visual, productividad); (2) **Universe-First** — preferir siempre recursos oficiales del ecosistema Typst sobre construir un registro paralelo propio; solo se justifica infraestructura propia (p. ej. el sidecar `dbv-template.toml`) cuando aporta valor claro al usuario final sin sustituir lo oficial.
- **2026-09-04 — "Universe Browser" (antes descrito como "marketplace de plantillas", una funcionalidad más) se reencuadra como punto de entrada de primer nivel de la aplicación**, no un añadido — sin reabrir el acuerdo de fases ya cerrado (sigue siendo Beta salvo las plantillas propias curadas de DBV, que ya eran MVP). Estructura de navegación acordada: `Universe` con dos ramas de igual peso — `Plantillas` (Template Explorer) y `Paquetes` (Package Explorer) — cada una con sus propias pestañas Instaladas/Comunidad/Favoritas/Recientes/Actualizaciones, sin fusionarse entre sí. Se conecta explícitamente con el MVP: el Lanzador (§7.13, ya MVP) es funcionalmente un Template Explorer reducido al catálogo curado, sin pestaña Comunidad. Ver `docs/ARCHITECTURE.md` §7.6.0.1.
- **2026-09-04 — "Capa de Plantillas DBV" (`dbv-template.toml`) formalizada y ampliada más allá de los campos de formulario del asistente**, a petición del usuario: también `dbv_category` (taxonomía académica propia, TFG/TFM no existen como categorías oficiales de Typst Universe), `localization` (nombre/descripción por idioma), `screenshots` (galería más allá del `thumbnail` oficial), `defaults` (valores precargados desde configuración local, nunca remota) y `validation` (reglas por campo). Sigue siendo estrictamente opcional y aditivo — nunca sustituye al `typst.toml` oficial, principio Universe-First. **Diseño para enriquecer plantillas comunitarias (no propias) con esta capa (spike resuelto a nivel de diseño):** el sidecar no debe co-ubicarse dentro de la caché de paquetes de Typst (fuera del control de DBV, sobrescribible por el propio CLI al actualizar); usar en su lugar un overlay propio de DBV indexado por `(namespace/nombre, versión)`, con degradación limpia a "sin formulario, solo scaffolding" si no hay overlay para la versión instalada — evita el riesgo de metadatos desincronizados de plantillas de terceros. Ver `docs/ARCHITECTURE.md` §7.6.3 y riesgo en §6.
- **2026-09-04 — 🔒 Congelación de especificaciones y arquitectura (v1.0) tras Architecture Review final.** `SPECIFICATIONS.md` y `ARCHITECTURE.md` pasan de "en definición/borrador" a baseline congelada del MVP. A partir de aquí, cualquier cambio de requisito o de arquitectura exige ADR previo en este fichero + nueva versión del documento + revisión de impacto en `implementation_plan.md` — no se cambia la arquitectura de facto durante `/build`. Cuatro rondas de refinamiento del usuario quedaron integradas antes de congelar.
- **2026-09-04 — Adversarial Architect Review del plan del MVP: condición de carrera en el bucle de vista previa.** El debate identificó que compilar con el sidecar `typst` una vez por ciclo de debounce sobre una tesis larga puede producir (1) acumulación de procesos hijo sin cancelar, (2) resultados fuera de orden pintando páginas obsoletas, (3) directorios temporales huérfanos y (4) preview en blanco ante cualquier error de sintaxis transitorio mientras se escribe. Resolución incorporada como criterios de aceptación obligatorios del Slice 5: cancelación del proceso en curso, **token de generación monótono** (descarta resultados tardíos), `TempDir` por compilación y patrón "última vista buena" con banda de error no intrusiva. Riesgo residual R-03 (latencia en documentos muy largos) aceptado con plan B documentado: migrar a `typst watch`.
- **2026-09-04 — Gate de CI de app nativa resuelto para el MVP:** Linux automatizado por GitHub Actions (AppImage + .deb, adjuntados a Release en borrador), Windows compilado manualmente por el mantenedor (mismo patrón que dbv-md-reader, firma local), macOS fuera del MVP. Paso de CI nuevo respecto a dbv-md-reader: **vendorizado del binario oficial `typst` en `src-tauri/binaries/` antes de `tauri build`**, con fallo explícito si falta (nunca producir un instalador sin compilador dentro).
- **2026-09-04 — Campos base canónicos del Asistente de creación de proyecto acordados:** nombre del proyecto, título, autor, institución, supervisor/tutor, curso/año académico — comunes a la mayoría de plantillas académicas, cada plantilla puede añadir campos propios vía `[[fields]]`. El asistente se posiciona como diferenciador de producto explícito: el usuario "crea un documento", no "inicializa un paquete". Ver `docs/ARCHITECTURE.md` §7.6.4.

- **2026-09-04 — Aprobación del usuario para pasar a `/build` con alcance MVP reducido, más cuatro ajustes finales.** (1) v0.1 = slices 1-7, slice 8 con 4 plantillas (Proyecto en blanco, TFG, Artículo académico, CV), slice 9 solo PDF, slice 10; `.dbvt` y las otras 4 plantillas → v0.2. (2) Mantener la arquitectura preparada para Universe desde el principio pese a ser Beta (formalizado como restricciones R-MVP-1..4 en `docs/ARCHITECTURE.md` §0.2). (3) Abrir proyectos ajenos (repos Git clonados, proyectos Typst preexistentes, plantillas comunitarias) es flujo de primer nivel, no un caso secundario → nuevo RF-02b. (4) Operaciones de proyecto explícitas en MVP (Abrir carpeta / Mostrar en el explorador / Recientes) → nuevo RF-02c. `SPECIFICATIONS.md` sube a v1.1 siguiendo la propia regla de congelación (ADR + nueva versión + revisión de impacto en el plan).
- **2026-09-04 — Regla de trabajo para `/build` fijada por el usuario: priorizar software funcionando sobre refinamiento arquitectónico.** No se reabren decisiones congeladas salvo que (a) aparezca un bloqueante real, (b) una asunción del research phase resulte falsa, o (c) se cree un ADR explícito para ello. En esas tres situaciones el procedimiento es el ya establecido: ADR primero, cambio después.

- **2026-09-04 — El objetivo de instalador `<30 MB` deja de ser restricción dura (decisión del usuario).** Riesgo R-01 cerrado por decisión, no por medición. **Epílogo medido en el Slice 2: la restricción no hacía falta relajarla.** El instalador quedó en **18 MB**, por debajo del objetivo original, *pese* a llevar dentro el compilador Typst (51 MB sin comprimir).
- **2026-09-04 — `webviewInstallMode: offlineInstaller` (heredado de DBV Markdown Reader) inflaba el instalador a 268 MB.** El coste no era Typst sino el instalador **offline de WebView2** embebido (~200 MB). Cambiado a `downloadBootstrapper` → **268 MB → 18 MB**. Contrapartida asumida: instalar en una máquina que *a la vez* carezca de WebView2 y de conexión falla; en Windows 11 y en Windows 10 actualizado WebView2 viene con el sistema, y la promesa offline-first del producto es sobre **usar** la aplicación, no sobre instalarla sin red. **Decisión cerrada el 2026-09-05** (el usuario delega el criterio): se mantiene `downloadBootstrapper` por defecto y se añade un overlay opcional `src-tauri/tauri.windows.offline.conf.json` (`npm run build:win:offline`) que reconstruye el instalador offline de 268 MB sin tocar la configuración por defecto — el caso de aulas educativas aisladas es real dentro del público objetivo, así que la alternativa se conserva como variante de empaquetado explícita en vez de como una línea que haya que recordar cambiar a mano. El proyecto hermano tomó la decisión contraria por defecto; aquí se invierte el valor por defecto sin perder la capacidad. Lección transversal: heredar configuración de empaquetado sin medirla puede costar un orden de magnitud.
- **2026-09-04 — Slice 2: tres supuestos del research phase resultaron FALSOS al verificarlos contra el binario real.** Es exactamente el escenario para el que existía el gate de verificación, y el motivo por el que el research phase no bastaba por sí solo:
  1. **`typst init` NO acepta rutas de fichero.** El `--help` dice *"supports both local and published templates"*, pero una ruta falla con `package specification must start with '@'`: "local" significa el **namespace `@local`**. Mecanismo correcto verificado: `typst init --package-path <dir> @local/<n>:<v> <destino>`, que además no escribe en el directorio de datos del usuario. Las plantillas curadas de DBV deben desplegarse como árbol `local/<nombre>/<versión>/`. Corregido en `ARCHITECTURE.md` §7.2/§7.6.3 y R-MVP-2.
  2. **`typst query` está deprecado** en 0.15.1 — el propio CLI recomienda `typst eval`.
  3. **`query --field location` devuelve `[]`**: no sirve para el outline. Pero **`typst eval` sí**, y con creces (ver siguiente entrada).
- **2026-09-04 — Spike del outline CERRADO en el Slice 2, adelantándose a Beta y a favor.** `typst eval 'query(heading).map(h => (nivel: h.level, texto: h.body, pagina: h.location().page(), y: h.location().position().y))' --in main.typ --format json` devuelve nivel, texto, **número de página y coordenada vertical** por encabezado — todo lo que necesita la navegación clic→posición del panel de outline (§7.8). **Se descarta el plan B con `tinymist`** para esta función. Aviso de implementación: el campo `body`/`texto` llega como objeto anidado `{func, text}`, no como cadena.
- **2026-09-04 — El sidecar se invoca siempre desde Rust, nunca desde el frontend.** Consecuencia deliberada: el WebView no necesita ningún permiso de shell en `capabilities/main.json` (siguen bastando `core:default` + `dialog:default`), y toda ejecución del compilador pasa por comandos propios validados. Esto también encauza el futuro terminal avanzado (§7.14): no se expondrá shell al WebView, sino un comando nuestro.

- **2026-09-05 — Slice 4: el riesgo R-02 (calidad del modo de lenguaje Typst para CodeMirror 6) se cierra a favor; se descarta el plan B de gramática propia.** El spike encontró `codemirror-lang-typst` (Apache-2.0, mantenido, versión 0.6.0 de agosto de 2026) que ofrece **dos** implementaciones: `typst()`, que consigue el resaltado ejecutando la crate oficial `typst-syntax` compilada a WebAssembly, y `typst_lezer()`, un parser Lezer nativo de la sintaxis de Typst 0.15 **sin WASM**. **Se elige `typst_lezer()`**: arrastrar un binario WebAssembly al bundle contradiría el argumento de instalador ligero que motivó ADR-EDITOR-001, y la variante Lezer ya aporta árbol sintáctico real, plegado, indentación, linter y autocompletado de las funciones, tipos, módulos y símbolos matemáticos integrados de Typst 0.15. Consecuencia medida: el bundle del frontend pasa de 17 kB a **520 kB (161 kB gzip)**, por encima de los 200-400 kB estimados en ADR-EDITOR-001 — la diferencia son los datos de autocompletado de la biblioteca estándar de Typst, que son exactamente el tipo de valor que el producto quiere dar y viajan en local, sin red. Sigue siendo un orden de magnitud menos que Monaco.

- **2026-09-05 — Slice 5: R-03 medido contra el binario real y cerrado a favor; el cuello de botella NO era la latencia de compilación sino el VOLUMEN de SVG.** Una tesis sintética de **209 páginas** (550 kB de fuente) compila a SVG en **0,85 s** y a PDF por stdout en 0,78 s — muy por debajo de lo que exigía el plan B (`typst watch`), que queda descartado para el MVP. Pero esas 209 páginas ocupan **82 MB de SVG** (≈400 kB por página, porque cada página embebe los contornos de sus glifos): devolverlas enteras por el puente IPC en cada ciclo de debounce habría hecho inusable justo el escenario insignia del producto. **Corrección de diseño aplicada:** una compilación devuelve la *geometría* de todas las páginas (leída de los primeros 512 bytes de cada SVG, sin cargar el fichero) y el *marcado* solo de las visibles; el `TempDir` de la compilación vigente se conserva vivo en `EngineState` y las demás páginas se sirven una a una con `typst_preview_page` mediante `IntersectionObserver`. Sustituir la sesión destruye el `TempDir` anterior, así que sigue habiendo como mucho un directorio de páginas vivo.
- **2026-09-05 — Slice 5: la vista previa de un documento con cambios sin guardar compila un espejo oculto (`.dbv-preview.typ`) junto al documento real, no un fichero en el directorio temporal del sistema.** Motivo: Typst resuelve las rutas relativas (`#include "chapters/01.typ"`, `image("images/x.png")`) respecto a la ubicación del fichero compilado; un espejo en `%TEMP%` rompería todos los `#include` e imágenes del proyecto. Contrapartida asumida: durante unos milisegundos aparece un fichero oculto en la carpeta del usuario — se borra siempre al terminar la compilación, está filtrado en el explorador de proyecto y en el watcher (si disparase el watcher, cada compilación provocaría la siguiente: un bucle), y **solo se escribe cuando hay cambios sin guardar**, así que abrir y leer un repositorio ajeno nunca escribe nada (RF-02b).
- **2026-09-05 — Slice 5: verificado contra el binario real que el patrón `{0p}` rellena con ceros hasta el ancho del número TOTAL de páginas** (209 páginas → `page-001.svg`), no a un ancho fijo. Consecuencia: el orden lexicográfico de los ficheros es el orden de página correcto dentro de una misma compilación. Queda fijado con un test para que un cambio futuro del patrón no reordene las páginas en silencio.

- **2026-09-05 — Slice 7: las plantillas curadas de DBV son AUTOCONTENIDAS; el proyecto generado no importa su propio paquete.** Verificado contra el binario real: si `template/main.typ` hace `#import "@local/dbv-tfg:1.0.0"`, el proyecto resultante **solo compila si se le vuelve a pasar `--package-path`** — y forzar ese flag en toda compilación taparía los paquetes `@local` propios del usuario (`--package-path` sustituye al directorio de paquetes locales del sistema, no se suma a él). Diseño adoptado: la carpeta `template/` incluye su propio fichero de estilo y `main.typ` lo importa por ruta relativa; `typst init` copia el árbol entero. Consecuencia deseable y alineada con el principio Universe-First: **un proyecto creado con DBV Typst Editor compila con `typst` a secas, sin DBV de por medio** — no hay atadura al producto. Refina R-MVP-2 sin contradecirlo: el scaffolding sigue yendo siempre por `typst init`.
- **2026-09-05 — Slice 7: `npm run verify:templates` como criterio de aceptación ejecutable del catálogo.** En vez de dejar "cada plantilla compila sin errores y su formulario genera un documento coherente" como prueba manual, el script hace por cada plantilla: `typst init` real → sustitución de marcadores con valores de ejemplo acentuados → comprobación de que no queda ningún `{{...}}` huérfano → compilación a PDF con verificación de tamaño. Es la misma lección del Slice 2 (verificar contra el binario real) aplicada a contenido en vez de a flags.

- **2026-09-05 — Slice 8: las plantillas curadas usan EXCLUSIVAMENTE las fuentes que vienen dentro del compilador Typst** (`Libertinus Serif`, `New Computer Modern`, `New Computer Modern Math`, `DejaVu Sans Mono` — comprobado con `typst fonts --ignore-system-fonts`). Motivo descubierto al verificar: una lista de respaldo con fuentes del sistema (`Inter`, `Segoe UI`, `Helvetica Neue`) hace que Typst emita **un aviso por cada familia que no encuentra**, y esos avisos aparecerían en la banda de la vista previa **en cada recompilación**, enseñando al usuario a ignorar la banda justo cuando le avise de algo suyo que sí importa. Beneficio adicional: el documento se compone exactamente igual en cualquier máquina, sin conexión y sin instalar nada — coherente con la promesa offline-first. `npm run verify:templates` incluye ahora una comprobación de "compila sin avisos" para que la regla no se pierda al añadir plantillas nuevas.

### ADR-UNIVERSE-001 — Typst Universe entra por lista curada + identificador libre (plantillas y paquetes)

*Registrada el 2026-09-05. Desbloquea el mayor de los puntos que estaban "deliberadamente fuera" desde la sesión autónoma, y lo hace porque el usuario tomó la decisión editorial que faltaba — no era un problema técnico.*

- **Contexto medido, no supuesto:** `packages.typst.org/preview/index.json` son 2,1 MB con **4.675 paquetes, 2.266 de ellos plantillas**. Y el mecanismo de instalación ya funcionaba sin escribir una línea: `typst init @preview/charged-ieee destino` descarga, cachea y genera el proyecto — es el MISMO comando que las plantillas curadas, cambiando `@local` + `--package-path` por el namespace público. Lo que faltaba nunca fue código, era la política.
- **Decisión del usuario (mezcla de dos opciones que se le ofrecieron):** lista revisada **y** campo libre para pegar cualquier identificador. La lista da confianza a quien no quiere juzgar código de terceros; el campo no le pone un techo a quien sabe lo que busca. Además señaló algo que faltaba en el planteamiento inicial: en Universe **no solo hay plantillas, también paquetes** que se importan igual (`#import "@preview/quick-maths:0.2.1"`), así que el panel tiene dos pestañas de igual peso — coherente con §7.6.0.1, que ya pedía Plantillas y Paquetes como dos ramas, no una.
- **El catálogo curado se generó contra el registro real, no de memoria** (`universe/curatedCatalog.js`): 6 plantillas y 14 paquetes con nombre, versión exacta y licencia comprobados el 2026-09-05. En la primera pasada, uno de los candidatos "obvios" (`association-for-computing-machinery-article`) **no existía** con ese nombre; el real es `faithful-acmart`. Al actualizar el catálogo hay que repetir la comprobación: una versión inventada es un error de descarga en la cara del usuario.
- **Cada tarjeta muestra identificador y licencia.** Es código de terceros que se descarga y ejecuta en la máquina del usuario; ocultarle qué instala y bajo qué condiciones sería incoherente con el aviso que el propio panel le da.
- **Validación duplicada a propósito** (`universe.rs::parse_universe_spec` y `universe/universeSpec.js`): la de Rust es la que protege —es la que decide qué llega como argumento a un proceso hijo, y por eso tiene tests de rutas maliciosas (`@preview/../../etc/passwd:1.0.0`)—; la de JS existe para avisar al usuario mientras escribe sin un viaje al backend por tecla. Si divergieran, el síntoma sería un mensaje de error menos amable, nunca un agujero.
- **La importación de un paquete va SIEMPRE al principio del documento, tras los `#import` que ya haya**, no donde esté el cursor: Typst resuelve las importaciones en orden y ponerlo en medio dejaría el paquete sin definir para todo lo escrito por encima, con un "unknown variable" que no señala a la causa. No se duplica si ya estaba.
- **Degradación limpia y declarada:** una plantilla de Universe no trae `dbv-template.toml`, así que no tiene formulario — el asistente solo pide nombre y ubicación. Es lo que ya preveía el diseño ("sin overlay, solo scaffolding"), y está dicho en la ayuda para que no se lea como una carencia.

### ADR-ACTUALIZADOR-001 — Auto-actualización activada, con la clave de firma generada por el usuario

*Registrada el 2026-09-05. **Levanta el bloqueo** que `UPGRADE_PROMPT.md` §4 imponía desde el principio ("la clave de firma la genera el usuario en su propio terminal, nunca la IA"): la regla no se ha roto, se ha cumplido — el usuario generó el par de claves y entregó **únicamente la pública**, que es la que va al repositorio.*

- **Decisión:** `tauri-plugin-updater` + `tauri-plugin-process`, ambos `#[cfg(desktop)]`. Clave pública en `tauri.conf.json` (`plugins.updater.pubkey`), endpoint en `releases/latest/download/latest.json` del repositorio público, y `bundle.createUpdaterArtifacts: true`.
- **Dos reglas de comportamiento heredadas de DBV Markdown Reader, conscientes:** (1) **nunca se comprueba al arrancar**, solo cuando el usuario pulsa el botón — una aplicación offline-first no debe salir a la red por su cuenta al abrirse; (2) en una instalación **de Microsoft Store el botón no se muestra**: allí actualiza la Store, y descargar y ejecutar un instalador NSIS dentro del entorno aislado de MSIX fallaría o crearía una segunda instalación desconectada. La detección de origen ya existía (`commands::app_info::is_packaged_app`, por ruta `WindowsApps`).
- **Un solo botón que cambia de función** ("Buscar actualizaciones" → "Instalar y reiniciar") en vez de dos: el segundo solo tendría sentido durante unos segundos.
- **Los módulos del actualizador se importan de forma perezosa** (`await import(...)` dentro del manejador). Fuera de la ventana de Tauri —tests de Vitest, `verify:frontend`— esos módulos no existen, y un import estático habría reproducido exactamente el fallo de arranque del Slice 4 (un import que compila pero revienta la interfaz al abrirla).
- **Solo Windows tiene auto-actualización, y es deliberado — corrección de un error propio de esta misma sesión.** El primer intento metía la clave privada como secreto de GitHub para firmar en CI; el usuario lo paró señalando el patrón real de la casa, y tenía razón: `NATIVE_APPS_RELEASE_CI.md` §7 descarta explícitamente esa vía, porque fusionar en un mismo `latest.json` una firma generada en CI con otra generada en la máquina del mantenedor es una coordinación cross-máquina frágil. El reparto correcto, que es el que queda: **Windows se compila y firma en la máquina del mantenedor** (`createUpdaterArtifacts: true` del `tauri.conf.json` base) y es la única plataforma con actualización automática; **Linux y macOS se compilan en CI sin firmar** (`createUpdaterArtifacts: false` en sus overlays, `includeUpdaterJson: false` en sus workflows) y se actualizan a mano. Deuda técnica consciente y registrada, no un olvido.
- **`scripts/updater-manifest.mjs` (nuevo) cierra el hueco que abre ese reparto:** en CI, `tauri-action` genera el `latest.json` solo; en un build local NO, así que el manifiesto habría que escribirlo a mano — justo donde se cuela una firma pegada a medias o una URL con la versión anterior. El script lo compone leyendo el `.nsis.zip` y su `.sig` recién generados. `npm run updater:manifest`.
- **`createUpdaterArtifacts: true` hace que `tauri build` FALLE en local si no están las variables `TAURI_SIGNING_*`.** Es deliberado y hay que saberlo: preferible un build que se niega a otro que publica en silencio una Release que ningún usuario podrá instalar por no ir firmada.
- **Custodia de la clave:** la privada nunca entra en el repositorio, ni en la conversación, ni en un log, ni —por lo anterior— en los secretos de GitHub; vive solo en la máquina del mantenedor. Si se perdiera, ninguna versión futura podría actualizar a las ya instaladas: habría que publicar una versión nueva con otra pubkey e instalarla a mano.

### ADR-FUENTES-001 — Un proyecto puede traer sus propias fuentes en `fonts/`

*Registrada el 2026-09-05, a petición del usuario y motivada por un caso de uso real de esa misma sesión (no por una hipótesis de diseño). Alcance aditivo: no reabre ninguna decisión congelada.*

- **Contexto:** el usuario abrió el proyecto de un compañero (RF-02b) y la vista previa avisó `unknown font family: flux` en cada recompilación. Tenía que instalar la fuente en el sistema para verlo como su autor — y aun así resultó ser una homónima distinta (hay varias familias llamadas "Flux" sin relación entre sí; la instalada era una decorativa de MonibDesign de 1996). El documento se componía, pero no igual que en la máquina de origen.
- **Problema de producto:** un proyecto compartido sólo se compone igual en otra máquina si sus fuentes viajan con él. Exigir una instalación manual en el sistema es justo la fricción que este producto existe para quitar, y además no siempre es posible (aulas, equipos sin permisos de administrador) — contradice tanto la promesa offline-first como el principio de "el usuario no debe pelearse con la infraestructura". Es además incoherente con que las plantillas propias ya sean autocontenidas por diseño (fuentes embebidas en el compilador, ver la entrada del Slice 8).
- **Decisión:** si un proyecto tiene una carpeta `fonts/` en su raíz, la aplicación se la pasa al compilador (`--font-path`) en **todas** las invocaciones que componen el documento: vista previa, exportación a PDF, exportación a PNG y el esquema (`typst eval`, que también compone para resolver `h.location()` y emitiría los mismos avisos). `typst_engine::font_path_args()`, función pura y testeada.
- **Verificado contra el binario real ANTES de implementar** (misma disciplina del Slice 2, y aquí decidía el diseño entero): `--font-path` **añade** directorios, no sustituye a las del sistema —para eso está `--ignore-system-fonts`—, busca de forma **recursiva**, y lo aceptan tanto `compile` como `eval`. Comprobado además de extremo a extremo con el caso real: el mismo documento, con `--ignore-system-fonts` en ambos lados, pasa de `warning: unknown font family: flux` (19.434 bytes de SVG, tipografía de respaldo) a compilar sin un solo aviso (17.112 bytes, glifos reales) — el tamaño distinto prueba que compone con la fuente del proyecto, porque el SVG incrusta los contornos.
- **Consecuencia deliberada y de la que depende media función:** el export `.dbvt` (RF-11) ya empaqueta el árbol entero, así que la carpeta `fonts/` viaja dentro del archivo sin tocar nada — un `.dbvt` pasa a ser un proyecto verdaderamente portable. Se fija con un test propio (`export_incluye_las_fuentes_propias_del_proyecto`) para que un filtro futuro de "ruido de repositorio" no las excluya por accidente y rompa la promesa en silencio.
- **Alternativas descartadas:** (a) instalar las fuentes del proyecto en el sistema desde la app — invasivo, necesita permisos y ensucia la máquina del usuario con fuentes de documentos ajenos; (b) `--ignore-system-fonts` para forzar reproducibilidad estricta — rompería todos los proyectos que hoy dependen de fuentes instaladas, sin pedirlo nadie.

### ADR-EDITOR-001 — CodeMirror 6 en lugar de Monaco Editor (decisión definitiva)

*Registrada a petición explícita del usuario antes de iniciar `/build`, para preservar el razonamiento. La decisión **no** se reabre; este ADR es la referencia canónica si alguien la cuestiona en el futuro.*

- **Contexto:** DBV Typst Editor necesita un editor de código real (el proyecto origen, DBV Markdown Reader, solo tenía un `<textarea>` plano). El usuario pidió explícitamente, en dos ocasiones, evaluar Monaco como opción principal, citando minimapa, multi-cursor y rendimiento en documentos largos.
- **Decisión:** **CodeMirror 6**.
- **Ventajas de CodeMirror 6:**
  - Coherencia con la propuesta de valor de toda la familia DBV (ligero, offline-first, arranque instantáneo).
  - Modelo de extensiones (decoraciones, widgets, paneles) pensado exactamente para UI de edición enriquecida — es el mecanismo con el que Obsidian implementa su "Live Preview", y es el mismo patrón que necesitan los asistentes de inserción rápida y el objetivo de "el usuario no debe ver código si no quiere".
  - ESM nativo y tree-shakeable: solo se paga por lo que se usa.
  - Es la base del editor web oficial de Typst, lo que maximiza la probabilidad de que exista y se mantenga un modo de lenguaje Typst de calidad.
  - Obsidian —la referencia de producto explícita del propio usuario— está construido sobre CodeMirror 6, no sobre Monaco: prueba de que el nivel de calidad buscado es alcanzable con esta base.
- **Desventajas asumidas:**
  - **Minimapa**: no es nativo; existe como extensión de comunidad, menos madura que la de Monaco. Es la **única ventaja real y no discutible de Monaco** tras dos evaluaciones. Se difiere a Beta como mejora opcional.
  - Curva de adopción mayor: API funcional basada en extensiones componibles frente al enfoque "todo incluido" de Monaco.
  - Integración LSP (`tinymist`, Beta) requiere algo más de trabajo manual (`codemirror-languageserver`) que en Monaco, que nació para LSP.
  - Obliga a introducir un bundler (Vite), rompiendo el patrón "sin bundler" heredado — coste aceptado y registrado como ADR aparte.
- **Implicaciones de tamaño de bundle:** CodeMirror 6 ≈ 200-400 KB (núcleo, modular) frente a Monaco ≈ 2-5 MB (motor completo de VS Code). En un producto cuyo argumento comercial es un instalador ligero y arranque en frío por debajo de los 200 ms, la diferencia es material, no cosmética — sobre todo sumada al peso del sidecar `typst` ya presente en el instalador.
- **Implicaciones de rendimiento:** ambos manejan bien documentos largos; CodeMirror 6 usa una estructura de datos tipo *rope*. A la escala real del producto (una tesis de cientos de páginas, no un repositorio de código), el rendimiento **no es un factor diferenciador** entre ambos: el motivo citado a favor de Monaco no se sostiene en este caso de uso concreto.
- **Soporte del ecosistema Typst:** no hay gramática Typst oficial activamente mantenida para Monaco; para CodeMirror 6 existe ecosistema comunitario y el precedente del editor web oficial de Typst. Riesgo residual R-02 del plan (calidad real del modo de lenguaje) con plan B definido: gramática mínima propia suficiente para el MVP.
- **Consecuencias:** introducción de Vite; el minimapa queda como deuda funcional consciente evaluable en Beta; la integración LSP de Beta requerirá trabajo adicional respecto a lo que habría costado con Monaco.

### ADR-EDITOR-002 — La barra de herramientas de inserción del editor sube de Beta a v0.2

*Registrada el 2026-09-05 a petición explícita del usuario, que la señaló como funcionalidad que "debería estar desde el principio". Aplica la regla de congelación de `SPECIFICATIONS.md`: ADR primero, cambio de specs después.*

- **Contexto:** la barra de herramientas de inserción existe en DBV Markdown Reader (16 botones: negrita, cursiva, tachado, código en línea, H1-H3, listas, enlace, imagen, cita, bloque de código, tabla, regla) y estaba inventariada como componente reutilizable #19 en `ARCHITECTURE.md` §3, con su equivalente Typst ya diseñado en §7.7. Pero el recorte de alcance de v1.1 la agrupó con los "asistentes de inserción rápida" y la difirió entera a Beta (`SPECIFICATIONS.md` §6).
- **Problema con esa clasificación:** contradice el principio de producto nº2 de las propias specs — *"el usuario no debe ver código si no quiere"* — y el Escenario D de §2, que describe pulsar "Insertar figura" como el flujo normal. Un editor Typst con resaltado de sintaxis pero sin ningún botón de inserción es, exactamente, "un editor de código con soporte Typst": el posicionamiento que el usuario descartó explícitamente. Además es una regresión percibida frente a DBV Markdown Reader, el producto hermano del que viene el usuario.
- **Decisión:** la barra de herramientas pasa a **v0.2** como requisito propio, **RF-13**, separada de los asistentes con mini-formulario (que siguen en Beta). El criterio de corte entre ambos es el coste: un botón que emite marcado en la posición del cursor es una transacción de CodeMirror 6 y no necesita infraestructura nueva; un asistente que abre un formulario y valida datos (galería de símbolos con búsqueda, tabla con dimensiones, cita con autocompletado sobre el `.bib`) sí.
- **Objetivo de calidad fijado por el usuario:** no basta con paridad con DBV Markdown Reader — la barra debe **superar a la del editor web oficial de Typst**. Se traduce en compromisos verificables, no en una aspiración: sensibilidad al contexto (los botones cambian dentro de `$...$`), envolver la selección en vez de solo insertar, alternancia (volver a pulsar quita el marcado), un único paso de deshacer por acción, atajos de teclado con el atajo visible en el tooltip, i18n ES/EN y funcionamiento sin conexión.
- **Consecuencias:** `SPECIFICATIONS.md` sube a v1.2; `ARCHITECTURE.md` §7.7 se reescribe con el inventario de botones y cambia de fase; `implementation_plan.md` registra el Slice 11 de v0.2. **No** afecta a v0.1, ya construida y validada: es alcance nuevo, no una corrección.
- **Verificación pendiente antes de construir (deuda explícita, misma lección que el Slice 2):** el inventario real de la barra del editor web de Typst debe comprobarse **contra la aplicación web real**, no de memoria, antes de escribir la tabla comparativa definitiva. Lo escrito hoy en §7.7 es el objetivo de producto de DBV, no una descripción verificada del producto ajeno.
- **2026-09-05 — Slice 11 completado; la deuda de verificación de §7.7 sigue abierta, deliberadamente.** Se implementó el diseño de §7.7 tal cual —21 botones, `toolbarActions.js`/`toolbar.js`, 24 tests— pero **no** se comprobó la barra del editor web oficial de Typst contra la aplicación real (sin acceso a ella en esta sesión), así que ni los glifos elegidos ni la comparación "supera a la barra oficial" del objetivo de producto están verificados todavía — solo implementados según el objetivo declarado. Tampoco se pudo abrir la ventana Tauri real para una comprobación visual: la verificación se apoya en `verify:frontend` (56 elementos) y en tests de lógica pura, no en una captura de la interfaz. Ambas cosas quedan como trabajo pendiente antes de dar el Slice 11 por visualmente terminado.
- **2026-09-05 — Nodos reales del árbol Lezer de Typst, verificados contra el binario del parser (no supuestos):** `Equation` es el nodo que envuelve una ecuación completa (`$...$`), con `Math`/`MathAttach`/`MathText` dentro; `Strong` es `*negrita*` y `Emph` es `_cursiva_` — confirma que el mapeo Typst↔Markdown de la tabla de RF-13 es correcto y no una suposición. Usado para `isInsideMath()`, que resuelve el nodo en la posición del cursor por ambos lados (`resolveInner(pos, -1)` y `(pos, 1)`) y sube por los padres buscando `'Equation'`.

## ⚠️ Lecciones Aprendidas

- **El pipeline "watch(directorio padre) → debounce → supresión de auto-eco → re-render preservando scroll" de DBV Markdown Reader es el activo más valioso a heredar**, muy por encima de cualquier componente visual concreto — es exactamente la arquitectura que necesita un editor con vista previa en tiempo real, independientemente del formato (Markdown u otro lenguaje de composición).
- **DBV Markdown Reader es, pese a su nombre "Reader/editor", fundamentalmente un visor**: su capacidad de edición es un `<textarea>` plano sin resaltado, autocompletado ni plegado. No confundir "hay un modo de edición" con "hay un editor de código reutilizable" — esta distinción cambió sustancialmente el alcance de esfuerzo estimado para el MVP de DBV Typst Editor.
- **El sistema de plantillas de DBV Markdown Reader (`templates/`) no tiene ninguna integración en la app** (ni comando Rust, ni selector UI, ni siquiera existe "Guardar como") — es contenido estático documentado en un README, copiado manualmente por el usuario. Para DBV Typst Editor, el patrón organizativo (bilingüe, por categorías) es reutilizable, pero la carga/scaffolding desde la app es trabajo 100% nuevo.
- **Cuando un stakeholder pide re-evaluar una decisión ya tomada (aquí: Monaco vs. CodeMirror), la forma correcta de responder es una re-evaluación explícita con los nuevos criterios citados uno por uno — no ignorar la petición ni revertir la decisión por inercia.** En este caso la propia referencia de producto que motivó la petición (Obsidian) terminó siendo la evidencia más fuerte a favor de la decisión original.
- **Cuando un stakeholder revierte una decisión con una instrucción explícita e inequívoca ("shall"), no es un caso de re-evaluación — es una directiva a aplicar, propagando sus consecuencias en cascada a todas las secciones que asumían la decisión anterior**, no solo a la sección donde se tomó originalmente (aquí: §7.6 sobre paquetes/plantillas asumía la API de `typst-kit`, y tuvo que corregirse en la misma sesión al cambiar §7.2 de crates embebidas a CLI sidecar). Rastrear y actualizar todas las dependencias cruzadas de una decisión es tan importante como la decisión en sí.
- **Los tres fallos que rompieron la aplicación en manos del usuario eran de la MISMA categoría, y ninguno lo detectaba la batería de verificación:** un import usado con la forma equivocada, una API del navegador que el WebView no permite sin permiso explícito, y una suposición sobre qué fichero alimenta la vista previa. Los 77 tests de Rust, las 28 comprobaciones contra el compilador y el `vite build` daban verde con la aplicación inservible. La conclusión operativa para este proyecto: **el frontend es la capa con menos red de seguridad, y "compila y empaqueta" no es evidencia de que arranque.** De ahí `npm run verify:frontend`, y de ahí que la fase `/test` no sea negociable antes de `/ship`.
- **Un `import` que compila no es un `import` que funciona: hay que comprobar la FORMA de lo que se importa, no solo que exista.** `codemirror-lang-typst` exporta `typstLezerListKeymap` ya como extensión de CodeMirror (un `PrecExtension`), no como el array de atajos que su nombre sugiere; usarlo con `...typstLezerListKeymap` dio un `TypeError` que **ninguna de las verificaciones existentes detectó** —`vite build` empaqueta sin ejecutar, los 77 tests de Rust no tocan el frontend y las 28 comprobaciones contra el compilador tampoco— y que solo apareció al abrir la ventana, con la interfaz muerta detrás. Lección operativa: **cuando un paquete exporta varios nombres parecidos (`...Keymap`, `...Folding`, `...Highlighting`), comprobar en un `node -e` de qué tipo es cada uno antes de escribir el código que los consume**, igual que en el Slice 2 se verificó cada flag del CLI contra el binario real. El mismo error de categoría, en otra capa.
- **`EditorState.create()` valida la lista completa de extensiones de CodeMirror SIN necesidad de DOM** (solo `EditorView` necesita ventana). Ese hallazgo es lo que hace posible `npm run verify:frontend`, que reproduce el fallo anterior en Node en menos de un segundo. Se comprobó reintroduciendo el bug a propósito: 5 de las 7 comprobaciones pasan a fallo con el mensaje exacto que veía el usuario. El script cubre además la otra causa habitual de "ventana en blanco" en esta aplicación: un `getElementById` cuyo id no existe en `index.html` (55 elementos comprobados hoy).
- **Medir el escenario insignia completo, no solo su latencia.** En el Slice 5, la métrica que el plan pedía para R-03 (¿cuánto tarda en compilar una tesis de 200 páginas?) salió excelente —0,85 s— y aun así el diseño original era inviable: nadie había medido *cuánto ocupa* el resultado. 82 MB de SVG por compilación habrían hundido la aplicación en el mismo caso de uso que la métrica declaraba sano. Lección: cuando se fija una métrica de riesgo, comprobar también las magnitudes adyacentes del mismo escenario (tamaño, memoria, número de ficheros), porque el riesgo real puede estar en la de al lado.
- **Un research phase sobre documentación NO sustituye a verificar contra el binario real: en el Slice 2, 3 de los supuestos documentados resultaron falsos** — y uno de ellos (`typst init` con rutas locales) venía literalmente de una frase ambigua del `--help` oficial ("supports both local and published templates", donde "local" significaba un namespace de paquetes, no el sistema de ficheros). La lección operativa: en cuanto exista el binario/API real, **el primer slice que lo toque debe verificar cada supuesto documentado antes de construir encima** — barato entonces, carísimo tres slices después. El gate de verificación del Slice 2 se pagó solo.
- **Antes de comprometerse con una integración externa (aquí: ecosistema Typst), un research phase dedicado con fuentes oficiales primarias (manifiestos, manpages, READMEs del propio proyecto) descubre matices que invalidan supuestos razonables pero no verificados** — p. ej. asumir "packages" y "templates" como registros separados (razonable a priori, dada la UI de Typst Universe) resultó ser incorrecto: son el mismo dataset con un campo opcional. Vale la pena el research phase explícito antes de `/plan` cuando la integración con un ecosistema externo es central al producto.
- **2026-09-05 — Fase `/test`: `mockReset()` manual en `beforeEach` combinado con `restoreMocks: true` de la config de Vitest hace que un mock con implementación `async` que lanza se reporte como "Unknown Error" no gestionado, en vez de como el resultado normal de la promesa que `try/catch` captura dentro del código bajo prueba.** Costó una sesión de depuración aislada (fichero de scratch reduciendo el caso línea a línea) porque el síntoma —"Unknown Error: <mensaje>" sin traza al código del test— parece un fallo del código de producción (`services/backend.js`), y no lo es: `call()` funciona perfectamente en aislamiento con el mismo mock. La causa es la duplicación del reset (config global + `beforeEach` manual) justo antes de registrar la siguiente implementación. Lección operativa: si el proyecto fija `restoreMocks: true` globalmente, **no añadir un reset manual del mismo mock en `beforeEach`** — y si un mock que lanza async se reporta como error no gestionado en vez de como fallo de aserción, sospechar primero de una duplicación de reset antes que del código bajo prueba. Ver `src/services/backend.test.js`.
- **2026-09-05 — La cobertura del frontend antes de esta sesión era CERO tests de lógica** (solo `verify:frontend`, que comprueba forma de extensiones y existencia de elementos del DOM, no comportamiento). Se añade Vitest + jsdom con 33 tests sobre las cuatro funciones puras ya aisladas a propósito en el código (`isTypstPath`, `joinPath`, `baseName`, `validateFields`, `localizeTemplate`, `normalizeError`/`call`) — exactamente los candidatos que `task.md` había señalado como pendientes de la fase `/test`. `npm test` ahora ejecuta Vitest antes que los tests de Rust; CI (`ci.yml`) no necesitó cambio de pasos, solo de comentario, porque ya invocaba `npm test`.


- **2026-09-05 — Validación manual del Slice 11 (RF-13) por el usuario en la ventana real detecta dos fallos que ninguna verificación automática cubría, y confirma la lección de la fase `/test`.** (1) Pulsar sobre una cita insertada por la barra revienta la app entera con `TypeError: r.href.startsWith is not a function`. Causa raíz verificada compilando un `#cite()` real: Typst incrusta un `<a>` (espacio de nombres SVG) sin `href` alrededor de cada cita/referencia cruzada —el hueco se rellena solo al exportar a PDF—, pero `SVGAElement.prototype.href` devuelve SIEMPRE un `SVGAnimatedString`, nunca una cadena, tenga o no atributo. El runtime de la ventana intercepta clics en cualquier `<a>` asumiendo HTML y llama `.href.startsWith(...)` directamente. Corregido interceptando el clic en fase de captura sobre `pagesEl` (`preview.js`): estos `<a>` no son navegables en la vista previa, así que se cortan antes de que lleguen a ese código. (2) El zoom de la vista previa por encima del 100% recortaba el documento de forma asimétrica al hacer scroll —posible ver el hueco de la derecha pero no todo el contenido de la izquierda—: `align-items: center` en un contenedor flex con overflow no reparte el desbordamiento por igual en Chromium/WebView2. Corregido sustituyéndolo por `margin-inline: auto` en la propia página, la técnica estándar para overflow simétrico. **Patrón que se repite en este proyecto:** los fallos que rompen la aplicación en manos del usuario siguen siendo, en su mayoría, de la misma familia — una API asumida (aquí, `<a>` = HTML) que no vale para el contexto real (aquí, `<a>` dentro de SVG) — y ninguno lo detectan los 57 tests de Vitest ni las 8 comprobaciones de `verify:frontend`, porque ninguno monta un `EditorView`/SVG real ni simula un clic. Sigue faltando la prueba de integración de ventana real, diferida en `task.md`.

- **2026-09-05 — Sesión autónoma "avanza todo lo que puedas hasta Beta" (usuario ausente, repetida dos veces en la misma sesión): v0.2 cerrado y Beta funcionalmente completa salvo lo explícitamente bloqueado — 8 slices, 10 commits, CI en verde en cada uno.** Regla aplicada para decidir qué abordar: priorizar lo ya especificado sin preguntas abiertas, en slices verificados uno a uno (mismo método que `/build`), y dejar fuera explícitamente lo que exige una decisión del usuario (Universe Browser: whitelist de seguridad editorial), infraestructura no disponible en esta sesión Windows (empaquetado macOS), una regla ya registrada que lo impide (auto-actualizador: la clave de firma la genera el usuario, nunca la IA — `UPGRADE_PROMPT.md` §4), un research phase no hecho (LSP `tinymist`), un bloque de trabajo propio y mayor (sincronización editor↔preview por posición real), o una pregunta pendiente que no hacía falta reabrir (bibliografía visual completa: sigue sin resolverse qué crate BibTeX usar, `SPECIFICATIONS.md` §9; el asistente de citas de esta sesión la sorteó extrayendo solo claves con un escaneo ligero, sin parsear campos). Slices completados: Outline (§7.8), PNG de página actual (§7.12), terminal avanzado (§7.14), modos de escritura (§7.9), gestión de imágenes por arrastre (§7.10), y los cuatro asistentes con formulario de §7.7.4 (cita, figura por arrastre y por selector, símbolos, tabla). Detalle completo en `task.md`, sección "Deliberadamente fuera de esta sesión".
- **2026-09-05 — Ningún slice de Beta de esta sesión se ha visto en la ventana real de la aplicación.** Toda la verificación (105 tests de Rust + 66 de Vitest, `verify:frontend`, compilaciones directas contra el binario vendorizado, y en el caso de la galería de símbolos y del selector de imagen también contra la API real de `@tauri-apps/api`) es la misma familia de comprobación que en el Slice 11 YA dejó pasar dos fallos reales (el crash de `SVGAElement.href` y el recorte asimétrico del zoom) hasta que el usuario probó la app de verdad. Esta sesión, además, un test SÍ cogió un bug real antes de producción (`figureActionForPath(path)` llamado como `.buildTransaction(state)` en vez de invocado directamente, en dos sitios) — prueba de que la disciplina de test-antes-de-confiar funciona, pero no sustituye la pasada manual. La lección de la fase `/test` sigue vigente sin excepción: antes de dar por buena visualmente cualquiera de estas funcionalidades, hace falta la misma pasada manual que ya demostró su valor una vez en esta sesión.

- **2026-09-05 — Primera pasada manual de Beta con el usuario presente confirma exactamente la lección de las dos entradas anteriores: 3 de los 4 asistentes con formulario (Cite, Σ, Tabla) estaban rotos en la ventana real pese a 105/66 tests en verde, y la causa era la MISMA en los tres.** `panels/registerPanel.js` cierra un panel si detecta un clic fuera de él (`closeOnOutsideClick`), comparando el objetivo del clic contra la lista de `trigger` que el propio `registerPanel` conoce. Los tres asistentes abren su panel llamando a `openNear()` **desde fuera** de ese mecanismo (`editor/toolbar.js` invoca el `specialHandler` del botón, que no está registrado como `trigger`), así que el mismo clic que abre el panel se propaga hasta `document` y lo cierra de inmediato — se abre y se cierra en el mismo evento, de forma imperceptible; el síntoma para el usuario es "no pasa nada al pulsar". Los paneles que sí funcionaban (Outline/Terminal/Acerca de) lo evitaban por accidente de diseño: usan `trigger` + `toggle: true`, y ese camino sí llama a `event.stopPropagation()`. Corregido añadiendo la misma llamada en el punto donde `toolbar.js` invoca un `specialHandler`. **Lección operativa, generalizable más allá de este bug concreto:** cuando un mismo defecto aparece en varios sitios "independientes" que en realidad comparten una pieza de infraestructura común (aquí, los tres asistentes de §7.7.4 comparten `registerPanel`), buscar la causa en esa pieza compartida antes de depurar cada síntoma por separado — y ninguna suite de tests de este proyecto simula un `click` real con su fase de propagación completa hasta `document`, que es justo lo que hacía falta para atraparlo antes de la prueba manual.
- **2026-09-05 — La misma sesión de prueba manual pidió, y se implementaron, dos rediseños de UX explícitamente inspirados en `dbv-md-reader` (D:\Programacion\github-davidbuenov\dbv-md-reader), confirmando que sigue siendo la referencia de patrones de interacción de toda la familia DBV, no solo de arquitectura de bajo nivel.** (1) Los 4 modos de escritura fijos (`app/writingMode.js`) se sustituyen por 3 interruptores independientes (`app/workspacePanels.js`): proyecto/esquema, editor, vista previa, combinables libremente — el usuario los comparó explícitamente con la falta de flexibilidad de los modos fijos frente al equivalente de mdreader. (2) El panel de Outline pasa de flotante a una pestaña "Esquema" del panel lateral, junto a "Archivos" — patrón `#sidebar-tabs`/`setActiveTab` de `dbv-md-reader/src/filetree.js`, con la diferencia de que aquí no hay resaltado de scrollspy (el outline de Typst navega la vista previa, no un documento HTML con `IntersectionObserver` sobre encabezados reales). **Patrón de mdreader confirmado NO aplicable sin cambios: no existe ningún panel arrastrable en 2D en todo el proyecto** (solo splitters 1D de ancho/columna) — el arrastre libre del terminal avanzado (`panels/draggablePanel.js`) se construyó desde cero para esta sesión, generalizando la resta de coordenadas de puntero que ya usa `ui/splitter.js`.
- **2026-09-05 — Detección de Microsoft Store portada de `dbv-md-reader` (`commands/app_info::is_packaged_app`, mismo mecanismo: ¿algún componente de la ruta del ejecutable es literalmente `WindowsApps`?) pero SIN portar el auto-actualizador que la acompaña allí.** Motivo: la regla ya registrada más abajo en este mismo fichero (auto-actualizador: la clave de firma la genera el usuario en su propio terminal, nunca la IA, `UPGRADE_PROMPT.md` §4) sigue sin resolverse — no ha cambiado nada que la levante. El panel "Acerca de" usa la detección solo para decidir qué texto informativo mostrar (Store vs. instalación manual), no para ofrecer un botón de "buscar actualizaciones" que no tendría nada real que comprobar.

- **2026-09-05 — Una regla CSS huérfana e "inofensiva" durante meses se volvió un fallo visible sola, sin que nadie tocara el código: el motor cambió debajo.** `base.css` conservaba desde el Slice 1 el `.app-body { display: grid; place-items: center; padding: 24px }` que centraba la tarjeta del "Hola mundo". Cuando esa pantalla desapareció, `layout.css` añadió su propia regla `.app-body { display: block; padding: 0; overflow: hidden }` — y como la cascada resuelve **propiedad a propiedad, no regla a regla**, `display`, `padding` y `overflow` quedaron pisados pero **`place-items` no**. Daba igual: `justify-items` no hacía nada en un contenedor de bloque. Hasta que Chromium implementó `justify-items`/`justify-self` para cajas de bloque y la propiedad zombi cobró vida: todo hijo de `.app-body` (lanzador y espacio de trabajo enteros) pasó a encogerse a su contenido y centrarse, dejando media ventana muerta. **Dos lecciones distintas y las dos operativas:** (1) una regla duplicada para el mismo selector no es sólo deuda estética — deja propiedades sin pisar que son bombas de relojería a merced de la evolución del motor; regla del proyecto a partir de aquí: **un selector estructural, una sola definición**. (2) Cuando el usuario dice "hay un nivel por encima de lo que estás mirando", conviene creerle: dos intentos de arreglo por lectura del CSS no cambiaron nada porque el razonamiento de cascada era correcto y aun así la conclusión era falsa — el error estaba en dar por inerte una propiedad. Lo resolvió **medir**, no razonar.
- **2026-09-05 — De ahí sale `npm run verify:layout` (`scripts/verify-layout.mjs` + `layout-probe.html`), la primera verificación del proyecto que ejecuta un motor de render de verdad.** Abre el esqueleto real del shell con las hojas de estilo reales en Edge/Chrome headless (mismo motor que WebView2) y comprueba geometría pura, sin datos ni Tauri: `.app-body` y el lanzador ocupan la ventana entera, y las 7 combinaciones de paneles de §7.9 reparten el ancho exacto sin desbordar. Cubre el hueco que este proyecto arrastra desde el principio y que ya había dejado pasar el crash de `SVGAElement.href` y el recorte del zoom: **jsdom no calcula layout, `verify:frontend` no monta cajas y `vite build` no ejecuta nada**, así que ninguna verificación anterior podía ver un fallo geométrico. Validado a la contra (misma disciplina con la que se creó `verify:frontend`): al reintroducir el `place-items: center`, 8 de las 9 comprobaciones fallan con el número exacto del síntoma real. La misma sonda destapó de paso un segundo fallo que nadie había notado: un separador (`#splitter-preview`) que sobrevivía a ocultar su panel y hacía que la rejilla le creara una columna implícita, desbordando 260px.

## 🗺️ Mapa del Producto (Áreas de Foco)

- **Producto:** "El entorno de escritorio más accesible para el ecosistema Typst" (posicionamiento oficial) — no editor de código con soporte Typst. Herramienta orientada a documento/proyecto ("Obsidian for Typst"): lanzador por tareas, asistente de creación de proyecto, plantillas como funcionalidad de primer nivel.
- **Núcleo de edición:** Editor CodeMirror 6 con el parser Lezer de `codemirror-lang-typst` (sin WASM), ya en producción + barra de herramientas de inserción (RF-13, v0.2, ADR-EDITOR-002) + outline vía `typst eval` (Beta; `query` está deprecado) + autocompletado semántico vía `tinymist`/LSP (Beta, integración separada del sidecar `typst`).
- **Motor de compilación:** Módulo `typst_engine` en Rust — gestor de proceso sidecar del binario oficial `typst` (CLI vendorizado, no crates embebidas), preview SVG + export PDF por stdout.
- **Universe Browser:** punto de entrada de primer nivel (no add-on) sobre una sola fuente de datos pública (`index.json` de `packages.typst.org`) — dos ramas de igual peso, Plantillas (Template Explorer) y Paquetes (Package Explorer), Beta — nunca depender de la API privada `api.typst.app`. El Lanzador (MVP) ya adelanta esta experiencia con el catálogo curado.
- **Modelo de datos:** Proyecto (no fichero suelto) con manifiesto propio DBV; Project Archive `.dbvt` (zip) para compartir/respaldar.
- **Infraestructura heredada:** Watcher de ficheros, single-instance/file-association, recent-projects, auto-actualizador, theming, paneles flotantes, CI de release, patrón de toolbar de inserción y de TOC — todo portado de DBV Markdown Reader con adaptaciones menores.
- **Plantillas:** 4 curadas en v0.1 —Proyecto en blanco, TFG, Artículo académico, CV— (`typst.toml`/`[template]` oficial + `dbv-template.toml` sidecar propio), creadas vía `typst init` y **autocontenidas**: el proyecto generado compila con `typst` a secas, sin DBV. Las otras 4 (TFM, Tesis, Informe técnico, Presentación) → v0.2; Template Explorer comunitario en Beta; ecosistema completo (IEEE/ACM/Springer/LNCS/docencia) en v1.0.
- **Power users:** Terminal avanzado (Beta) — subcomandos oficiales de Typst expuestos directamente, reutilizando la infraestructura de sidecar del MVP.
