# Backlog — DBV Typst Editor

## Contexto del Proyecto (Context Snapshot)

* **Objetivo**: Construir "el entorno de escritorio más accesible para el ecosistema Typst" (posicionamiento oficial) — no un editor de código con soporte Typst — orientado a documento/proyecto ("para Typst lo que Obsidian es para Markdown"), ligero, offline-first y multiplataforma, reutilizando al máximo la arquitectura de [DBV Markdown Reader](https://github.com/davidbuenov/dbv-md-reader).
* **Ubicación**: `d:/Programacion/github-davidbuenov/dbv-typst-editor`.
* **Estado actual**: **/build v0.5.0 — Previsualizaciones Typst Universe de Alta Fidelidad en Modal Completo y Hover Enriquecido COMPLETADOS (2026-09-09)**.
  1. **Previsualización de Alta Fidelidad en Modal Completo para Typst Universe (`templateGalleryModal.js`, `templateThumbnails.js`)**: Las plantillas de Typst Universe (`charged-ieee`, `faithful-acmart`, `springer-spaniel`, `ilm`, `modern-cv`, `appreciated-letter`) y cualquier plantilla comunitaria cuentan ahora con la misma previsualización maquetada completa que las plantillas locales: lienzo A4 renderizado a alta resolución en SVG con tipografía realista, fondos nítidos, sombreado de papel, fichas de metadatos (`📄 main.typ`, `🏷️ @preview/...`, `⚖️ licencia`) y botón de acción directa "Usar plantilla".
  2. **Hover Enriquecido en CodeMirror 6 (`universeHover.js`)**: Detección interactiva de `@preview/nombre:version` con renderizado de tarjeta tooltip (`cm-universe-hover-tooltip`), miniaturas maquetadas de plantillas, icono temático para librerías/paquetes, metadatos y botón a `typst.app/universe`.
  3. **Catálogo de Typst Universe con Miniaturas (`universePanel.js`, `base.css`)**: Tarjetas enriquecidas con vista previa visual de la maquetación del documento con carga lazy, sombras realistas y apertura directa del modal de galería completa.
  4. **Tinymist LSP y Python Runner**: LSP en segundo plano con diagnósticos `@codemirror/lint` en vivo, atajos de autocompletado y entorno Python compartido multiplataforma.
* **Última decisión técnica**: Mapeo estricto de rutas y mockups vectoriales completos en `templateThumbnails.js` y `universeThumbnails.js` con fallback SVG elegante offline; 414 tests en verde (176 Rust + 238 Vitest) + 48 verificaciones de frontend y plantillas (8/8 verify:frontend, 40/40 verify:templates).
* **Alcance ampliado el 2026-09-09**: el usuario, tras abrir la aplicación construida, pidió simplificar la pantalla de inicio ("aparece compleja... demasiado enrevesado") y decidió que el rediseño entra **en la misma v0.5.0**, reabriendo el `/spec`. Diseño validado en un lienzo de 6 artboards (fuentes en `spikes/launcher-redesign/`) y congelado como RF-25 a RF-27 en `SPECIFICATIONS.md` v1.4 §5d, con `ADR-LANZADOR-001` en `memory.md`.
* **v0.5.0 cerrado y publicado (2026-09-09/11).** `/spec` de **v0.6.0 CONGELADO v1.7 (2026-09-11)**: RF-31 a RF-38 en `docs/SPECIFICATIONS.md` §5f, en una sola pasada para evitar las tres reaperturas de v0.5.0 — editor WYSIWYG de diagramas (sustituye a RF-23), menú Herramientas, clonar repo por URL, Universe Browser completo, bibliografía visual completa, macOS, auto-actualizador, runner de JavaScript con `jogs`. Ver `ADR-V060-001`, `ADR-GITHUB-001`, `ADR-JOGS-001` en `memory.md`.
* **Próximo paso al retomar**: `/plan` de v0.6.0 — Modo Orquestador (8 RF, afecta a muchos ficheros), Adversarial Architect Review citando términos concretos de §5f, y desglose en `implementation_plan.md`. Detalles de implementación diferidos a propósito desde el `/spec` (no reabren la pregunta de alcance): crate BibTeX de RF-35, tamaño/criterio de whitelist de RF-34, verificación de sandboxing de `jogs` para RF-38.

## Checklist de Tareas

- [x] **Fase 0: Bootstrap del framework**
  - [x] Copiar `dbv-specs-ops` v2.8.0 a la raíz del proyecto y generar activadores.
  - [x] Rellenar `project.config.md`, generar `README.md` / `README.en.md`, `LICENSE` (MIT), `.gitignore`.
  - [x] `git init` + commit inicial (confirmado explícitamente por el usuario).

- [x] **Fase 1: Especificación y arquitectura (`/spec` + análisis `/plan` inicial)**
  - [x] Análisis exhaustivo de DBV Markdown Reader y clasificación de componentes reutilizables.
  - [x] Spec Addendum, Additional Specification Clarification, TYPST CLI INTEGRATION y feedback de posicionamiento procesados.
  - [x] Research phase dedicado del ecosistema Typst (`docs/TYPST_ECOSYSTEM_RESEARCH.md`).
  - [x] **🔒 Especificaciones y arquitectura CONGELADAS v1.0/v1.1 (2026-09-04).**
  - [x] **`docs/DESIGN.md` — ESCRITO el 2026-09-09.** Deuda documental saldada antes de rediseñar el lanzador, para que ese trabajo amplíe el sistema visual en vez de abrirle excepciones. Es **descriptivo, no prescriptivo**: recoge lo que ya está en `tokens.css`/`base.css`/`layout.css` (los 3 temas y sus 13 tokens semánticos, la escala tipográfica real de 24 a 10 px, espaciado y elevación, los 7 modificadores de botón, criterio modal vs. panel flotante, e interacción/foco). Su §9 registra 4 deudas encontradas al leer el CSS —paleta de estado tipo Tailwind escrita a mano que no cambia con el tema, `--accent-subtle` usado pero nunca declarado (siempre gana el respaldo azul, también en sepia), 2 transiciones con el valor a mano y espaciado sin tokenizar— y su §10 fija las 6 restricciones que debe respetar el rediseño del lanzador.

- [x] **Fase 2: Planificación de implementación (`/plan`)** — plan generado y **APROBADO por el usuario** (2026-09-04) con alcance reducido de v0.1.

- [x] **Fase 3: Construcción (`/build`) — COMPLETADA (2026-09-05).** Un commit por slice, la app arrancable al final de cada uno:
  - [x] **Slice 1** — Andamiaje Tauri v2 + Vite + shell (theming, paneles, i18n). ✅ `7a77fe2`
  - [x] **Slice 2** — Sidecar `typst` v0.15.1 verificado contra binario real (8/8). ✅ `8023821`
  - [x] **Decisión de instalador WebView2 cerrada** — `downloadBootstrapper` (18 MB) por defecto + overlay `npm run build:win:offline` (268 MB) para aulas sin conexión. ✅ `bc74407`
  - [x] **Slice 3** — Modelo de Proyecto, explorador y operaciones de proyecto (RF-02, RF-02b, RF-02c). ✅ `5c695d8`
  - [x] **Slice 4** — Editor CodeMirror 6 + lenguaje Typst (RF-05). R-02 cerrado a favor. ✅ `50e72a1`
  - [x] **Slice 5** — Bucle de vista previa SVG (RF-06), con los 4 requisitos del Adversarial Review. R-03 cerrado a favor. ✅ `78fb3bc`
  - [x] **Slice 6** — Guardar / Guardar como / conflicto externo (RF-07). ✅ `dadb582`
  - [x] **Slice 7** — Lanzador orientado a tareas + asistente de creación (RF-01, RF-03). ✅ `08bdf0e`
  - [x] **Slice 8** — Plantillas curadas: Proyecto en blanco, TFG, Artículo académico, CV (RF-04). ✅ `d9800b6`
  - [x] **Slice 9** — Exportación PDF (RF-10). ✅ `d26b257`
  - [x] **Slice 10** — Empaquetado, asociación `.typ` y CI (RF-12). ✅ `ccce34a`

- [x] **Slice 11 — RF-13, Barra de herramientas de inserción del editor — COMPLETADO (2026-09-05).** Subida de Beta a v0.2 a petición del usuario: sin ella el producto contradice su principio nº2 ("el usuario no debe ver código si no quiere") y es una regresión frente a DBV Markdown Reader. ADR-EDITOR-002 · `SPECIFICATIONS.md` v1.2 RF-13 · `ARCHITECTURE.md` §7.7.
  - `src/editor/toolbarActions.js`: tabla de 21 acciones como dato (no `switch`), cada una con `buildTransaction(state)` puro y testable sin DOM. Envolver + alternancia (marcadores simétricos y asimétricos), listas/encabezados con familias mutuamente excluyentes, plantillas con hueco para enlace/figura/tabla/ecuación/etc., y `isInsideMath()` vía el árbol Lezer real para la sensibilidad al contexto.
  - `src/editor/toolbar.js`: capa DOM fina — pinta los botones, aplica la transacción con `view.dispatch`, refresca tooltips en cambio de idioma y deshabilita/reordena grupos dentro de una ecuación.
  - `editor.js`: `buildToolbarKeymap()` añade los 7 atajos (Mod-b/i/e/k, Mod-Shift-1/2/3) al keymap de CodeMirror; `getView()` y `onSelectionChange` nuevos en el contrato del editor.
  - **24 tests nuevos** (`toolbarActions.test.js`) — 57/57 Vitest + 77/77 Rust en verde tras el cambio.
  - **Decisión de alcance registrada:** no todas las ~21 acciones tienen atajo de teclado — el modelo de interacción principal es el clic (así lo enmarca la propia arquitectura, §7.7.4); asignar 21 combinaciones sin colisión no aportaba valor proporcional al riesgo.
  - **Deuda pendiente heredada de ADR-EDITOR-002, sin resolver en este slice:** los glifos de los botones y la comparación con la barra del editor web oficial de Typst siguen sin verificarse contra la aplicación real — se implementó el objetivo de producto documentado, no una réplica comprobada.
  - **✅ Validado en ventana real por el usuario (2026-09-05), con 2 fallos encontrados y corregidos en la misma sesión** — confirma exactamente la lección de la fase `/test`: ninguno de los 57 tests de Vitest ni las 8 comprobaciones de `verify:frontend` los detectaba.
    1. Pulsar sobre una cita/referencia cruzada insertada por la barra reventaba la app entera (`TypeError: r.href.startsWith is not a function`). Causa: los `<a>` que Typst incrusta en el SVG para citas/referencias son elementos SVG reales sin `href`, y `SVGAElement.href` siempre devuelve un `SVGAnimatedString`, nunca una cadena. Corregido en `preview.js` (fase de captura sobre el contenedor de páginas).
    2. El zoom por encima del 100% recortaba el documento de forma asimétrica al hacer scroll (hueco a la derecha, contenido inaccesible a la izquierda). Causa: `align-items: center` en un flex con overflow no reparte el desbordamiento por igual en Chromium. Corregido con `margin-inline: auto`.
  - **Mejora pedida, no implementada todavía:** que el botón "Cite" abra un desplegable con las claves reales del `.bib` del proyecto. Ya está en el alcance — es literalmente el asistente "Insertar cita con autocompletado" de `ARCHITECTURE.md` §7.7.4, agendado para **Beta** (necesita parseo de BibTeX + UI de desplegable, más trabajo que un botón de la barra). Pendiente de que el usuario decida si se adelanta a v0.2, como se hizo con RF-13, o se deja en Beta.
  - [x] **Slice 12 — Project Archive `.dbvt` (RF-11) — COMPLETADO (2026-09-05).** Diseño cerrado en `ARCHITECTURE.md` §7.12, sin re-análisis. `src-tauri/src/archive.rs`: ZIP con `manifest.json` propio (versión de la app, del compilador, plantilla de origen); import con doble comprobación anti zip-slip (`enclosed_name()` de la crate `zip` + verificación independiente de que la ruta resuelta sigue dentro del destino). 8 tests nuevos (incluida la entrada maliciosa `../fuera-del-proyecto.typ`, rechazada). UI: "Exportar proyecto" en las acciones de proyecto, "Importar proyecto" en el lanzador. Verificación: 83/83 Rust, 57/57 Vitest, build y `verify:frontend` (8/8, 58 elementos) en verde.
  - [x] **Slice 13 — 4 plantillas restantes (RF-04, v0.2) — COMPLETADO (2026-09-05).** TFM (estructura de TFG, misma normativa en la práctica), Tesis doctoral (dedicatoria, agradecimientos, resumen + abstract bilingüe, 5 capítulos, apéndice con numeración propia), Informe técnico (más ligero: sin portada a toda página, resumen ejecutivo destacado, cabecera repetida por página) y Presentación (diapositivas 16:9 **sin ningún paquete de Typst Universe** — usa el preset nativo `presentation-16-9`, coherente con que las plantillas de v0.1 ya eran autocontenidas). Catálogo: 4 → 8 plantillas. `npm run verify:templates`: 40/40 en verde. Test de Rust que fijaba la lista de v0.1 actualizado a las 8 de v0.2.

- [x] **Fase 4: Pruebas (`/test`) — cerrada el 2026-09-05**, con alcance explícitamente acotado (ver decisión de alcance debajo):
  - [x] **Cobertura de lógica del frontend** (era cero antes de esta fase): Vitest + jsdom, 33 tests sobre las funciones puras ya aisladas — `isTypstPath`/`joinPath`/`baseName`, `validateFields`, `localizeTemplate`, el contrato `Result` de `call()`. `commit 94aaad5`.
  - [x] **Verificación completa en verde tras el cambio**: 33/33 Vitest · 77/77 Rust · `build:vite` · `verify:frontend` 8/8.
  - [x] **Lección de la fase registrada en `memory.md`**: interacción `mockReset()` manual + `restoreMocks: true` que hacía parecer un fallo del código de producción.
  - [ ] ⏸ **Diferido conscientemente, no bloquea `/ship` v0.1.0** (ver razón en cada punto):
    - Prueba de integración de ventana real (arrancar la app, escribir, comprobar que la vista previa cambia). Razón: exige `tauri-driver` + WebDriver de Edge/WebKitGTK, infraestructura no montada aún y con alto riesgo de fragilidad cross-plataforma; ya está **validada a mano por el usuario** (ver Context Snapshot). Candidato a Beta, cuando el terminal avanzado y el Universe Browser obliguen de todos modos a automatizar la ventana.
    - Los tres escenarios de conflicto de RF-07 en integración. Misma razón — cubiertos hoy por prueba manual + la lógica ya testeada de `workspace.js`.
    - Prueba del instalador generado en las dos variantes de Windows y en Linux. Razón: exige máquinas limpias sin WebView2/deps preinstaladas; el instalador de Linux ni siquiera se ha generado nunca (`release-linux.yml` solo dispara con tags `v*.*.*`).

- [x] **Fase 5: Simplificar (`/code-simplify`) — cerrada el 2026-09-05.** Revisión de tres pases de `docs/REVIEW.md` sobre el estado del repo (construcción de argumentos del sidecar `typst`, `reveal_in_file_manager`, manejo de rutas, secretos): **0 hallazgos Críticos.** 1 hallazgo Importante — `baseName()` duplicada en `app/workspace.js` y `launcher/launcher.js` — corregido (launcher importa la de workspace). Registrado en `CHANGELOG.md`.
- [x] **Fase 6: Entrega (`/ship`) — cerrada el 2026-09-05** en lo que es documentación: README raíz actualizado, `walkthrough.md` completado, gate de `/code-simplify` satisfecho (0 Críticos), `CHANGELOG.md` con sección fechada `[0.1.0] - 2026-09-05`. **No incluyó** crear el tag ni publicar la Release — ver Fase 8 abajo, donde esa decisión pendiente se resuelve saltando directamente a `v0.2.0`.

- [ ] **Fase 7: Beta (v0.2–v0.4) — en curso desde el 2026-09-05.** Sesión autónoma ("avanza todo lo que puedas hasta la versión Beta"), sin el usuario disponible para resolver ambigüedades: se prioriza lo ya especificado sin preguntas abiertas, en slices verificados uno a uno, y se deja explícitamente fuera lo que exige una decisión del usuario o infraestructura no disponible aquí (ver "Deliberadamente fuera de esta sesión" al final de esta fase).
  - [x] **Slice 25 — Primera pasada manual de Beta en la ventana real (2026-09-05), usuario presente: 3 bugs encontrados y corregidos, 2 rediseños de UX a petición explícita.**
    1. **Bug — Cite/Σ/Tabla no abrían nada al pulsar.** Causa raíz común en `panels/registerPanel.js`: `closeOnOutsideClick` cierra el panel si el clic no viene de un elemento registrado como `trigger`; los tres botones abren su panel llamando a `openNear()` directamente desde `editor/toolbar.js` (no vía el mecanismo `trigger` de `registerPanel`), así que el mismo clic que abría el panel se propagaba hasta `document` y lo volvía a cerrar en el mismo evento — se abría y cerraba de forma imperceptible. Corregido con `event.stopPropagation()` en el manejador de esos tres botones (`editor/toolbar.js`), mismo patrón que ya usaban los paneles con `toggle` (Outline/Terminal/Acerca de). Lección: cualquier panel nuevo que abra un `registerPanel` con `closeOnOutsideClick` desde fuera de su propio `trigger` necesita este mismo corte de propagación.
    2. **Rediseño — modos de escritura → 3 interruptores independientes (`app/workspacePanels.js`, sustituye a `app/writingMode.js`).** Los 4 modos fijos (Edición/Escritura/Dividido/Lectura) cubrían solo 3 de las 7 combinaciones útiles y con menos libertad (Escritura también ocultaba la barra de herramientas). Ahora "P" (proyecto+esquema)/"E" (editor)/"V" (vista previa) se combinan libremente vía `data-panel-sidebar|editor|preview` en `.workspace`; siempre debe quedar al menos uno visible (`togglePanel` ignora el intento de ocultar el último). Un panel único ocupa todo el ancho (arreglа lo que en la vieja Lectura dejaba huecos laterales). 6 tests nuevos.
    3. **Rediseño — Outline pasa de panel flotante a pestaña del panel lateral (`main.js: wireSidebarTabs`), estilo dbv-md-reader (`filetree.js`/`#sidebar-tabs`).** El árbol de proyecto y el esquema ahora son dos pestañas ("Archivos"/"Esquema") del mismo `.sidebar`, en vez de un botón `☰` que abría un panel flotante aparte — el interruptor "P" de arriba controla el conjunto. `outline/outline.js` no cambió (solo cambia dónde vive `#outline-list` en el DOM).
    4. **Mejora — terminal avanzado**: botón de cerrar (✕), botón "Limpiar" (ya existía `terminal.clear()` sin botón que lo llamara), frase introductoria con ejemplo (`terminal.intro`) y arrastre libre por la cabecera (`panels/draggablePanel.js`, nuevo — congela la posición centrada en píxeles al primer arrastre).
    5. **Mejora — consola de errores de la vista previa redimensionable.** `ui/splitter.js` generalizado con un eje (`axis: 'x'|'y'`); nuevo separador `#splitter-band` entre `#preview-pages` y `#preview-band`, persistido en `--preview-band-height`. Sustituye el `max-height: 30%` fijo original.
    6. **Añadido — detección de Microsoft Store en el panel "Acerca de"** (`commands/app_info::is_packaged_app`, puramente por ruta del ejecutable — mismo mecanismo que `dbv-md-reader`), para mostrar un texto distinto según el origen de la instalación. **No** se ha activado ningún auto-actualizador real: la regla ya registrada en `memory.md` (la clave de firma la genera el usuario, nunca la IA) sigue sin resolverse, así que esto es solo el texto informativo, no un botón de actualización funcional.
    7. **Mejora — pedida tras ver capturas de la ventana real:** el lanzador (sin proyecto abierto) tenía el contenido fijo a `900px` centrado, dejando casi la mitad de una pantalla ancha vacía (`.launcher__inner`, ahora `min(1400px, 100%)`). Y en la vista previa a pantalla completa, la página se queda con su ancho de referencia (920px × zoom) centrada — comportamiento normal de cualquier visor de documentos, pero el usuario prefirió añadir un modo real de **"Ajustar al ancho"** (botón ↔ junto al zoom) en vez de solo agrandar el tope fijo: `preview.js` calcula el zoom necesario para que la página ocupe el ancho disponible del panel (`fitZoom()`, referencia 920px que debe coincidir con el `max-width` de `.preview-page` en `layout.css`) y lo recalcula con un `ResizeObserver` sobre `#preview-pages` — necesario porque ahora el ancho del panel cambia en caliente al tocar los interruptores de panel del punto 2. Pulsar +/−/100% sale del modo ajustar, igual que en cualquier visor de PDF.
    8. **Bug de layout de raíz, encontrado midiendo en un motor real (no razonando sobre el CSS): media ventana desperdiciada a los lados, tanto en el lanzador como en el espacio de trabajo.** El usuario insistió ("hay un nivel por encima de los CSS") después de que dos intentos de arreglo por inspección de hojas de estilo no cambiaran nada — y tenía razón. Causa: `base.css` conservaba desde el Slice 1 un `.app-body { display: grid; place-items: center; padding: 24px }` del andamiaje del "Hola mundo". `layout.css` ya pisaba `display`, `padding` y `overflow`, así que la regla parecía muerta — pero **`place-items` no estaba pisado**, y en cuanto Chromium implementó `justify-items`/`justify-self` para cajas de bloque dejó de ser inerte: pasó a encoger al contenido y centrar TODO hijo de `.app-body`. Medido con Edge headless: `.app-body` 1342px pero `#empty-view` 268px centrado. Corregido dejando **una sola** definición de `.app-body` (en `layout.css`, con el `flex: 1 1 auto` que sí hacía falta). La misma medición destapó un **segundo fallo invisible**: `#splitter-preview` sobrevivía a ocultar la vista previa y, sin columna en la plantilla, la rejilla le creaba una implícita (+260px de desbordamiento en "editor sin vista previa"); ahora cada separador se oculta si falta cualquiera de los dos paneles que separa.
    9. **`npm run verify:layout` (nuevo, `scripts/verify-layout.mjs` + `scripts/layout-probe.html`), añadido a CI.** Abre el esqueleto real del shell con las hojas de estilo reales en Edge/Chrome headless —el mismo motor que WebView2— y comprueba geometría: que `.app-body` y el lanzador ocupan la ventana entera y que **las 7 combinaciones de paneles reparten el ancho exacto sin desbordar** (9 comprobaciones). Cierra el hueco estructural que este proyecto arrastra desde el principio: jsdom no calcula layout, `verify:frontend` no monta cajas y `vite build` no ejecuta nada, así que NINGUNA verificación existente podía ver un fallo de este tipo. Validado a la contra reintroduciendo el `place-items: center`: 8 de las 9 comprobaciones pasan a fallo con el número exacto del síntoma real (268/1342). Se omite sola (sin fallar) si no encuentra navegador, para no bloquear un runner pelado.
    10. **Primer proyecto ajeno real con paquetes de Typst Universe (RF-02b), probado por el usuario: funciona — y de paso destapa ruido en la banda de avisos.** Al abrirlo, el CLI descargó sus 6 paquetes (`@preview/marginalia`, `codly`, `itemize`, `elembic`, `colorful-boxes`, `droplet`) sin intervención: es la primera confirmación en la ventana real de que la resolución de paquetes por sidecar funciona de punta a punta. Pero el CLI escribe el **progreso de descarga por stderr**, mezclado con los diagnósticos, así que la banda de la vista previa —que existe para avisos y errores— se llenaba de barras de progreso y enterraba lo único que había que leer (un `unknown font family: flux`, correcto: el proyecto usa una fuente que la máquina no tiene). Nuevo `typst_engine::compile::strip_download_progress()`, aplicado en `run_cancelable` (así cubre a la vez avisos de preview y errores de export) y **deliberadamente NO aplicado a `typst_run_raw`**: el terminal avanzado (§7.14) existe para dar la salida cruda del CLI y filtrarla ahí sería contradecirlo. 4 tests nuevos, escritos contra la salida REAL capturada del proyecto del usuario, incluido el repintado con `\r` (sin normalizarlo, todo el progreso viaja en una sola línea y se cuela) y el caso "todo era ruido → la banda no se abre".
    11. **Fuentes propias del proyecto (`fonts/`) — ADR-FUENTES-001, a petición del usuario.** Sale directamente del caso anterior: un proyecto ajeno sólo se compone igual en otra máquina si sus fuentes viajan con él. `typst_engine::font_path_args()` añade `--font-path <raíz>/fonts` cuando esa carpeta existe, en las cuatro invocaciones que componen el documento (vista previa, PDF, PNG y `typst eval` del esquema, que también compone). Flags verificados contra el binario **antes** de escribir nada (`--font-path` añade y no sustituye, es recursivo, lo aceptan `compile` y `eval`) y prueba de extremo a extremo con el caso real: con `--ignore-system-fonts` en ambos lados, el mismo documento pasa de avisar `unknown font family: flux` a compilar limpio, y el SVG cambia de tamaño (19.434 → 17.112 bytes) porque incrusta los contornos reales de la fuente. Como el `.dbvt` empaqueta el árbol entero, la carpeta viaja dentro del archivo: un proyecto exportado pasa a ser portable de verdad, y hay un test propio que lo fija para que ningún filtro futuro de "ruido de repositorio" la excluya en silencio. 4 tests nuevos (3 del helper + 1 del archivo).
    12. **Ayuda bilingüe dentro de la aplicación (nueva, a petición del usuario).** Botón `?` en la cabecera → panel ancho con índice lateral y 10 secciones que cubren el producto real: lanzador y plantillas, los tres paneles P/E/V, la barra de escritura con su tabla de atajos, los cuatro asistentes, vista previa (zoom, ajustar al ancho, banda de errores, esquema), guardar/exportar (PDF, PNG, `.dbvt`), fuentes propias del proyecto, paquetes de Universe, terminal avanzado y apariencia/idioma. El contenido vive como DATO en `help/helpContent.js` (ES/EN en la misma estructura) y `help/help.js` es solo render, mismo reparto que `toolbarActions.js`/`toolbar.js`; **no** va en `i18n.js` a propósito, que es un diccionario de etiquetas de una línea y se volvería ilegible con párrafos. 5 tests nuevos que fijan lo que de verdad se rompe en un fichero bilingüe: que ninguna sección, párrafo, lista o atajo se quede sin traducir (una lista con distinto número de puntos en ES y EN falla: significa que una versión ha perdido información) y que las secciones clave del producto siguen documentadas.
    13. **Auto-actualización activada (ADR-ACTUALIZADOR-001) — el usuario generó el par de claves y entregó solo la pública, levantando el único bloqueo que quedaba.** `tauri-plugin-updater` + `tauri-plugin-process` bajo `#[cfg(desktop)]`, pubkey y endpoint en `tauri.conf.json`, permisos `updater:default`/`process:default`, y botón en "Acerca de" que se oculta en instalaciones de Microsoft Store (reutiliza `is_packaged_app`, ya existente) y **nunca comprueba al arrancar**. Los módulos del actualizador se importan de forma perezosa dentro del manejador: fuera de la ventana de Tauri no existen, y un import estático habría repetido el fallo de arranque del Slice 4.
    14. **Corrección de un error propio en el reparto de la firma, señalado por el usuario.** El primer intento subía la clave privada a los secretos de GitHub para firmar en CI. `NATIVE_APPS_RELEASE_CI.md` §7 descarta esa vía explícitamente: la firma vive en la máquina del mantenedor y mezclarla con firmas de CI en un mismo `latest.json` es coordinación cross-máquina frágil. Reparto correcto ya aplicado: **Windows local y firmado** (única plataforma con auto-actualización, `createUpdaterArtifacts: true` heredado de la config base) y **Linux/macOS en CI sin firmar** (`false` en sus overlays, `includeUpdaterJson: false` en sus workflows). Nuevo `scripts/updater-manifest.mjs` (`npm run updater:manifest`) que compone el `latest.json` a partir del `.nsis.zip` y su `.sig` recién construidos — en CI lo genera `tauri-action`, en un build local no lo genera nadie.
    15. **`release-macos.yml` (nuevo), siguiendo la plantilla de la casa** (`NATIVE_APPS_RELEASE_CI.md` §9) con build universal `--target universal-apple-darwin` y los dos targets de Rust, porque `macos-latest` es Apple Silicon y sin eso un Mac Intel no podría ejecutarlo (§5). **Dos pasos propios que la plantilla no tiene, porque el starter no lleva sidecar:** vendorizar el compilador Typst, y —al ser un build universal— vendorizar **las dos arquitecturas** y fundirlas con `lipo` en `typst-universal-apple-darwin`, que es el nombre que Tauri busca; Typst publica un binario por arquitectura, así que sin ese paso el build falla. `scripts/vendor-typst.mjs` gana un flag `--target <triple>` para poder pedir uno distinto al del host. **Este workflow será la primera vez que `macos_menu.rs` (Slice 24) pase por un compilador** — sigue siendo la pieza menos verificada del proyecto y está avisado en la cabecera del propio workflow.
    16. **Typst Universe: plantillas Y paquetes de la comunidad (ADR-UNIVERSE-001) — cae el mayor de los puntos "deliberadamente fuera".** No era trabajo técnico: `typst init @preview/<nombre>` ya funcionaba tal cual con el binario vendorizado (comprobado antes de escribir nada), y el índice público son 4.675 paquetes / 2.266 plantillas. Lo que faltaba era la política, y la decidió el usuario: **lista revisada + campo libre**, para plantillas y para paquetes (él señaló que Universe no es solo plantillas — `#import "@preview/quick-maths:0.2.1"` va por la misma vía). Botón ✦ en la cabecera → panel con dos pestañas. Backend `universe.rs` con `parse_universe_spec` estricto y testeado contra rutas maliciosas. Catálogo curado (`universe/curatedCatalog.js`) de 6 plantillas y 14 paquetes con **versiones y licencias verificadas contra el registro real** — en la primera pasada un candidato "obvio" no existía con ese nombre. Cada tarjeta enseña identificador y licencia. La importación de un paquete se inserta al principio del documento tras los `#import` existentes (Typst resuelve en orden; en medio quedaría sin definir) y no se duplica. 16 tests nuevos (4 Rust + 12 Vitest). Ayuda actualizada con la sección correspondiente.
    Verificación final de la sesión: **119/119 Rust · 98/98 Vitest · `verify:frontend` 8/8 · `verify:layout` 9/9 · `verify:typst` 8/8 · `verify:templates` 40/40 · `build:vite`** — todo en verde.
    - **Reportado sin resolver, necesita repro en vivo:** el usuario observó que al escribir dentro de una ecuación el estado de la vista previa alterna "Compilando…"/"Página 1" de forma continua incluso tras sacar el cursor de la ecuación, hasta guardar. Revisada la cadena `editor.js` (el `onChange` ya filtra por `update.docChanged`, no dispara con solo mover el cursor) y `watcher.rs` (`.dbv-preview.typ` ya está filtrado, no hay bucle de watcher) sin encontrar una causa de bucle infinito en el código — el comportamiento más probable es el debounce de 350 ms recompilando en cada pulsación mientras el usuario sigue escribiendo en CUALQUIER parte del documento, no solo dentro de la ecuación, y estabilizándose en cuanto se deja de teclear del todo. Pendiente confirmar con el usuario si de verdad continúa sin ninguna pulsación durante varios segundos (bug real) o si simplemente seguía escribiendo en otra parte (comportamiento esperado).
  - [x] **Slice 24 — Menú nativo de macOS (`NATIVE_DESKTOP_APPS.md` §6.10) — COMPLETADO (2026-09-05), a petición explícita del usuario.** Sin él en macOS no hay `Cmd+Q`, `Cmd+H` ni el `Edit` del sistema — Tauri v2 solo trae menú de aplicación por defecto en Windows/Linux, que ya tienen su propia UI de ventana para eso. `src-tauri/src/macos_menu.rs`, módulo entero bajo `#[cfg(target_os = "macos")]` (ni se compila en Windows/Linux): 5 menús (App/Archivo/Edición/Vista/Ventana), atajos **solo los estándar universales de macOS** (Cmd+N/O/Shift+O/S/Shift+S/W/Q/Z/Shift+Z/X/C/V/A/M) — comprobado a propósito que ninguno coincide con los de la barra de inserción del editor (Mod-b/i/e/k/Shift-1/2/3, RF-13): un acelerador de menú de macOS se resuelve a nivel de sistema, antes de que la tecla llegue al DOM, así que una colisión habría roto un atajo del editor sin ningún error visible. Los ítems predefinidos (Cortar/Copiar/Pegar/Deshacer/Ocultar/Salir...) los localiza el propio SO; los propios (Guardar, Exportar PDF, Esquema...) se localizan a mano con `sys-locale`. Cada ítem propio reemite un evento (`app.emit("menu-save", ())`) que el frontend ya escuchaba o pasa a escuchar con `on()` genérico, reenviando el clic al MISMO botón que ya existía (`el('btn-save').click()`) — la lógica de cada acción no se duplica en Rust, tal cual documenta `NATIVE_DESKTOP_APPS.md`. **Verificación atípica de esta sesión, y hay que decirlo con toda claridad: no ha pasado por ningún compilador.** No hay toolchain de macOS en esta máquina Windows, y `cargo check --target x86_64-apple-darwin` falla en el primer build script por falta de un enlazador de macOS (`cc-rs: failed to find tool "cc"`), no solo de SDK — ni siquiera un type-check parcial es posible aquí. En su lugar, cada llamada a la API (`SubmenuBuilder`, `MenuItemBuilder`, `Menu::append_items`, tipos de `on_menu_event`/`set_menu`) se verificó leyendo el código fuente vendorizado real de `tauri` 2.11.5 y `muda` 0.19.3 en la caché de Cargo — y ese proceso ya corrigió dos errores propios antes de darlo por bueno: `on_menu_event` entrega un `&AppHandle`, no un `&App` (tipos distintos aunque ambos implementan `Emitter`), y `MenuBuilder` no tiene `.items()` — el ensamblado real es `Menu::new(app)?` + `.append_items(...)`. Es, con diferencia, la pieza menos verificada de toda la sesión: necesita una compilación real en un Mac antes de darse por buena. Sin tests nuevos (código de integración con `AppHandle`/`App` real, no lógica pura aislable). Verificación disponible: 105/105 Rust y `cargo build`/`cargo test` sin cambios en Windows (el módulo no se toca en esta plataforma), confirmando que la adición no rompe nada fuera de macOS.
  - [x] **Slice 23 — Instancia única (candidato registrado desde el `/build`, no un ítem de `ARCHITECTURE.md` §7.x) — COMPLETADO (2026-09-05).** Cierra "el defecto de producto más visible que quedaba" (anotado ya en el punto de retorno del `/build`): abrir un segundo `.typ` desde el explorador del sistema con la app ya abierta lanzaba una segunda instancia entera en vez de reusar la existente. Mismo patrón que DBV Markdown Reader, documentado desde el análisis inicial de reutilización (`ARCHITECTURE.md`, fila "single-instance", "solo desktop"). `tauri-plugin-single-instance`, registrado **antes** que el resto de plugins en la cadena del `Builder` (lo exige la propia documentación del plugin) y con `#[cfg(desktop)]` porque el plugin no existe en móvil. El segundo lanzamiento enfoca la ventana existente y reutiliza `first_document_argument()` —ya existente y testeado desde el arranque en frío (RF-12)— sobre el nuevo `argv`, emitiendo un evento `open-document` que el frontend escucha con el mismo `on()` genérico que ya usa el observador de proyecto. Sin tests nuevos: la única lógica propia (`first_document_argument`) ya estaba testeada; el resto es cableado de Tauri sin acceso a `AppHandle` real desde `cargo test`. Verificación: 105/105 Rust, 79/79 Vitest, `verify:frontend` (8/8, 85 elementos).
  - [x] **Slice 22 — Asistente "Nueva entrada bibliográfica" (§7.11) — COMPLETADO (2026-09-05), a petición explícita del usuario mientras estaba fuera.** Genera **BibTeX**, deliberadamente no el formato Hayagriva nativo de Typst: las 8 plantillas curadas y el picker de citas (Slice 16) ya usan `refs.bib`, cambiar de formato tocaría ambos para un beneficio incierto. Va en la dirección OPUESTA a la pregunta pendiente de `SPECIFICATIONS.md` §9 ("qué crate de parseo BibTeX usar") — esa pregunta es sobre LEER campos de entradas ajenas; este asistente solo ESCRIBE entradas nuevas con plantillas propias por tipo, sin parsear nada, así que no la reabre. 6 tipos de entrada (article, book, inproceedings, phdthesis, mastersthesis, misc) con sus campos típicos; sugerencia automática de clave a partir de autor+año (editable); comprobación de que la clave no colisiona con una ya existente (reutiliza `bibliography_keys`, Slice 16); al guardar, añade la entrada a `refs.bib` (creándolo si el proyecto no lo tenía) **e inserta la cita en el cursor** — cierra el círculo de "no encuentro la fuente que busco" sin que el usuario tenga que volver a abrir el desplegable de citas a mano. Se abre desde un botón "+ Nueva entrada" al final del propio desplegable de citas. **Refactor incidental necesario:** `joinPath`/`isTypstPath`/`baseName` se extrajeron de `workspace.js` a un nuevo módulo hoja `app/paths.js` — el panel nuevo necesitaba `joinPath` y, si lo hubiera importado de `workspace.js` directamente, se habría creado un ciclo de módulos (`workspace.js` → `bibEntryPanel.js` → `workspace.js`); `workspace.js` re-exporta las tres para no romper a quien ya las importaba de ahí (`launcher.js`, los propios tests). 13 tests nuevos sobre la lógica pura (sugerencia de clave, serialización, validación). Verificación: 105/105 Rust, 79/79 Vitest, `verify:frontend` (8/8, 85 elementos).
  - [x] **Slice 21 — Selector manual de imagen (§7.7.4) — COMPLETADO (2026-09-05).** El botón "Fig" de la barra abre ahora un selector nativo de fichero (filtro de imagen) como vía alternativa a arrastrar y soltar (Slice 19), para quien lo prefiera. Reutiliza el 100% de la infraestructura ya construida: `copy_asset_into_project` para copiar a `images/` sin sobrescribir nada, `figureActionForPath()` para insertar con el hueco en el pie de figura. Único código nuevo: `pick_image_dialog` (selector nativo con filtro de extensiones de imagen) y la función que enlaza ambos pasos. Sin tests nuevos — es cableado puro entre dos piezas ya testeadas por separado (Slices 19 y 20), sin lógica propia que aislar. Verificación: 105/105 Rust, 66/66 Vitest, `verify:frontend` (8/8, 77 elementos).
  - [x] **Slice 20 — Galería de símbolos y diálogo de tabla (§7.7.4) — COMPLETADO (2026-09-05), cierra los asistentes con formulario de Beta.** Generaliza `toolbar.js`: en vez de un caso especial hardcodeado para "Cite" (Slice 16), un mapa `specialHandlers` indexado por `id` de acción — más limpio para tres botones que una cadena de `if`. **Verificado contra el binario real antes de escribir la galería** (misma disciplina del Slice 2): los 40 identificadores candidatos (griegas, sumatorio/productorio/integral, flechas, relaciones, teoría de conjuntos, `partial`/`nabla`/`forall`/`exists`) se compilaron de golpe en un único documento de prueba — 0 avisos, los 40 son nombres reales de Typst, no supuestos. `insertSymbolAction()`: dentro de una ecuación inserta el nombre desnudo, fuera lo envuelve en `$...$` — misma comprobación de contexto (`isInsideMath`) que ya usaba la barra. `tableAction({rows, cols, header})`: la cabecera opcional usa `table.header(...)` (la forma oficial), y el hueco de edición cae siempre en la primera celda del CUERPO, nunca en la cabecera. 4 tests nuevos sobre estas dos funciones puras. Con este slice quedan completos los cuatro asistentes con formulario descritos en §7.7.4 (cita, figura vía arrastre, símbolos, tabla). Verificación: 105/105 Rust, 66/66 Vitest, `verify:frontend` (8/8, 77 elementos).
  - [x] **Slice 19 — Gestión de imágenes por arrastre (§7.10) — COMPLETADO (2026-09-05).** `assets::copy_asset_into_project` copia el fichero soltado a `images/` del proyecto (creando la carpeta si no existe) y devuelve la ruta relativa con `/` normal en cualquier plataforma; nunca sobrescribe una imagen existente con el mismo nombre (`foto.png` → `foto-1.png`, `foto-2.png`...). **Descubrimiento antes de escribir una sola línea de frontend:** el `drop` nativo del DOM no sirve para esto en Tauri v2 — el `File` del navegador nunca expone una ruta absoluta (diseño de la API web) y, además, el drop nativo de ficheros de Tauri intercepta el evento del DOM por defecto. Se verificó contra el propio paquete instalado (`node_modules/@tauri-apps/api/webview.js`) que la vía correcta es `getCurrentWebview().onDragDropEvent()`, que sí entrega rutas reales (`event.payload.paths`) — evita implementar un dead-end que pareciera funcionar y no lo hiciera. La posición del evento llega en píxeles físicos de ventana; hubo que dividir por `devicePixelRatio` para compararla con `getBoundingClientRect()` del editor y decidir si la imagen se soltó ahí y no en el árbol o la vista previa. Reutiliza `figureActionForPath()`, variante de la acción "figura" de la barra (RF-13) con la ruta ya rellena y el hueco solo en el pie de figura. **Bug real cogido por un test antes de llegar a producción:** `figureActionForPath(path)` devuelve la propia función de transacción, no un objeto con método `.buildTransaction` — el primer test lo escribí mal (`.buildTransaction(state)`) y falló también en `main.js`, que tenía el mismo error; corregido en ambos sitios porque el test lo señaló, no porque se probara a mano. 9 tests nuevos (8 Rust + 1 Vitest). Verificación: 105/105 Rust, 62/62 Vitest, `verify:frontend` (8/8, 69 elementos).
  - [x] **Slice 18 — Modos de escritura: Edición / Escritura / Dividido / Lectura (§7.9) — COMPLETADO (2026-09-05).** Sin infraestructura nueva, tal como preveía el diseño: cuatro preajustes de `grid-template-columns` sobre `.workspace[data-mode]` (mismo mecanismo que `[data-theme]` en `theme.js`), con persistencia en `localStorage`. El conmutador vive en la cabecera persistente (`#project-actions`), no en la barra del documento — a propósito: el modo Lectura oculta `.document` entero (barra + herramienta + editor), y si el conmutador estuviera dentro no habría forma de salir de Lectura sin recargar la app. 4 tests nuevos sobre la lógica de modo (sin DOM real, igual que `theme.js`, que tampoco los tenía — se añaden aquí porque además hay una regla nueva, "ignora un modo desconocido", que theme.js no necesitaba). Verificación: 97/97 Rust, 61/61 Vitest, `verify:frontend` (8/8, 69 elementos).
  - [x] **Slice 17 — Terminal avanzado (§7.14) — COMPLETADO (2026-09-05).** Panel oculto por defecto (principio "el usuario no debe ver código si no quiere", `SPECIFICATIONS.md` §2), vía de escape para subcomandos directos del CLI de Typst contra el proyecto activo. `typst_engine::typst_run_raw`: reutiliza exactamente el mecanismo de sidecar existente, sin lógica de parseo (el frontend separa por espacios, tal cual pide §7.14 — sin comillas ni variables) y, a diferencia de `run()`, conserva `stdout` aunque el proceso termine con código de salida distinto de cero. Sin superficie de inyección nueva: los argumentos van a `Command::args()` como array, nunca a un intérprete de shell — es el mismo binario vendorizado que ya ejecutan compile/export/outline. Sin tests de Rust dedicados (mismo criterio que `run()`/`typst_export_pdf`, que tampoco los tienen: son envoltorios finos de `AppHandle`, no lógica pura). Verificación: 97/97 Rust, 57/57 Vitest, `verify:frontend` (8/8, 69 elementos).
  - [x] **Slice 16 — Asistente "Insertar cita" con autocompletado (§7.7.4) — COMPLETADO (2026-09-05).** Es la mejora que el usuario pidió explícitamente al validar el Slice 11 en la ventana real ("cuando se pulsa sobre Cite debería salir un desplegable con las citas disponibles"); confirmó dejarla "donde estuviera planificado" (Beta), y esta sesión llegó hasta ahí. `src-tauri/src/bibliography.rs`: extrae solo las CLAVES de los `.bib` de la raíz del proyecto con un escaneo de texto ligero (`@tipo{clave,`) — a propósito **no** un parser BibTeX completo, así que no reabre la pregunta pendiente de `SPECIFICATIONS.md` §9 sobre qué crate usar (esa pregunta es para leer los CAMPOS de cada entrada, que este asistente no necesita). Mismo patrón que la detección de "Paquetes usados" del Package Explorer (Beta, §7.6.2), que abandonó un parser completo por el mismo motivo. Frontend: el botón "Cite" de la barra (antes insertaba un marcador genérico) ahora abre un desplegable filtrable posicionado bajo el propio botón; elegir una clave inserta `#cite(<clave>)`. 8 tests nuevos en Rust. Verificación: 97/97 Rust, 57/57 Vitest, `verify:frontend` (8/8, 65 elementos).
  - [x] **Slice 15 — Exportación PNG, alcance "página actual" (§7.12) — COMPLETADO (2026-09-05).** Verificado contra el binario real que `--pages N` con una sola página acepta un nombre de fichero normal como salida (el patrón `{p}`/`{0p}` solo hace falta para exportar más de una imagen a la vez, que no es el caso). Botón junto a "Exportar PDF"; exporta la página que se está leyendo en ese momento en la vista previa. **No cubre** rango de páginas ni documento completo (multi-PNG) — quedaría para un slice propio, con selector de rango y patrón de nombre de fichero, más scope del que aporta este slice.
  - [x] **Slice 14 — Panel de navegación estructural / Outline (§7.8) — COMPLETADO (2026-09-05).** Spike de `typst eval` del Slice 2 llevado a producción, con dos sorpresas del binario real absorbidas en `typst_engine/outline.rs`: el cuerpo del encabezado no es una cadena (es contenido Typst serializado, `{"func":"text","text":...}` o una `sequence` si mezcla estilos — se aplana recursivamente) y la coordenada `y` llega como cadena con unidad (`"70.87pt"`), no como número. Comando `typst_outline` reutiliza `prepare_input` de `compile.rs` (ahora `pub(crate)`) para ver también los cambios sin guardar, igual que la vista previa. Frontend: panel flotante junto al árbol de proyecto, clic→navegación **a la vista previa** (página + desplazamiento vertical, con el `heightPt` real de cada página aunque su SVG no haya cargado todavía). **No cubre** clic→navegación en el editor: mapear posición de PDF a posición de texto fuente es la sincronización "por posición real" que `ARCHITECTURE.md` ya deja aparte, más adelante en Beta. 6 tests nuevos en Rust, incluida la deserialización de una salida real capturada del binario. Verificación: 89/89 Rust, 57/57 Vitest, `verify:frontend` (8/8, 61 elementos).

  ### ⏸ Deliberadamente fuera de esta sesión (Beta, no bloquea nada de lo anterior)

  Cada uno de estos exige una decisión del usuario, infraestructura no disponible aquí, o reabrir una pregunta explícitamente pendiente — no son un olvido:

  - **Universe Browser (Package Explorer + Template Explorer sobre `index.json` de Typst Universe, §7.6).** El mayor bloque de Beta y el de más riesgo para hacerlo sin el usuario: implica primera descarga por red de la aplicación, una whitelist curada de seguridad para el catálogo comunitario (decisión editorial explícita de DBV, `ARCHITECTURE.md` §6) y varias vistas nuevas grandes (pestañas Instaladas/Comunidad/Favoritas/Recientes/Actualizaciones). Necesita validación del usuario antes de construir, no solo verificación técnica.
  - **LSP `tinymist` (autocompletado semántico, diagnósticos en línea).** El propio `memory.md` fija la regla: "antes de comprometerse con una integración externa, un research phase dedicado" — no se ha hecho para `tinymist`, así que empezar a integrarlo sin ese research repetiría el error que la regla existe para evitar.
  - **Empaquetado macOS (compilar, firmar, `.app`/`.dmg`) y CI de release para esa plataforma.** El menú nativo (Slice 24) sí se escribió, pero **sin compilar ni una vez** — sigue sin haber forma de verificar nada de macOS de verdad desde esta sesión en Windows. Antes de anunciar soporte de macOS hace falta, como mínimo, compilar el Slice 24 en un Mac real.
  - **Auto-actualizador (`tauri-plugin-updater`).** Regla explícita ya registrada (`UPGRADE_PROMPT.md` §4): la clave de firma la genera el usuario en su propio terminal, nunca la IA. No se puede avanzar más allá de un scaffolding sin esa clave.
  - **Sincronización editor↔preview por posición real** (no por anclas). Necesitaría mapear posición de PDF a posición de texto fuente — bloque de trabajo propio, no una extensión del Outline de este slice.
  - **Bibliografía visual — solo la parte de LECTURA** (panel de exploración de entradas `.bib` ajenas: autor, título, año por entrada). La parte de ESCRITURA ya está — ver Slice 22 (asistente "Nueva entrada bibliográfica"). Sigue abierta la pregunta de `SPECIFICATIONS.md` §9 sobre qué crate de parseo BibTeX usar para la lectura completa; el Slice 22 la esquiva porque generar una entrada nueva no necesita parsear las que ya existen, solo sus claves (`bibliography.rs`, Slice 16).
  - **Ninguno de los slices de esta sesión se ha visto en la ventana real de la aplicación** (misma limitación que ya constaba en la fase `/test`): toda la verificación es `verify:frontend`, tests de Rust/Vitest y compilaciones directas contra el binario vendorizado. Antes de dar Beta por visualmente terminada hace falta una pasada manual completa, como la que ya encontró los dos fallos del Slice 11.

- [x] **Fase 8: Entrega `/ship` v0.2.0 — cerrada el 2026-09-06.** Primer tag de versión real del proyecto (51 commits previos, ninguno etiquetado). Ver `walkthrough.md` de esta sesión para el resumen completo.
  - [x] **Slice 26 — Typst Universe (3 mejoras tras uso real), rediseño de cabecera (icono+etiqueta corta) y panel "Acerca de" homogeneizado con DBV Markdown Reader.** Detalle en `CHANGELOG.md` `[0.2.0]`.
  - [x] **Investigación de migración LaTeX→Typst + spike S-1 sobre 3 proyectos reales del usuario (2 sin publicar, solo local) → ADR-LATEX-001: aplazada como pilar de v1.0, baja a investigación viva.** Ver `memory.md` y `docs/LATEX_MIGRATION_RESEARCH.md`. 2 incidencias reportadas aguas arriba: [tylax#44](https://github.com/scipenai/tylax/issues/44), [tylax#45](https://github.com/scipenai/tylax/issues/45). Banco de pruebas conservado en `spikes/latex-migration/`.
  - [x] **`.claude/commands/` traído del repo oficial de `dbv-specs-ops`** (7 ficheros: `spec, plan, build, test, code-simplify, ship, maintain`) — el paso opcional de la v2.8.0 nunca se había ejecutado en este proyecto; diagnosticado tras un "Unknown command: /ship" del terminal.
  - [x] **`/test`**: 98/98 Vitest · 122/122 Rust.
  - [x] **`/code-simplify`**: pase de calidad (4 agentes, reuso/simplificación/eficiencia/altitud) + pase `REVIEW.md` (Bugs/Seguridad/Cumplimiento) — **0 hallazgos Críticos**, gate de `/ship` satisfecho.
  - [x] **Versionado: `0.1.0` → `0.2.0`** (`package.json`, `src-tauri/Cargo.toml`, `src-tauri/tauri.conf.json`) — decisión explícita del usuario de saltar directamente a 0.2.0 en vez de etiquetar un 0.1.0 ya muy superado por el contenido real del repositorio.
  - [x] **`CHANGELOG.md`**: sección `[Sin publicar]` (acumulada desde hace varias sesiones) movida a `[0.2.0] - 2026-09-06`, con las entradas de esta sesión añadidas al principio.
  - [x] **Tag de versión `v0.2.0` creado y empujado** (confirmado explícitamente por el usuario, "Adelante") — primera vez que `CI`, `release-linux.yml` y `release-macos.yml` se ejecutan de verdad. **Los tres fallaron.**

- [x] **Fase 9: Entrega `/ship` v0.3.0 — cerrada el 2026-09-06.** Origen: uso real del usuario con la plantilla TFG-ETSI-UMA-Typst (proyecto externo, fuera de este repositorio) en marcha en la aplicación.
  - [x] **3 bugs de raíz encontrados y corregidos, reportados en vivo por el usuario:**
    1. `"[object Object]"` en vez del error real de Typst al fallar una compilación — `TypstError::CompilationFailed` colapsada de dos campos a `CompilationFailed(String)` (el `#[serde(tag, content)]` de dos campos serializaba `message` como objeto, no cadena).
    2. Rutas de imagen rotas al insertar una figura en un capítulo dentro de una subcarpeta — `figureActionForPath()` y el botón "Fig" anteponen `/` a la ruta devuelta por `copy_asset_into_project`, para anclarla siempre a la raíz del proyecto (Typst resuelve rutas sin `/` relativas al FICHERO que las contiene, no a la raíz).
    3. Duplicados al arrastrar la misma imagen varias veces — `find_existing_copy()` (tamaño + contenido byte a byte) en `assets.rs`, aplicado a imágenes y fuentes. 2 tests nuevos.
  - [x] **Causa real del fallo de los 3 GitHub Actions de la Fase 8 — encontrada y corregida.** `is_packaged_path_detecta_una_instalacion_de_microsoft_store` depende de que `Path::components()` trocee por `\`, cierto solo en Windows; la CI corre en `ubuntu-22.04` y nunca se había ejecutado antes del primer `/ship`. Tests afectados marcados `#[cfg(target_os = "windows")]`.
  - [x] **Pista legible cuando una cita no se resuelve al previsualizar un capítulo suelto** (la vista previa compila el fichero abierto, no `main.typ`) — con color distinto al del error real del compilador (`preview__band-hint` vs `preview__band-message`), tras un segundo aviso del usuario de que ambos se veían del mismo rojo. Evaluada y descartada la alternativa de compilar siempre por el entrypoint (coste de recompilar el proyecto entero en cada pulsación).
  - [x] **Botón de búsqueda (⌕) en la barra del editor** — Ctrl+F ya abría el panel de `@codemirror/search` sin botón visible; documentado además en el panel de Ayuda junto con el autocompletado de referencias con `@`.
  - [x] **Tema Sepia + selectores segmentados de tema/idioma, copiados de DBV Markdown Reader a petición explícita del usuario.** Mismos valores de paleta; el atajo del menú nativo de macOS ahora rota entre los tres temas.
  - [x] **Páginas de privacidad para tiendas de aplicaciones** (`docs/privacidad.html` / `docs/privacy.html`, modelo de `dbv-md-reader`, contenido verificado contra el comportamiento real de esta app) y **GitHub Pages activado** sobre `master`/`/docs` (antes inexistente en este repositorio).
  - [x] **`/test`**: 99/99 Vitest · 124/124 Rust · `verify:frontend` 8/8 (111 elementos) · `build:vite`.
  - [x] **`REVIEW.md`** (Bugs/Seguridad/Cumplimiento) — **0 hallazgos Críticos.**
  - [x] **Versionado: `0.2.0` → `0.3.0`** (decisión explícita del usuario: Minor, nueva funcionalidad visible sin romper nada).

- [x] **Fase 10: Entrega `/ship` v0.3.1 — cerrada el 2026-09-07.** Entrega correctiva de urgencia. Origen: el usuario instala la app **desde Microsoft Store** y falla al compilar un `.typ` con `os error 2`; en un segundo ordenador, además, no aparece ninguna plantilla.
  - [x] **CAUSA RAÍZ — el `.msix` publicado en la Store salía sin el compilador Typst dentro.** `@choochmeque/tauri-windows-bundle` v0.1.29 (la última publicada) **no implementa `bundle.externalBin`**: copia el `.exe` principal, `Assets` y `bundle.resources`, y nunca los binarios externos. Confirmado sobre la carpeta de instalación real de la Store (`SignatureKind: Store`), que contenía solo `AppxManifest.xml`, `Assets/` y `dbv-typst-editor.exe`.
  - [x] **Segundo fallo del mismo empaquetador:** `bundle.resources` en forma de array con prefijo `../` (`"../templates"`) se copiaba con `path.join(appxDir, "../templates")`, dejando `templates/` **fuera** de la raíz del paquete. De ahí el catálogo vacío en el segundo ordenador.
  - [x] **Corregido con `patches/@choochmeque+tauri-windows-bundle+0.1.29.patch`** (`patch-package` + `postinstall`, sobrevive a `npm install`). ⚠️ El sidecar debe copiarse **sin el sufijo del target triple** (`typst.exe`, no `typst-x86_64-pc-windows-msvc.exe`): `tauri-plugin-shell` lo resuelve vía `relative_command_path()` a `<carpeta del exe>/typst.exe`. La primera versión del parche falló exactamente por esto.
  - [x] **Verificado end-to-end en la máquina del usuario**: `.msix` firmado con certificado de pruebas, versión de la Store desinstalada, paquete corregido instalado (contiene ya `typst.exe` 52 MB + `templates/`), y el usuario confirma en pantalla que crear proyecto, vista previa y exportar PDF funcionan. **El `.msix` pasa de 6 MB a 29,8 MB** — señal de alarma más rápida para el futuro.
  - [x] **2 bugs propios corregidos de camino, que afectaban a TODAS las plataformas** (no solo a la Store): la terminal avanzada nunca fijaba `current_dir` (rutas relativas siempre fallaban), y el prefijo `\\?\` de `fs::canonicalize` se filtraba a la interfaz (→ `dunce`).
  - [x] **Dos hipótesis descartadas y documentadas** para que nadie las repita: el prefijo `\\?\` **no** era la causa (una prueba mal escapada en bash lo hizo parecer; `typst.exe` acepta esas rutas sin problema), y tampoco el sandboxing MSIX (`Windows.FullTrustApplication`, sin AppContainer).
  - [x] **`/test`**: 99/99 Vitest · 124/124 Rust · `verify:frontend` 8/8 · assets MSIX 4/4 con imagen real (gate de `MARKETPLACE_PUBLISHING.md`).
  - [x] **`REVIEW.md`** (Bugs/Seguridad/Cumplimiento) — **0 hallazgos Críticos.**
  - [x] **Versionado: `0.3.0` → `0.3.1`** (Patch — solo corrección de bugs, propuesto por el usuario).
  - [ ] **PENDIENTE Y URGENTE (acción del usuario):** reenviar el `.msix` corregido a Partner Center. **La ficha publicada sigue sirviendo el paquete roto a usuarios nuevos.** Checklist de verificación obligatoria en `docs/MICROSOFT_STORE.md` §6.
  - [ ] Reportar el bug aguas arriba (`npx patch-package @choochmeque/tauri-windows-bundle --create-issue` deja el borrador hecho) — afecta a cualquier app Tauri con sidecar publicada por esa vía.
  - [x] **`CHANGELOG.md`**: sección `[0.3.0] - 2026-09-06` añadida.
  - [x] **Tag de versión `v0.3.0` creado.** Push **no ejecutado** — queda como sugerencia (ver Context Snapshot).

---

- [x] **Fase 11: v0.4.0 — `/ship` COMPLETADO el 2026-09-08 (`/plan` cerrado el 2026-09-08 — `implementation_plan.md`).**
  - [x] **Slice 28 (2026-09-08) — Imágenes (RF-17, RF-18).** Comando Rust `project_images` espejo de `bibliography_keys`; generalizar `citationPicker.js` a componente de lista filtrable reutilizable y construir sobre él el picker de imágenes, con "Buscar una imagen…" como última opción; quitar la restricción `droppedOnEditor` de la *copia* en `wireImageDrop` conservándola solo para decidir si además se inserta. Independiente de la vista previa: se puede revertir solo.
  - [x] **Slice 29 (2026-09-08) — Vista previa del documento completo y control de refresco (RF-14, RF-15).** Módulo `typst_engine/shadow.rs` (raíz sombra en temporal: copia para `.typ`/`.bib`, enlace duro para el resto) que **sustituye por completo a `prepare_input()`**; alcance conmutable documento/fichero compartido por vista previa, outline y exportación; modos de refresco automático/manual con indicador de "desactualizada". **Arranca verificando R-10 contra el binario real; si falla, se para.** Al cerrar, `MIRROR_FILE_NAME` y su escritura en la carpeta del usuario deben haber desaparecido.
  - [x] **Slice 30 (2026-09-08) — Sincronización editor ↔ vista previa (RF-16).** Anclas entre bloques inyectadas en la sombra con el número de línea del fichero **original**; comando `typst_sync_anchors` atado a la generación de `EngineState`; tabla cacheada bajo demanda; búsqueda por (página, banda de x, y). Depende del Slice 29.
  - [x] **Mejoras complementarias y pulido de v0.4.0:**
    - Corrección de preservación del scroll en vista previa al navegar con doble clic entre capítulos.
    - Placeholder de carga por página ("Cargando página X...") y spinner animado en primera composición.
    - Eliminación de proyectos recientes desde el lanzador con icono de cruz.
    - Actualización bilingüe (ES / EN) de `helpContent.js` y tests en verde.
    - Colaboradores y créditos de IA en `README.md` y `README.en.md`.
    - Bump de versión `0.3.1` → `0.4.0` en `package.json`, `Cargo.toml` y `tauri.conf.json`.
    - 313 pruebas pasando al 100% (152 Vitest + 161 Rust).

- [x] **Fase 12: v0.5.0 — Productividad Profesional, Robustez y Consolidación de la Interfaz — ENTREGADA EN LOCAL el 2026-09-09.** Ciclo completo: `/spec` (reabierta tres veces a petición del usuario), `/plan`, `/build`, `/test`, `/code-simplify` y `/ship`. **Falta solo `git push origin master --tags`.**
  - [x] **`/spec` v0.5.0 CERRADA (2026-09-09)**. Congelados RF-19 a RF-24 en `SPECIFICATIONS.md` v1.3.
    - RF-19: Integración con Git y resolución visual de diferencias (diff side-by-side).
    - RF-20: Galería visual de plantillas con previsualización renderizada.
    - RF-21: Inteligencia de código con Language Server `tinymist` (vendorizado como sidecar).
    - RF-22: Generación de figuras y datos dinámicos con Python.
    - RF-23: Asistente visual de diagramas nativos CeTZ.
    - RF-24: Robustez de plataforma en Windows (`augment_path`, `write_atomic`, last-good-preview).
  - [x] **`/plan` v0.5.0 CERRADO (2026-09-09)**: Modo Orquestador, Adversarial Architect Review de 4 objeciones resueltas, plan en `implementation_plan.md` aprobado por el usuario.
  - [x] **`/build` v0.5.0**: Implementación slice a slice con pruebas — **COMPLETADO (2026-09-09)**.
    - [x] **Slice 31 — Robustez de Plataforma en Windows y Guardado Atómico (RF-24) — COMPLETADO (2026-09-09).** `platform::augment_path()` inyecta en caliente rutas de WinGet, Cargo, Scoop, Python y Git; `file_io::write_atomic()` con fichero temporal oculto `.dbv-tmp`, renombrado atómico y reintentos ante bloqueos de antivirus en Windows; `watcher.rs` filtra temporales; `preview.js` preserva Last Good Render marcando como desactualizado en fallos de sintaxis. 316 tests pasando (164 Rust + 152 Vitest).
    - [x] **Slice 32 — Galería Visual de Plantillas con Previsualización (RF-20) — COMPLETADO (2026-09-09).** Miniaturas vectoriales SVG de alta fidelidad para las 8 plantillas en `templateThumbnails.js`, integradas en las tarjetas del lanzador con sombras suaves, escalado interactivo y soporte de teclado. 324 tests pasando (164 Rust + 160 Vitest).
    - [x] **Slice 33 — Asistente Visual de Diagramas CeTZ (RF-23) — COMPLETADO (2026-09-09).** Panel flotante en editor con botón `⬡`, 4 tipos de diagramas CeTZ (flujo, bloques, gráficas 2D, lienzo) e inyección automática de `#import` deduplicada. 339 tests pasando (164 Rust + 175 Vitest).
    - [x] **Slice 34 — Runner de Figuras y Datos Dinámicos con Python (RF-22) — COMPLETADO (2026-09-09).** `python_runner::execute_python_script` con `MPLBACKEND=Agg`, timeout de seguridad, descubrimiento de imágenes en `images/`, panel interactivo con plantillas e inserción directa en Typst. 348 tests pasando (169 Rust + 179 Vitest).
    - [x] **Slice 35 — Integración con Git CLI y Diff Side-by-Side (RF-19) — COMPLETADO (2026-09-09).** Módulo `commands::git` no bloqueante con `GIT_TERMINAL_PROMPT=0`, indicador de rama en cabecera con popover para commit/push/pull, y modal comparador Side-by-Side (`diffModal.js`) en colisiones de guardado. 355 tests pasando (171 Rust + 184 Vitest).
    - [x] **Slice 36 — Sidecar Tinymist LSP e Inteligencia de Código (RF-21) — COMPLETADO (2026-09-09).** Binario `tinymist` v0.15.8 vendorizado offline en `externalBin`, actor Rust con encuadre `Content-Length` y JSON-RPC asíncrono, cliente CodeMirror 6 con autocompletado semántico Typst, hover docs enriquecido y formateo Typstyle con `Shift-Alt-f`. 368 tests pasando (175 Rust + 193 Vitest).
  - [x] **Corrección previa a `/test` (2026-09-09) — los workflows de release no vendorizaban Tinymist.** `bundle.externalBin` pasó a declarar dos sidecars en el Slice 36, pero `ci.yml`, `release-linux.yml` y `release-macos.yml` seguían llamando solo a `npm run vendor:typst`. CI no lo delataba porque no empaqueta: el fallo habría salido con el tag `v0.5.0` ya empujado, al abortar `tauri build` sin encontrar `binaries/tinymist-<triple>`. Mismo patrón que la incidencia de la Store en v0.3.1. Corregido: Linux y macOS vendorizan y verifican los dos sidecars (macOS funde cada uno con `lipo`, porque `--target universal-apple-darwin` exige el sufijo por sidecar), y CI también los vendoriza aunque no empaquete, para detectar en cada push un asset de Tinymist que cambie de nombre. Verificado: los 5 assets de `tinymist v0.15.8` responden 200, los tres YAML parsean y los 28 bloques `run` pasan `bash -n`. Actualizado también el checklist obligatorio de `docs/MICROSOFT_STORE.md` §6 (dos `.exe` en la raíz del paquete, ~95 MB en vez de ~30 MB).
  - [x] **Corrección previa a `/test` (2026-09-09) — `verify:layout` daba un falso "la sonda ha reventado".** Dos fallos del verificador, ninguno de layout: un navegador que sale con código 0 y stdout vacío (Edge headless en algunas máquinas, también con una página trivial) se contaba como sonda rota en vez de omitirse; y en Windows la limpieza del perfil temporal reventaba con `ENOTEMPTY` dentro del `finally`, tumbando la comprobación entera. Ahora se OMITE en el primer caso y se reintenta/ignora en el segundo. **Consecuencia:** esta verificación lleva sin poder ejecutarse en esta máquina, así que la geometría de la interfaz de v0.5.0 —que añade bastante CSS (`base.css` +541, `layout.css` +441)— no está cubierta por nada. Conviene ejecutarla en un runner de CI o en una máquina con Chrome antes de `/ship`.
  - [x] **`/spec` v0.5.0 REABIERTA Y AMPLIADA (2026-09-09) — consolidación del lanzador.** A decisión explícita del usuario tras ver la aplicación construida: el home tenía un muro de 8 tarjetas más 5 botones, y elegir plantilla se podía hacer por **tres** superficies distintas (rejilla del lanzador, galería modal de RF-20, panel de Universe de la Beta) con dos estéticas. El dato que cerró el diagnóstico: en `launcher.js:87-93` el clic de cada tarjeta **solo abre la galería**, así que la rejilla era un menú que abría otro menú mejor. Ninguna de las tres fue un error en su día — se añadió una por versión sin retirar la anterior.
    - **Antes de especificar** se escribió `docs/DESIGN.md` (deuda documental abierta desde el `/spec` original) leyendo `tokens.css`/`base.css`/`layout.css`, para que el rediseño usara el sistema real en vez de abrirle excepciones. Su §9 registra 4 deudas encontradas al leer el CSS y su §10 las 6 restricciones del rediseño.
    - **Diseño validado visualmente ANTES de congelar nada**: lienzo de 6 artboards (home, las tres pestañas de la galería, y home + galería en sepia) con fuentes en `spikes/launcher-redesign/`. Aprobado por el usuario ("perfecto diseño").
    - **Congelado en `SPECIFICATIONS.md` v1.4 §5d**: RF-25 (lanzador de una sola vía: fuera la rejilla y los dos botones de catálogo; los recientes heredan el espacio), RF-26 (galería unificada con tres pestañas sobre el mismo esqueleto — locales, Universe curado, dirección libre — donde solo cambia la fuente de la lista), RF-27 (tokens semánticos `--status-*` en los tres temas y declaración de `--accent-subtle`, que hoy se usa en `layout.css:1148` sin existir).
    - **Restricción explícita:** la pestaña "Paquetes" NO se absorbe — se importan en el documento ya abierto, es trabajo del editor y sigue tras el botón ✦. Fusionarlas recrearía el problema que se arregla.
    - **Decisión del usuario en esta fase:** la previsualización del identificador libre va **solo bajo un control explícito** que declara que descarga y ejecuta la plantilla. Descartadas no previsualizar (pierde el rasgo distintivo) y previsualizar al validar (ejecutaría código de terceros solo por teclear, contra `ARCHITECTURE.md` §6).
    - **ADR-LANZADOR-001** registrado en `memory.md` según exige la regla de congelación.
  - [x] **`/plan` del rediseño del lanzador CERRADO (2026-09-09)** — `implementation_plan.md` reescrito, Modo Orquestador, Adversarial Architect Review con 2 objeciones resueltas y 5 riesgos registrados (R-25 a R-29). **Pendiente de aprobación del usuario para `/build`.**
    - **Hallazgo que reduce el trabajo:** `main.js:590-594` demuestra que **la galería ya está unificada en contenido** — `getFullGalleryCatalog()` devuelve `[...local, ...universe]` y `openGalleryForSpec()` ya sintetiza una entrada para un identificador libre. Lo que falta no es fusionar catálogos, sino **distinguirlos y ofrecer dónde escribir un identificador**. RF-26 pasa a ser presentación sobre datos existentes y RF-25 una poda; lo único nuevo de verdad es el backend de la previsualización.
    - **R-25, el riesgo que más caro habría salido:** retirar `#btn-launcher-universe` del HTML sin tocar `main.js:637` deja un `null` en el array `trigger` de `registerPanel`, que no filtra (`registerPanel.js:32`) y hace `trigger.addEventListener` (`:45`). No degrada la vista de Universe: **impide arrancar la aplicación**. Mitigado retirando marcado y entrada del array a la vez, más `.filter(Boolean)` defensivo con test.
    - **R-26:** `createLauncher` es el único sitio que llama a `listTemplates()`. Podar la carga junto con la rejilla dejaría la pestaña "Plantillas locales" vacía **sin ningún error visible**. El lanzador conserva `load()`, la caché y `getCatalog()`.
    - **R-27:** `parseUniverseSpec` valida la forma, no que sea una plantilla (`@preview/cetz:0.3.1` pasa y no es plantilla). La previsualización bajo botón explícito resulta ser el detector temprano: falla en la galería y no tres pasos después, en el formulario de RF-03.
    - **Decisión resuelta en esta fase:** `universePanel.js` pasa a **panel de una sola vía "Paquetes del Typst Universe"** — se retira su barra de pestañas y la rejilla de plantillas; el botón ✦ se retitula. Dejar dos pestañas con una vacía sería una pregunta que la interfaz hace sin necesidad.
    - **Orden de slices como parte del plan, no como preferencia:** no se retira ninguna ruta hasta que su sustituta está construida y verificada. La poda es lo último.
  - [x] **Slice 37 — RF-27: tokens semánticos de estado — COMPLETADO (2026-09-09).** `--status-ok/warn/error/info`, sus fondos translúcidos y `--accent-subtle`, en los tres temas; en sepia salen de la paleta que ese tema ya usa para el código (`--code-string`, `--code-class`, `--code-keyword`) y el informativo reutiliza el azul de `--band-hint`, el único frío sancionado allí. Fuera los literales de `base.css`/`layout.css` y las dos transiciones a mano. **`verify:frontend` pasa de 8 a 10 comprobaciones** con dos reglas nuevas que fijan la disciplina: *ningún color literal fuera de `tokens.css`* y *todo token usado sin respaldo está declarado*. **La segunda encontró un fallo real en su primera ejecución:** `base.css` usaba `font-family: var(--font-sans)`, un token que no ha existido nunca (es `--font-ui`), así que el tooltip de hover de Universe heredaba la fuente por accidente desde que se construyó. Excepciones documentadas: la banda de error fatal (no puede depender de un tema quizá no cargado) y los blancos/negros puros sobre acento. ~~pendiente~~ `--status-ok/warn/error/info` y `--accent-subtle` en los tres temas; fuera los literales de `base.css`/`layout.css` y las dos transiciones a mano. Aditivo e independiente.
  - [x] **Slice 38 — Backend de la previsualización del identificador libre — COMPLETADO (2026-09-09).** Comando `universe::preview_universe_template`: `typst init` en un `TempDir` (que se borra solo por cualquier camino de salida) del identificador **reconstruido** con `parse_universe_spec` + `to_spec()`, `typst compile --pages 1 --format svg`, y devolución del SVG. **Verificado contra el binario real antes de escribir el código**, según la disciplina del proyecto: `typst init @preview/cetz:0.3.1` responde `error: package @preview/cetz:0.3.1 is not a template`, y ese mensaje estable es el que `classify_init_error` traduce a la variante nueva `AppError::NotATemplate` — un `kind` propio que el frontend explica sin parsear texto del compilador. 179 tests de Rust (+3). ~~pendiente~~ Comando Rust `preview_universe_template`: `typst init` en temporal del identificador **reconstruido** con `parse_universe_spec`+`to_spec()`, compilación de la primera página, limpieza en todos los caminos, y error propio para "es un paquete, no una plantilla". Nadie lo llama todavía.
  - [x] **Slice 39 — RF-26: galería de tres pestañas — COMPLETADO (2026-09-09).** `templateSource()` y `countBySource()` como ayudantes puros (la marca no se inventa: una entrada de Universe lleva `universeSpec` o el prefijo `@preview/`), filtrado y contadores por pestaña, buscador **dentro de la pestaña abierta**, y pestaña "Dirección" con validación por `parseUniverseSpec`, acción principal deshabilitada mientras el identificador no sea válido y previsualización solo bajo el botón que declara que descarga y ejecuta. Abrir la galería con un identificador de Universe deja visible su pestaña, para que la selección no quede en una lista oculta. Enter en el campo previsualiza, no crea. 9 tests nuevos, incluido uno que fija que validar NO dispara la descarga. i18n en ES y EN. **No retiró nada:** al cerrar este slice convivían los caminos viejos y el nuevo. ~~pendiente~~ Discriminante `source` (`local`/`universe`), filtrado y contadores por pestaña, buscador dentro de la pestaña activa, pestaña Dirección con validación, acción deshabilitada y previsualización bajo botón. i18n en ES y EN. **No retira nada:** conviven caminos viejos y nuevo.
  - [x] **Slice 40 — RF-25: poda del lanzador + panel de Universe de una vía — COMPLETADO (2026-09-09).** Fuera `.template-grid`, `.launcher__template-actions`, `#btn-launcher-gallery` y `#btn-launcher-universe` (marcado, estilos, claves de i18n y `el()` retirados a la vez); entra la acción dominante "Nuevo documento" con tres secundarias, y los recientes heredan el sitio. `createLauncher` **conserva `load()`, la caché y `getCatalog()`** — es el único sitio que llama a `listTemplates()` y de ahí come la pestaña "Plantillas locales" (R-26), con dos tests que lo fijan. `registerPanel` filtra los disparadores nulos (R-25) con tres tests de regresión propios. `universePanel.js` pasa a una sola vía y el botón ✦ se retitula.
    - **RF-26.10, a observación del usuario:** el botón ✦ **se deshabilita mientras no hay documento abierto** y explica por qué al pasar el cursor, en lugar de abrir un panel cuya única acción posible terminaba en un aviso de error. Nace deshabilitado en el HTML y lo habilita `renderDocumentBar`.
  - [ ] **Puertas antes de `/test` — PENDIENTES LAS DOS:** tests verdes tras CADA slice · `verify:frontend` · **`verify:layout` en CI** (no ejecutable en esta máquina, y este trabajo es de layout: innegociable, R-28) · **pasada manual del usuario en la ventana real**, lanzador y las tres pestañas, en los tres temas.
  - [x] **`/test` v0.5.0 COMPLETADO (2026-09-09) — 266 Vitest (de 24 a 26 ficheros) + 189 Rust, `verify:frontend` 10/10, `verify:typst` 8/8, `verify:templates` 40/40.** La fase no se limitó a añadir cobertura: encontró **dos fallos reales en `commands/git.rs`**, ambos reproducidos contra el `git` real antes de tocar código.
    - **Rutas con espacios y renombrados.** El parser de `git status --porcelain=v2` sacaba la ruta con `split_whitespace().last()`: `mi documento.typ` se mostraba como `documento.typ` (un fichero que no existe con ese nombre), un renombrado devolvía la ruta VIEJA —que va después del tabulador— en vez de la nueva, y una línea truncada llegaba a listar `100644` como fichero modificado. Corregido con `parse_entry_path()`, que salta los campos fijos del formato (8 para `1 `, 9 para `2 `) y corta en el tabulador. 7 tests con la salida literal capturada de un repositorio de prueba.
    - **Nombres con acentos escapados en octal.** `core.quotePath` viene activado de fábrica, así que `sección.typ` llegaba escapado byte a byte y entre comillas, y así se pintaba en el panel. En un editor para escribir en español ese es el caso normal, no el raro: los comandos se lanzan ahora con `-c core.quotePath=false`.
    - **Cobertura nueva, elegida por riesgo y no por relleno:**
      - `commands/git.rs`: de 2 a 9 tests (los de arriba, más adelanto/retraso respecto al remoto y degradación a `HEAD` cuando no hay rama).
      - `commands/tinymist.rs`: de 4 a 7. Los caminos felices del marcado LSP ya estaban; faltaban los tres que fallan en silencio — que `Content-Length` se mida en **bytes** y no en caracteres (los cuerpos con acentos son el caso normal aquí, y un `chars().count()` desincronizaría el canal sin lanzar nada), que un cuerpo ilegible **no atasque el buffer** dejando mudo al LSP, y que una cabecera partida entre dos lecturas no consuma nada a medias.
      - `src/i18n/i18n.js` (708 líneas, **cero tests** hasta ahora): paridad ES/EN de todas las claves —la misma disciplina que el proyecto ya exige al changelog, que a mano se olvida y no lanza nada: `t()` devuelve el identificador y el usuario ve `gallery.tabSpec` en mitad de la pantalla—, ninguna traducción vacía, y que `applyTranslations` **no borre el icono** de un botón con icono y etiqueta. Ese último es exactamente de la familia de fallos que este proyecto arrastra: `applyTranslations` escribe `textContent`, así que un `data-i18n` puesto en el botón en vez de en su `<span>` borraría el SVG **en el primer cambio de idioma**, no al pintar — se vería bien hasta que alguien pulsara EN.
      - `src/universe/universePanel.js` (reescrito hoy, sin tests): 8 tests que fijan la frontera de RF-26.7 — el panel pinta paquetes y **ninguna** plantilla, identificador y licencia siempre visibles, y el enlace a typst.app no dispara además la importación.
  - [ ] **Diferido conscientemente de `/test`, con motivo:**
    - **La pasada manual en la ventana real.** La hará el usuario antes de la publicación. Sigue siendo la única capa que atrapa la familia de fallos que ninguna verificación automática ve en este proyecto.
    - **`npm run verify:layout` en CI.** No ejecutable en esta máquina y el trabajo de hoy fue de layout: innegociable antes de `/ship` (R-28).
    - **`preview_universe_template` en su conjunto.** Solo están cubiertos su clasificador de errores y la reconstrucción del identificador: la función necesita un `AppHandle` y descarga de red, así que probarla entera exigiría una infraestructura de test que este proyecto no tiene. Se verificó a mano contra el binario real, con una plantilla y con un paquete que no lo es.
    - **`macos_menu.rs` (200 líneas, 0 tests) y `platform.rs`.** El primero sigue sin compilarse nunca en un Mac real —es la pieza menos fiable del proyecto— y el segundo muta el `PATH` del proceso: probarlo de verdad pide extraer una función pura, que es trabajo de `/code-simplify`, no de esta fase.
  - [x] **`/code-simplify` v0.5.0 COMPLETADO (2026-09-09) — 1 Crítico encontrado y resuelto, gate satisfecho.**

    **Pase 1 — Bugs.**
    - **[Crítico, RESUELTO] Bloqueo de tubería en `commands/python_runner.rs`.** El proceso hijo se lanzaba con `stdout`/`stderr` conectados, pero el padre no los leía hasta **después** de que terminara. El búfer de una tubería del sistema ronda los 64 KB: al llenarlo, Python se bloquea escribiendo y no termina nunca, así que el bucle de espera agotaba el timeout completo. El usuario veía "Tiempo de ejecución excedido" —y perdía toda la salida— en un script correcto. Un `print` de un DataFrame grande o una ejecución verbosa de Matplotlib alcanzan ese tamaño sin esfuerzo, que es justo el uso para el que existe RF-22. **Reproducido antes de tocar código:** el test de 300 KB fallaba tras consumir los 30 s del límite; con las tuberías vaciadas en hilos aparte termina en 1,27 s. Ninguna de las 5 pruebas que ya tenía el módulo lo cogía porque todas imprimen una línea corta.
    - Se auditó el MISMO patrón en los otros tres `spawn()` del backend y **ninguno lo tiene**: `typst_engine/compile.rs` y `commands/tinymist.rs` consumen el flujo de eventos del shell de Tauri mientras el proceso corre, y `file_io::reveal_in_file_manager` no conecta tuberías. Los demás procesos usan `.output()`, que drena internamente.

    **Pase 2 — Seguridad.** Sin hallazgos nuevos.
    - `universe::preview_universe_template` (nuevo hoy): el identificador se reconstruye con `parse_universe_spec` + `to_spec()` antes de tocar el disco, nunca viaja crudo, y el `TempDir` se borra por cualquier camino de salida. Es código de terceros ejecutándose a propósito, tras un control explícito (RF-26.6).
    - `commands/git.rs` mantiene `GIT_TERMINAL_PROMPT=0` y `GIT_ASKPASS` vacío; el `-c core.quotePath=false` añadido en `/test` no abre ninguna vía nueva.
    - `archive.rs` conserva intacta su doble comprobación anti *zip-slip*.
    - `python_runner` ejecuta código del usuario a petición suya, con timeout y `MPLBACKEND=Agg`; el fichero temporal del script se borra en todos los caminos.

    **Pase 3 — Cumplimiento.** Sin hallazgos.
    - Los 4 ficheros nuevos llevan la cabecera de `project.config.md`; comprobado en todo `src/`, `src-tauri/src/` y `scripts/`: **ninguno sin cabecera**.
    - Ni `window.confirm` ni `alert` ni `prompt` fuera de un comentario que explica por qué no se usan.
    - Sin dependencias nuevas: la única descarga de red del trabajo de hoy es la del propio `typst init`, que es la funcionalidad especificada.

    **Simplificación (sin funcionalidad nueva).**
    - **Código muerto retirado de `main.js`:** la poda de RF-25 dejó `openGalleryForSpec()` (20 líneas) sin ningún llamante —su único invocador era el `onUseTemplate` del panel de Universe, que desapareció— y con él la importación huérfana de `specName`. **No era duplicación de `syntheticTemplate()` de la galería: era la copia vieja de lo mismo, olvidada.**
    - **`platform.rs`: extraída `compose_path()`**, la deuda que `/test` dejó explícitamente aquí. `augment_path()` mezclaba lectura de entorno, filtrado por disco y mutación global del `PATH`, y solo se podía probar afirmando que el resultado "no está vacío". La parte que decide el valor es ahora pura y tiene 5 tests, incluido el que fija que las rutas inyectadas van **delante** del `PATH` heredado (que es la razón de ser del módulo) y el que impide escribir una cadena vacía cuando no hay nada que añadir. **Comportamiento idéntico:** no se tocó el orden ni se añadió deduplicación, para no cambiar nada bajo una verificación de layout que hoy no se puede ejecutar.
    - Actualizado el comentario del panel de Universe en `main.js`, que seguía describiendo las dos ramas que ya no tiene.

    **Deliberadamente NO refactorizado**, por la regla de no tocar DOM ni CSS antes de una pasada manual que el usuario hará y con `verify:layout` inoperativo en esta máquina: `main.js` (893 líneas de cableado), `templateGalleryModal.js` (que creció hoy) y `preview.js` (605). Ninguno presenta un defecto: solo son grandes.
  - [x] **`/spec` v0.5.0 REABIERTA POR TERCERA VEZ y `/build` de §5e COMPLETADO (2026-09-09).** Tras probar el usuario la aplicación construida: echó en falta la chincheta de ventana encima ("es esencial en mis aplicaciones y todas la tienen, aquí se había pasado"), pidió ver la previsualización a tamaño grande ("se ve bien, pero muy pequeñita") y preguntó si estaba hecha "la integración con GitHub". Congelado en `SPECIFICATIONS.md` **v1.5 §5e**, con `ADR-VENTANA-001`.
    - **RF-28 — Chincheta "mantener la ventana encima".** No se diseñó: se **porta de DBV Markdown Reader** con su mismo pictograma, porque la misma acción no debe tener dos dibujos distintos entre aplicaciones de la familia. **Por ventana y sin persistencia**, replicando su `ADR-023`. Módulo nuevo `app/alwaysOnTop.js` con la ventana **inyectada** en vez de importada, para poder probarlo sin Tauri: 7 tests, incluido el que fija que si `setAlwaysOnTop` falla el control **no se marca activo** — mentir sobre el estado dejaría al usuario creyendo que su ventana está fijada y haría que la siguiente pulsación intentara soltar algo que nunca se fijó. Permisos: solo `core:window:allow-set-always-on-top` y `allow-is-always-on-top`, que `core:default` no trae porque no incluye las capacidades que MUTAN estado de ventana.
    - **RF-29 — Previsualización a tamaño grande.** Capa sobre la propia galería, no sobre la ventana entera. La página se **clona** en vez de moverse, así que al cerrar siguen intactas la selección y la pestaña; y con la ampliación abierta `Escape` la cierra **a ella y no la galería**, porque cerrarlo todo de golpe obligaría a rehacer la búsqueda. 7 tests. Vale también para la previsualización descargada de un identificador libre, que es donde más falta hace: es la única página que el usuario no ha visto nunca antes de crear el proyecto.
    - **Verificación:** 280 Vitest (27 ficheros) · 195 Rust · `verify:frontend` 10/10 · `verify:typst` 8/8 · `verify:templates` 40/40 · `build:vite`.
  - [x] **Documentado el alcance real de la integración con Git (`SPECIFICATIONS.md` §5e.1), a petición expresa del usuario.** La pregunta destapó que la diferencia entre "Git" y "GitHub" no era visible desde la interfaz. Lo construido en RF-19 son **cuatro operaciones sobre la CLI del sistema** (estado, commit, push, pull) y **nada específico de la plataforma**: funciona igual con GitLab, Codeberg o un remoto por SSH. No hay inicio de sesión, ni clonado desde una URL, ni creación de repositorios, ni PRs, ni API. Y la autenticación no la resuelve la aplicación sino el gestor de credenciales del sistema, porque `GIT_TERMINAL_PROMPT=0` y `GIT_ASKPASS` vacío impiden **a propósito** que un proceso hijo se cuelgue esperando una contraseña en una terminal que no existe. **Hueco reconocido:** no se puede clonar un repositorio desde la aplicación.
  - [ ] **v0.6.0 — decidir la integración con GitHub al INICIO de la versión**, con todas las alternativas sobre la mesa (petición explícita del usuario, enumeradas en `SPECIFICATIONS.md` §9 con su coste): no hacer nada específico · clonar desde una URL · publicar un proyecto local · autenticación propia · apoyarse en la CLI `gh`. La pregunta de fondo no es técnica y la evaluación debe responderla, no solo listarla: si el producto es para quien "quiere escribir su TFG" y no para desarrolladores, ¿cuántos de sus usuarios usan Git siquiera, y justifica eso que un editor offline-first empiece a custodiar credenciales y a atarse a una plataforma concreta?
  - [x] **`verify:layout` YA SE EJECUTA en la máquina de desarrollo (2026-09-09) — el bloqueo R-28 queda levantado.** El script elegía el **primer** navegador instalado de su lista —Edge— y nunca comprobaba si respondía. El `--dump-dom` de Edge devuelve salida vacía aquí (también con una página trivial), así que la única verificación de geometría del proyecto se omitía en silencio **teniendo Chrome instalado al lado**. Ahora prueba candidatos hasta dar con uno que conteste e informa de cuál usa. **14/14 en verde.**
  - [x] **La sonda pasa a montar la cabecera y el lanzador REALES** (`scripts/layout-probe.html`), de 9 a 14 comprobaciones. La cabecera era un sustituto de una línea que no podía enseñar nada, y el lanzador seguía llevando la rejilla que RF-25 retiró: se medía una pantalla que ya no se envía.
  - [x] **[Crítico visual, RESUELTO] La cabecera se partía en tres filas** con un proyecto abierto y la ventana estrecha, reportado por el usuario con captura. `.app-header__actions` está declarada `flex: 0 0 auto` —nunca encoge—, así que toda la presión caía sobre la marca, y `.app-header__title` no llevaba `nowrap`: el nombre del producto se troceaba en "DBV / Typst / Editor" y estiraba la cabecera a 88 px. El botón de la chincheta (RF-28) lo agravó. Corregido y **validado a la contra**: con el CSS anterior, las dos comprobaciones nuevas fallan con 88 px de cabecera y 63 px de título.
  - [ ] **Sigue pendiente la pasada del usuario en la ventana real.** `verify:layout` mide geometría del shell —cabecera, lanzador y las 7 combinaciones de paneles—, **no** la galería, ni sus tres pestañas, ni la vista ampliada, ni la chincheta funcionando de verdad. Eso solo lo ve un humano abriendo la aplicación.
  - [x] **RF-30 — La cabecera agrupa sus acciones de fichero en un menú "Archivo" (2026-09-09).** Propuesto por el usuario y medido en el motor real antes de discutirlo: los cinco botones de texto ocupaban **356 px** y el menú ocupa **71**. Usa las etiquetas completas que ya existían (las abreviadas se retiran) y suma "Importar proyecto", que solo estaba en el lanzador. Lo que necesita proyecto se **deshabilita**, no se oculta (mismo criterio de RF-26.10). **Cierra una historia vieja:** `.button--icon-label` nació en la Beta porque "la cabecera dejaba de caber solo con texto", y acortar etiquetas alivió el síntoma sin tocar la causa — por eso volvió a romperse al añadirle el indicador de Git y la chincheta. Verificado por el usuario en la ventana real, con proyecto abierto y sin él.
  - [x] **`/ship` v0.5.0 COMPLETADO (2026-09-09).** Versión Minor: funcionalidad nueva sin roturas. Bump `0.4.0` → `0.5.0` en los **cuatro** sitios (`package.json`, `tauri.conf.json`, `Cargo.toml` y la entrada del propio paquete en `Cargo.lock`); sección `[0.5.0] - 2026-09-09` cerrada en los DOS changelogs con 43 entradas simétricas, dejando `[Sin publicar]`/`[Unreleased]` vacías; `README.md` y `README.en.md` actualizados (insignias, versión actual y resumen de la entrega); `walkthrough.md` reescrito para v0.5.0; Snapshot de Contexto actualizado; tag `v0.5.0` creado. **`latest.json` se deja a propósito en `0.4.0`:** lo genera `npm run updater:manifest` a partir de un build firmado, y la clave de firma la genera el usuario en su terminal, nunca la IA — editarlo a mano dejaría un manifiesto con una firma que no corresponde.
  - [ ] **PENDIENTE (acción del usuario): `git push origin master --tags`.** Al empujar el tag arrancan los releases de Linux y macOS, y es la primera vez que corren con el arreglo del sidecar de Tinymist: conviene vigilar las dos ejecuciones en vez de darlas por buenas.

- [x] **Fase 12: `/spec` v0.6.0 — CONGELADO v1.7 (2026-09-11).** Alcance acordado en una sola pasada para evitar las tres reaperturas de v0.5.0 (ver `ADR-V060-001` en `memory.md`). `docs/SPECIFICATIONS.md` §5f añade **RF-31 a RF-38** (RF-39 se añadió después, hallazgo de la pasada manual — ver más abajo):
  - [x] **RF-31** Editor WYSIWYG de diagramas — sustituye al asistente CeTZ (RF-23). Construido, y con tres rondas de mejora sobre el feedback del usuario (formas/colores/zoom, dirección de flechas, encaje de etiquetas).
  - [x] **RF-32** Menú "Herramientas" en la cabecera (Terminal, Python, Git, y punto de entrada de RF-33/RF-38).
  - [x] **RF-33** Clonar repositorio por URL — decisión del usuario (2026-09-11) entre las 5 alternativas de integración con GitHub evaluadas en §9: **clonar por URL**, sin custodiar credenciales. Ver `ADR-GITHUB-001`.
  - [x] **RF-34** Universe Browser completo (Package Explorer + Template Explorer sobre el catálogo sin filtrar) — deuda de Beta. Búsqueda contra el índice real construida; **sin validar en ventana real** (ver pendientes).
  - [x] **RF-35** Bibliografía visual completa — deuda de Beta; `hayagriva` como motor. **Sin validar en ventana real** (ver pendientes).
  - [x] **RF-36** Empaquetado macOS — deuda de Beta/roadmap. Bundle y CI completos; firma condicionada a que el usuario configure su cuenta de Apple Developer (sin bloquear el resto).
  - [x] **RF-37** Auto-actualizador (`tauri-plugin-updater`) — ya estaba completo desde la Beta.
  - [x] **RF-38** Ejecución de módulos JavaScript con `jogs` — plugin WASM con QuickJS embebido, con límite de 45s ante un script colgado. Ver `ADR-JOGS-001`.
  - [x] **RF-39** Pegar una imagen del portapapeles — no estaba en el `/spec` original; hallazgo de la pasada manual (2026-09-12), añadido como requisito nuevo con sus propios criterios de aceptación.
  - **Próximo paso:** `/plan` de v0.6.0 (Modo Orquestador, Adversarial Architect Review, desglose en `implementation_plan.md`).

- [x] **Fase 14: `/build` v0.6.0 — 11 de 12 slices completados el 2026-09-11**, aprobado el `implementation_plan.md` (12 slices, 41-52). **Slice 49 (retirar `cetzAssistant.js`) queda diferido a propósito**: RF-31 se construyó con alcance reducido (Slice 48), así que el asistente viejo sigue cubriendo plot/canvas que el nuevo editor todavía no tiene — retirarlo ahora sería una regresión. Se retira cuando una iteración futura amplíe el editor visual a los 4 tipos. **Doce rondas de pasada manual después del build** (RF-33, RF-31 ×5, RF-39, RF-16 ×2, y hallazgos varios) — ver abajo, todas cerradas.
  - [x] **Cuarta ronda de la pasada manual (2026-09-11): el editor WYSIWYG "no se podía mover ni arrastrar" — bug real, corregido en la misma sesión.** Con el import de CeTZ ya arreglado, el usuario probó de verdad el lienzo y encontró que los nodos no se movían. Causa: `render()` reconstruía el SVG completo dentro de `pointerdown`/`pointermove`, destruyendo el elemento que acababa de capturar el puntero en el instante en que empezaba a arrastrarse. Separada la reconstrucción completa (solo al añadir/borrar/conectar) de la actualización en el sitio (`patchNodePosition()`). Verificado con `diagramEditor.test.js` (8 tests nuevos) simulando la secuencia real de eventos de puntero — el test de regresión comprueba explícitamente que el nodo arrastrado sigue siendo el MISMO elemento del DOM tras moverse. También: dos plantillas de arranque (mismo contenido que el asistente CeTZ) para que el lienzo no empiece vacío, y clic simple enfoca el texto para editar. Decisión: no se retiró el botón pese a que el usuario dio esa salida — la causa era un bug localizado, no una limitación de diseño. 339 Vitest + `verify:frontend` 11/11. Detalle en `memory.md`.
  - [x] **Quinta ronda (2026-09-11): el editor de diagramas gana formas, colores, zoom y paneo; y TODA ventana flotante se puede cerrar y mover.** Dos peticiones en el mismo mensaje del usuario. (a) El editor "solo permite añadir cuadros, sin colores": se rehizo tomando como referencia explícita **dbv-eer-studio** (`D:\Programacion\github-davidbuenov\eer-studio`), del que se adopta su modelo de interacción — paleta de 4 formas que insertan con un clic, `<g transform>` como único punto de zoom/paneo, arrastre del fondo para panear, controles de zoom sobre la esquina del lienzo. Seis colores con relleno y trazo emparejados. (b) "Haz que todas las ventanas puedan cerrarse y moverse": resuelto en `registerPanel()`, la factoría por la que pasan todos los paneles, en vez de panel a panel — solo 6 de 14 diálogos tenían "✕" y solo 3 eran arrastrables. Verificado contra el compilador real: las 4 formas y los 6 colores compilan, y `content()` con DOS coordenadas + `align(center + horizon)` encaja las etiquetas dentro de la forma (ancladas por nombre, un texto largo se salía de la caja). Encontrado de paso un bug que nadie había reportado: `replaceChildren()` sobre el `<svg>` borraba el `<defs>` con la punta de flecha. 353 Vitest + `verify:frontend` 11/11 + `verify:layout` 15/15. Detalle en `memory.md`.
  - [x] **Sexta ronda (2026-09-12): pegar una imagen del portapapeles — RF-39, funcionalidad nueva, no una corrección.** El usuario recortó una zona de pantalla y al pegar "no pasaba nada": un `grep` de `clipboard|paste` sobre todo el proyecto devolvió cero resultados — no existía el manejo de pegado. Registrado como RF-39 en `SPECIFICATIONS.md` con sus 5 criterios de aceptación. No pudo reutilizar RF-18 porque un recorte llega como bytes sin ruta de origen: comando nuevo `save_pasted_image` (bytes en base64 por el IPC, deduplicación por contenido compartida con el arrastre) y `clipboardImage.js` puro para la decisión de qué es un pegado de imagen — ignora el pegado con `text/plain` (para no romper el pegado de texto normal) y `image/bmp` (Windows lo ofrece a menudo y Typst no lo incrusta). El criterio "insertar o solo copiar" de RF-18 se traduce de coordenadas a foco. 215 tests de Rust (5 nuevos) + 363 Vitest (10 nuevos) + `verify:frontend` 11/11. **Pendiente de confirmar en ventana real** que WebView2 entrega el recorte en `clipboardData.items`. Detalle en `memory.md`.
  - [x] **Séptima ronda (2026-09-12): dirección, trazo y texto en las flechas del editor de diagramas (RF-31).** El usuario mandó una captura del lienzo de hilbert-editor y pidió "que las flechas puedan tener dirección y sea un poco más completo". Analizado el editor de referencia: es un apilador de primitivas sueltas (x/y/tamaño/rotación por elemento) y el nuestro un grafo de nodos conectados, así que se adopta lo que encaja y se descarta a propósito lo que sería cambiar de herramienta (rotación por elemento, lista de escena editable). Añadido: fila de propiedades al pulsar una conexión (4 direcciones, trazo continuo/discontinuo, texto sobre la flecha), formas triángulo y hexágono, y pie de figura + etiqueta `<fig:...>` editables para poder citar el diagrama. Hallazgo que solo apareció compilando y mirando el PNG: la etiqueta de una flecha se coloca con un PORCENTAJE (`50%`), no con `0.5` — CeTZ lee un número plano como distancia en unidades de lienzo y dejaba el texto encima de la caja de origen. Detalle de interacción: una flecha de 1,5 px es inacertable con el ratón, hace falta una línea transparente de 14 px debajo para poder elegirla. 370 Vitest (7 nuevos) + `verify:frontend` 11/11 + `verify:layout` 15/15. Detalle en `memory.md`.
  - [x] **Octava ronda (2026-09-12): la punta de flecha no se veía en el lienzo, aunque sí en el PDF.** El síntoma traía la pista: si compila bien y no se pinta, el modelo está bien. Causa raíz y trampa general de SVG: un `<marker>` NO hereda el color de la línea que lo usa — se pinta con su propio `fill`, negro por defecto si no se declara, y el marcador no tenía ninguna regla de CSS. Sobre el lienzo del tema oscuro (`#21262d`) quedaba invisible; en claro y sepia sí se veía, así que el fallo dependía del tema. Arreglado con `fill: context-stroke`, que además hace que la flecha seleccionada tome el color de acento sin un segundo marcador. Verificado con sonda en Chrome headless: primero el valor calculado (demuestra que el motor lo parsea) y después una CAPTURA DE PANTALLA (lo único que demuestra que lo pinta). 370 Vitest + `verify:frontend` 11/11 + `verify:layout` 15/15. Detalle en `memory.md`.
  - [x] **Novena ronda (2026-09-12): la punta de flecha seguía sin verse — el diagnóstico anterior era incompleto.** El usuario volvió con una captura en tema CLARO, donde una punta negra sí se vería: eso descartaba la teoría del color. Causa real: el lienzo trazaba las flechas de centro a centro y, como los nodos se pintan después, la punta quedaba DEBAJO de la caja de destino; en el PDF no pasaba porque CeTZ recorta al borde de la forma con nombre. Arreglado con `boundaryPointToward()` (corte contra polígono para rect/rombo/triángulo/hexágono, ecuación de la elipse para la elipse), lo que además acerca mucho el lienzo a lo que se compila. Lección de método anotada en `memory.md`: la primera sonda reproducía el bug con marcado escrito A MANO y "confirmó" la teoría equivocada; la que sirvió monta el editor REAL en jsdom y vuelca su SVG. 376 Vitest (6 nuevos, incluida la regresión "la flecha acaba en el borde, no en el centro") + `verify:frontend` 11/11 + `verify:layout` 15/15. Detalle en `memory.md`.
  - [x] **Décima ronda (2026-09-12): una imagen insertada llega con su etiqueta `<fig:...>`.** Petición del usuario para poder referenciarla con `@fig:...` sin escribirla a mano. La etiqueta se deriva del nombre del fichero, normalizando lo que Typst no admite en una etiqueta (tildes, espacios, signos) y cayendo en `fig:imagen` si no queda nada utilizable. Punto no obvio: **repetir una etiqueta es un ERROR de compilación en Typst**, así que insertar dos veces la misma imagen habría roto el documento entero — se comprueba contra el documento y se numera (`fig:foto-2`). Para ello `blockTemplate()` pasa ahora el `state` como segundo argumento al constructor de la plantilla. Vale para las tres vías: arrastrar, pegar y el botón "Fig". Verificado compilando dos figuras con etiqueta y sus dos `@fig:` resolviendo. 379 Vitest (3 nuevos) + `verify:frontend` 11/11.
  - [x] **Tercera ronda de la pasada manual (2026-09-11): causa real del "fallo actual" de CeTZ que el Slice 47 no pudo encontrar por lectura, corregida en la misma sesión.** El usuario probó el editor de diagramas nuevo (RF-31) y reportó que insertaba código roto, como el asistente viejo. Causa encontrada COMPILANDO contra el binario real: `@preview/cetz:0.3.1` es incompatible con Typst 0.15.1 (`rect((0,0),...)` revienta con `Failed to resolve coordinate`). Subido a `cetz:0.5.2` en `cetzAction()` y en `diagramEditor.js`. El tipo "Gráfica" necesitó además `@preview/cetz-plot:0.1.4` como segundo import (desde cetz 0.4 el módulo de gráficas vive en un paquete hermano). Verificadas las 4 plantillas + la salida del editor de diagramas contra el binario real antes de fijar el fix. También: documentado en `SPECIFICATIONS.md` RF-34.5 el hover enriquecido de Universe (ya existía, nunca se había escrito) — feedback positivo del usuario, que sugirió una estrella de favorito ya cubierta por RF-34.2 (pestaña "Favoritas", pendiente de construir). 331 Vitest (4 reescritos) + `verify:frontend` 11/11. Detalle en `memory.md`.
  - [x] **Undécima ronda (2026-09-12): el doble clic saltaba al capítulo siguiente (RF-16), probado contra un TFG real del usuario.** Dos fallos, ninguno visible en `testfiles/demo-proyecto`. (1) Muchas plantillas de tesis fuerzan página nueva por capítulo con `pagebreak(weak: true)` dentro del `show heading`; el ancla se sembraba como hermano ANTES del encabezado, así que se componía antes de ese salto y quedaba anclada a la ÚLTIMA página del capítulo ANTERIOR — un doble clic en la mitad inferior de esa página (a menudo la figura de cierre del capítulo) resolvía al capítulo siguiente. Arreglado sembrando el ancla de un encabezado DESPUÉS de él. Un intento anterior (meter el `#metadata` DENTRO del cuerpo del encabezado) se descartó con el compilador real: Typst reasigna cualquier `<label>` dentro de un encabezado al propio encabezado, y la consulta pierde el valor. (2) De paso: un `#metadata` se colaba como texto literal dentro de un bloque de código con vallas (```` ```python ````) porque el rastreador de anidamiento no las reconocía — visible en el PDF y desplazaba el documento una página entera. `Depth` gana seguimiento de vallas de comillas invertidas. Verificado con el propio `seed_anchors()` de producción (binario Rust desechable, borrado tras usarse) contra el TFG real: 19 páginas byte a byte idénticas al original sin sembrar. 221 tests de Rust (8 nuevos) + 379 Vitest + `verify:frontend` 11/11. Detalle en `memory.md`.
  - [x] **Duodécima ronda (2026-09-12): mismo síntoma un capítulo más adentro — el comentario y el `#import` de cabecera de cada capítulo.** El usuario probó la corrección anterior y preguntó explícitamente "dime si es un fallo de la plantilla o de nuestro código" (la plantilla es para la universidad, tiene que ser intachable). Respuesta: fallo nuestro. Causa: la MISMA familia que el fallo anterior — cada fichero de capítulo empieza con un comentario `//` y un `#import` ANTES del `= Título`; ninguno de los dos produce ningún píxel, pero seguían anclándose "antes", heredando la posición fantasma de la página anterior al `pagebreak` del encabezado. Un doble clic sobre el bloque de ecuaciones de "2.2 Métricas" (lo bastante alto para alcanzar esa posición) saltaba al capítulo 3. Arreglo: comentarios e `#import` ya no reciben ancla (`is_invisible()`) — no hay ningún clic posible al que pudieran corresponder. Verificado igual que la ronda anterior: `seed_anchors()` de producción contra el TFG real, tabla de anclas sin nada en la página 7 del capítulo 3, 19 páginas byte a byte idénticas. 224 tests de Rust (3 nuevos) + 379 Vitest + `verify:frontend` 11/11. Detalle en `memory.md`.
  - [x] **Decimotercera ronda (2026-09-12): el buscador de Universe insertaba una plantilla como paquete, y dos fallos de layout/estado en la Galería.** El usuario probó el buscador del catálogo completo (RF-34) con `campanile` (plantilla de tesis de Berkeley) y reportó tres cosas. (1) Se insertaba como `#import` en el documento — `index.json` mezcla paquetes y plantillas en la misma lista (2.281 de 4.712 son plantillas, verificado contra una descarga real completa), y nada distinguía unas de otras pese a que un test YA documentaba el hallazgo sin actuar ("campos de plantilla ignorados"). Añadido `is_template: bool` a `UniverseIndexEntry` (deserializado de la clave `template` del índice real); "Usar" sobre una plantilla ahora abre la Galería en "Dirección" con el identificador puesto, en vez de tocar el documento. (2) "La barra de búsqueda se pone en medio" — reproducido con una sonda en Chrome headless usando el marcado real: dos rejillas con `flex: 1 1 auto` se repartían la mitad del panel cada una SIEMPRE, cortando tarjetas por la mitad. Arreglado acotando la rejilla curada (`max-height`) para que la de resultados se lleve el espacio que sobra. (3) El pie de metadatos de la Galería (pestaña "Dirección") se quedaba con la plantilla de OTRA pestaña tras descargar y previsualizar — `previewTypedSpec()` nunca llamaba a la parte de `renderPreview()` que pinta el pie. Extraído `renderMeta()` y conectado a escribir + a la descarga con éxito. 224 tests de Rust (2 nuevos/renombrados) + 388 Vitest (11 nuevos) + `verify:frontend` 11/11 + `verify:layout` 15/15. Detalle en `memory.md`.
  - [x] **Decimocuarta ronda (2026-09-12): "Paquetes"/"Buscar" como pestañas de verdad, y ese buscador reutilizado también en "Crear documento a partir de plantilla" (RF-34.6, RF-34.7).** Tras el arreglo anterior el usuario pidió "algo más limpio, como otra pestaña" y, en el mismo mensaje, integrar la misma búsqueda en el flujo de crear documento ("la ventana de búsqueda podría ser la misma"). Confirmado el alcance ("vamos paso a paso pero a por todo"), se construyeron las dos cosas antes de que el usuario probara nada. (1) El panel de Typst Universe gana dos pestañas de verdad (mismo patrón que Archivos/Esquema del panel lateral) — cada rejilla tiene el panel entero, ya no compiten por altura. (2) El motor de búsqueda se extrajo a `universe/universeSearch.js` (`createUniverseSearch`), montado en DOS sitios: el panel de Universe (sin filtro) y una nueva pestaña "Buscar" de la Galería de plantillas que sustituye a "Dirección" (filtrada a solo plantillas vía `filterEntries`). Elegir un resultado en la Galería solo rellena la dirección, sin tocar la red (RF-26.6 intacto). Corregido de paso un roce de foco: `openWithSpec()` (de la ronda anterior) heredaba el nuevo autofoco del buscador y se lo robaba al campo ya relleno. 402 Vitest (19 nuevos, incluido `universeSearch.test.js`) + `verify:frontend` 11/11 + `verify:layout` 15/15. Detalle en `memory.md`.
  - [x] **Segunda ronda de la pasada manual (2026-09-11): conflicto de fusión REAL, corregido en la misma sesión.** El usuario reprodujo un `pull` con cambios divergentes de verdad y se encontró las marcas `<<<<<<<`/`=======`/`>>>>>>>` crudas dentro del documento, sin nada en la interfaz para resolverlo. Causa: `git status --porcelain=v2` reporta un fichero en conflicto en una línea `u ` que el parser no reconocía — desaparecía de la lista por completo. 3 piezas nuevas: `conflicted_files` en `GitStatusResult` (Rust, 2 tests con una reproducción real), `app/conflictParser.js` (puro, 11 tests) y `app/conflictResolver.js` (modal "Mío"/"Remoto" por bloque, mismo patrón que `diffModal.js`). Alcance deliberado: resolución por bloque completo, no edición línea a línea — quien necesite mezclar a mano sigue pudiendo editar el fichero directamente. Detalle en `memory.md`. 329 Vitest + 210 Rust + `verify:frontend` 11/11 + `verify:layout` 15/15.
  - [x] **Primera pasada manual en ventana real (2026-09-11), 4 hallazgos, corregidos en la misma sesión.** El usuario probó el flujo de clonado (RF-33) y encontró: (1) un proyecto recién clonado de un repo vacío no tenía ninguna forma de crear el primer fichero — nuevo botón "+" junto al filtro del árbol, fila de creación inline; (2) el popover de Git (RF-19, ya existente) no se podía arrastrar ni cerrar — añadidos ambos, mismo patrón que Terminal/Python; (3) un `pull` bloqueado por cambios locales sin confirmar mostraba el error crudo de Git en vez de una explicación accionable — detectado por texto y traducido a un mensaje claro; (4) el ejemplo por defecto del asistente `jogs` era una suma trivial — sustituido por una función recursiva, verificada dos veces contra el binario real. 317 Vitest (2 tests nuevos) + `verify:frontend` 11/11 + `verify:layout` 15/15 en verde. Detalle completo en `memory.md`, sección "Primera pasada manual de v0.6.0".
  - [ ] **Sigue pendiente:** validar en ventana real el resto de lo construido (editor de diagramas RF-31 con eventos de puntero de verdad, asistente `jogs` con el ejemplo nuevo, Universe Browser completo, bibliografía visual) antes de dar `/build` por cerrado.
  - [x] **Slice 41 — Clonar repositorio por URL (RF-33) — COMPLETADO (2026-09-11).** `commands::git::git_clone` (nuevo `GitCloneResult`, `derive_clone_destination_name`) reutiliza el wrapper no interactivo existente; nombre de destino derivado de la URL, nunca sobrescribe una carpeta no vacía. UI: botón "Clonar repositorio" en el lanzador y en el menú Archivo, panel `#clone-panel` (reutiliza `.floating-panel`/`.universe__spec`/`.form-row__input` ya existentes, sin CSS nuevo). 280 Vitest + 200 Rust + `verify:frontend` 11/11 en verde.
  - [x] **Slices 50-52 — Auto-actualizador (RF-37) y macOS (RF-36) — COMPLETADO (2026-09-11).** Hallazgo: RF-37 y la parte 1 de RF-36 (bundle universal + CI) ya estaban completos desde la Beta, sin nada que programar — `app/updater.js` (comprobar/descargar/instalar/reiniciar, degradación en Store) y `release-macos.yml` (`.dmg`/`.app` universal Intel+Apple Silicon con sidecars fundidos por `lipo`, verificación funcional) ya existían. Lo único real: **Slice 52**, firma condicionada — `release-macos.yml` pasa 6 variables `APPLE_*` desde `secrets.*`; sin esos secretos (hoy no existen) llegan vacías y el build sale sin firmar exactamente como antes, sin fallar. El día que el usuario configure la cuenta de Apple Developer, el mismo workflow empieza a firmar sin tocarlo de nuevo. Ver `ADR-RELEASE-002` (`memory.md`). **v0.6.0 puede considerarse funcionalmente completa en estos dos RF sin esperar a la cuenta.**
  - [x] **Slice 47 — Investigar el bug actual del asistente CeTZ (RF-23) — COMPLETADO (2026-09-11), sin hallazgo verificable.** Revisión estática de `cetzAssistant.js`/`toolbar.js`/`workspace.js`: wiring correcto, mismo patrón que los otros asistentes, sin defecto encontrado por lectura. Sin ventana real no se puede reproducir el fallo que mencionó el usuario. No bloquea RF-31 (lo sustituye igualmente). Ver `ADR-DIAGRAMA-001` (`memory.md`).
  - [x] **Slice 48 — Editor WYSIWYG de diagramas (RF-31), alcance reducido — COMPLETADO (2026-09-11).** Decisión explícita del usuario: lienzo genérico de nodos/flechas (cubre flujo+bloques) ahora, plot/canvas libre después. `diagramModel.js` (lógica pura: nodos, conexiones, traducción a CeTZ, reapertura por comentario JSON, 17 tests) + `diagramEditor.js` (SVG a mano, sin librería nueva, arrastre con pointer events, texto editable con `foreignObject`+`contenteditable`). Nuevo botón "✎" en la barra, coexiste con ⬡ (CeTZ) hasta el Slice 49. **Pendiente explícito: validación en ventana real** — ninguna prueba automática monta el SVG con eventos de puntero de verdad. 315 Vitest + `verify:frontend` 11/11 en verde.
  - [x] **Slice 46 — Bibliografía visual completa (RF-35) — COMPLETADO (2026-09-11).** Nuevo comando `bibliography_entries` (`bibliography.rs`, dependencia `hayagriva`, el mismo motor que usa `typst` para `#bibliography()`) parsea título/autor/año y marca duplicados y campos ausentes; `bibliography_keys` (escaneo ligero) no se sustituye, convive. El asistente de cita existente (`citationPicker.js`) se enriquece en el sitio — sin panel nuevo — con la etiqueta "clave — título — autor, año" y aviso ⚠. Cierra la pregunta abierta en `SPECIFICATIONS.md` §9 desde el spec original. Ver `ADR-BIBLIOGRAFIA-001` (`memory.md`). 208 Rust (5 nuevos, verificados contra la API real de hayagriva a la primera) + 298 Vitest + `verify:frontend` 11/11 en verde.
  - [x] **Slice 45 — Universe Browser completo (RF-34) — COMPLETADO (2026-09-11).** Nuevo comando `fetch_universe_index` (`commands/universe_index.rs`, dependencia `ureq`) sobre `packages.typst.org/preview/index.json`, cacheado en memoria (`UniverseIndexState`). Frontend: campo de búsqueda en el panel de Universe, `filterUniverseIndexEntries`/`universeIndexEntryToCard`/`isCuratedPackageName` en `curatedCatalog.js`, modelo de dos niveles (badge "✓ Verificado"/"Comunidad, sin revisar"). Ver `ADR-UNIVERSE-002` (`memory.md`) y `spikes/universe-index/`. 203 Rust + 293 Vitest + `verify:frontend` 11/11 en verde.
  - [x] **Slice 44 — Asistente de inserción `jogs` (RF-38) — COMPLETADO (2026-09-11).** `hasJogsImport`/`getJogsSnippet`/`jogsAction` en `toolbarActions.js` (mismo patrón que `cetzAction`); botón en el menú Herramientas que inserta `#import` deduplicado + plantilla `eval-js` con el aviso del margen de 45s incorporado en el propio snippet. Sin proceso ni comprobación de sistema, a diferencia de RF-22. 4 tests nuevos (284 Vitest) + `verify:frontend` 11/11 en verde.
  - [x] **Slice 43 — Spike de sandboxing de `jogs` (RF-38) — COMPLETADO (2026-09-11).** Verificado contra el binario real (`spikes/jogs-sandbox/`): `eval-js("while(true){}")` cuelga el proceso `typst` indefinidamente, sin límite propio del motor QuickJS embebido — confirma exactamente el riesgo R-38 del Adversarial Review. Mitigación implementada en el motor, no solo en el futuro asistente: `COMPILE_TIMEOUT` (45s) + `TypstError::TimedOut` en `typst_engine::compile::run_cancelable`, cubre vista previa en vivo Y exportación PDF/PNG. Detalle en `ADR-JOGS-001` (`memory.md`) y `spikes/jogs-sandbox/README.md`. 200 Rust en verde (sin tests nuevos automatizados para el propio timeout — comportamiento dependiente de tiempo real, verificado por spike manual, mismo criterio que otras mediciones de este proyecto).
  - [x] **Slice 42 — Menú "Herramientas" (RF-32) — COMPLETADO (2026-09-11).** Terminal y Python dejan de ser botones de icono sueltos y pasan a `#tools-menu` (mismo patrón que `#file-menu` de RF-30); "Clonar repositorio" se traslada de Archivo a Herramientas. **Decisión de alcance:** el indicador de rama de Git NO se mueve — es estado (RF-19.1), no una acción sin más, y esconderlo detrás de un menú le haría perder el vistazo rápido; documentado en el propio HTML para que no se reabra por accidente. Sonda `scripts/layout-probe.html` actualizada a la geometría real (4 botones en el lanzador, menú Herramientas en la cabecera) — `verify:layout` 15/15 con la cabecera todavía en una sola fila a 830 px. 280 Vitest + `verify:frontend` 11/11 en verde (sin cambios en Rust).

- [x] **Fase 13: `/plan` v0.6.0 — cerrado el 2026-09-11.** `implementation_plan.md` reescrito con 12 slices (41-52), Adversarial Architect Review (riesgo de `jogs` colgando la vista previa en vivo dentro del sidecar `typst`) y 5 riesgos resueltos (R-31 a R-36, ver frontmatter). Decisiones técnicas cerradas en esta fase, no diferidas más: **`hayagriva`** como crate BibTeX de RF-35 (el mismo motor que usa el propio compilador `typst`), **SVG interactivo a mano** para el lienzo de RF-31 (sin librería nueva), **modelo de dos niveles** (verificado/comunidad) para la whitelist de RF-34. RF-38 empieza por un spike de sandboxing (Slice 43) antes de construir el asistente. RF-37 y RF-36 (parte de firma) quedan aislados al final por depender de acciones externas del usuario (clave de firma, cuenta de Apple Developer) sin bloquear el resto de la versión.
  - **PENDIENTE: aprobación explícita del usuario antes de `/build`.**

## 🔄 Context Snapshot / Snapshot de Contexto

> ### 👉 CÓMO RETOMAR ESTE PROYECTO (leer esto primero) — actualizado 2026-09-09, `/ship` v0.5.0 cerrado
>
> **v0.5.0 está entregada en local: versiones subidas, changelog cerrado en los dos idiomas, tag creado.
> Lo único que falta es `git push origin master --tags`, y NO se ha hecho.**
>
> Al empujar el tag arrancan los workflows de release de Linux y macOS. **Es la primera vez que corren
> con el arreglo del sidecar de Tinymist** —los tres declaraban dos sidecars y solo vendorizaban
> `typst`, así que antes de esta versión habrían fallado al empaquetar con el tag ya empujado—, así que
> hay que vigilar las dos ejecuciones en vez de darlas por buenas.
>
> **Qué trae v0.5.0:** productividad profesional (LSP Tinymist vendorizado, runner de Python, asistente
> CeTZ, integración con Git y diff lado a lado, robustez de plataforma en Windows) y una revisión a
> fondo de la interfaz de entrada — el lanzador y la galería se consolidan en **una sola puerta de
> entrada** con tres vías, la cabecera agrupa sus acciones de fichero en un menú, y llegan la chincheta
> de ventana encima y la vista ampliada de plantillas. Detalle completo en `walkthrough.md`.
>
> **Verificación al cerrar:** 280 Vitest · 195 Rust · `verify:frontend` 11/11 · `verify:layout` 15/15 ·
> `verify:typst` 8/8 · `verify:templates` 40/40 · `build:vite`. Y el usuario validó en la ventana real
> el lanzador, la cabecera con y sin proyecto, el menú Archivo, las tres pestañas, la vista ampliada y
> la chincheta.
>
> **Dos cosas que se arreglaron solas al mirarlas, y conviene no perder de vista:**
> 1. `verify:layout` llevaba toda la sesión omitiéndose en silencio: elegía el primer navegador
>    instalado (Edge, cuyo `--dump-dom` no devuelve nada aquí) sin comprobar si respondía, **teniendo
>    Chrome al lado**. Ahora prueba hasta dar con uno que conteste.
> 2. La sonda de esa verificación montaba una cabecera de mentira de una línea y un lanzador con la
>    rejilla que RF-25 ya había retirado: medía una pantalla que no se envía. Ahora monta las reales.
>
> **Acciones que dependen del usuario, no de código:**
> 1. **`git push origin master --tags`** — siete commits y el tag `v0.5.0` esperando.
> 2. **Compilar Windows con las variables `TAURI_SIGNING_*` y ejecutar `npm run updater:manifest`.**
>    `latest.json` sigue apuntando a `0.4.0` **a propósito**: lo genera ese script a partir de un build
>    firmado, y la clave de firma la genera el usuario en su terminal, nunca la IA (regla ya registrada).
>    Editarlo a mano dejaría un manifiesto con una firma que no corresponde.
> 3. **Microsoft Store:** `task.md` arrastra desde v0.3.1 que hay que reenviar el `.msix` corregido a
>    Partner Center, pero un commit posterior anuncia disponibilidad oficial en la Store. **Contrastar
>    si sigue pendiente o quedó resuelto**, y recordar que el paquete crece ~64 MB con el sidecar de
>    Tinymist: el checklist de `docs/MICROSOFT_STORE.md` §6 ya está actualizado a ~95 MB y a los dos
>    `.exe` en la raíz del paquete.
> 4. **Vigilar la ejecución de macOS** si se empuja el tag: `macos_menu.rs` sigue sin haberse compilado
>    nunca en un Mac real y es la pieza menos fiable del proyecto.
>
> **v0.6.0 ya especificada (2026-09-11), no replanificar el alcance — ver Fase 12 arriba y `docs/SPECIFICATIONS.md` §5f (RF-31 a RF-38). Próximo paso: `/plan`.** Detalles que siguen abiertos, a propósito, para resolver en `/plan` en vez de en un nuevo `/spec`:
> - Crate/motor BibTeX para RF-35 (bibliografía visual completa).
> - Tamaño y criterio de expansión de la whitelist de RF-34 (Universe Browser completo).
> - Verificación de sandboxing/límites reales de `jogs` para RF-38 (la página de Typst Universe no lo documenta) — ver `ADR-JOGS-001`.
> - Causa raíz del fallo actual del asistente CeTZ (RF-23), a documentar antes de construir su reemplazo RF-31, para no heredarlo.
> - **RF-36 (macOS) bloqueado en la firma/notarización, no en el código: el usuario aún no tiene la cuenta de Apple Developer (2026-09-11), la espera "en unos días".** El resto de RF-36 (bundle `.dmg/.app`, CI dedicada) puede avanzar sin firma mientras tanto, igual que hoy — ver `ARCHITECTURE.md` §7.16.6. Revisar el estado de la cuenta al retomar antes de dar por bloqueado todo RF-36.
>
> **Sigue pendiente, sin relación con v0.6.0:**
> - Bug reportado sin reproducir, arrastrado desde hace sesiones: al escribir dentro de una ecuación, el
>   estado de la vista previa alterna "Compilando…"/"Página 1".
> - `macos_menu.rs` sin cobertura y sin compilar jamás en un Mac — se compilará de verdad al construir RF-36.
>
> **Última sesión:** 2026-09-11 · **Rama:** `master` · remoto `origin` = `github.com/davidbuenov/dbv-typst-editor` (público) · **7 commits + tag `v0.5.0` sin empujar** (pendiente de acción del usuario, ver arriba).

### ✅ Qué funciona hoy (MVP v0.1 completo)

| Funcionalidad | Requisito | Estado |
| --- | --- | --- |
| Lanzador orientado a tareas | RF-01 | ✅ |
| Modelo de Proyecto + explorador | RF-02 | ✅ |
| Proyectos ajenos sin manifiesto | RF-02b | ✅ (y no se escribe nada en su carpeta) |
| Abrir carpeta / Mostrar en el SO / Recientes | RF-02c | ✅ |
| Asistente de creación de proyecto | RF-03 | ✅ |
| 4 plantillas curadas | RF-04 | ✅ |
| Editor Typst (CodeMirror 6) | RF-05 | ✅ |
| Vista previa en tiempo real | RF-06 | ✅ |
| Guardar / Guardar como / conflicto | RF-07 | ✅ |
| Temas claro y oscuro | RF-08 | ✅ |
| Configuración persistente (tema, idioma, anchos, zoom, recientes) | RF-09 | ✅ |
| Exportación PDF | RF-10 | ✅ |
| Empaquetado Windows/Linux + asociación `.typ` | RF-12 | ✅ |
| Project Archive `.dbvt` | RF-11 | ⏸ diferido a v0.2 por decisión de alcance |

### 📊 Verificación disponible (ejecutable, no manual)

```bash
npm test                 # 33 tests de lógica del frontend (Vitest) + 77 del backend Rust
npm run verify:frontend  # 8 comprobaciones: extensiones del editor, parser Typst, elementos del DOM y ausencia de diálogos nativos
npm run verify:typst     # 8 comprobaciones del sidecar contra el binario real
npm run verify:templates # 20 comprobaciones: cada plantilla se instancia y compila sin avisos
npm run build:vite       # build del frontend
```

Los cuatro primeros se ejecutan también en CI (`.github/workflows/ci.yml`) en cada push.

### ⏸ Diferido conscientemente de la fase `/test` (no bloquea `/ship`)

1. **Prueba de integración de la ventana real:** arrancar la app, abrir un proyecto de prueba, teclear y
   comprobar que la vista previa cambia. Razón: exige `tauri-driver` + WebDriver (Edge/WebKitGTK), infraestructura
   no montada aún y con riesgo de fragilidad cross-plataforma; el flujo ya está validado a mano por el usuario.
2. **Escenarios de conflicto (RF-07) en integración:** editar el mismo fichero desde otro programa y
   comprobar las tres ramas (eco propio ignorado, recarga silenciosa, modal de conflicto). Misma razón —
   cubiertos hoy por prueba manual más la lógica ya testeada de `workspace.js`.
3. **Prueba del instalador generado**, en las dos variantes de Windows y en Linux. Razón: exige máquinas
   limpias sin WebView2/deps preinstaladas; el instalador de Linux ni siquiera se ha generado nunca.

### ✅ Infraestructura resuelta (2026-09-05)

**Remoto configurado y código empujado.** `origin` = `https://github.com/davidbuenov/dbv-typst-editor` (repositorio **público**, creado por el usuario). `git push -u origin master` hecho: 147 ficheros, sin `node_modules`, `dist`, binarios ni claves. `ci.yml` dispara con `["master", "main"]`, así que no hizo falta renombrar la rama. **`ci.yml` se ha ejecutado por primera vez y pasa en verde** (run `33957280085`, 6/6 pasos: tests de backend, build del frontend, verify:frontend, verify:typst, verify:templates y compilación de los ficheros de prueba) — deja de estar "verificado solo por lectura". `release-linux.yml` sigue sin ejecutarse nunca: dispara con tags `v*.*.*`, así que su primera ejecución real será en `/ship`.

### ⚠️ Deuda técnica registrada durante `/build`

- **`docs/DESIGN.md` sin escribir** (ver Fase 1). El sistema visual existe y es coherente; falta documentarlo.
- **El bundle del frontend son 520 kB** (161 kB gzip), por encima de los 200-400 kB estimados en
  ADR-EDITOR-001. Son los datos de autocompletado de la biblioteca estándar de Typst. Aceptado y
  registrado; si molestase, la vía es cargar el paquete de lenguaje con `import()` dinámico.
- ~~**Sin instancia única ni multiventana.**~~ La instancia única se cerró en el Slice 23 (Beta). Sigue sin
  haber multiventana (abrir varios documentos a la vez en pestañas/ventanas propias) — no estaba en el
  alcance del MVP y no se ha reevaluado.
- **El espejo de vista previa (`.dbv-preview.typ`)** es un fichero oculto y transitorio en la carpeta del
  usuario mientras hay cambios sin guardar. Filtrado del explorador y del watcher; si la aplicación muriese
  a mitad de una compilación podría quedar uno huérfano (se sobrescribe en el siguiente arranque).
- **Pista legible cuando un paquete de Typst Universe es incompatible con la versión de Typst incluida**
  (detectada el 2026-09-07, en la validación de `v0.3.1`). Caso real: crear un proyecto desde la plantilla
  `@preview/springer-spaniel:0.1.0` falla con `error: type state has no method 'display'` en
  `@preview/ctheorems:1.1.2`, porque `state.display()` se eliminó de Typst en la 0.13 y la app trae la
  0.15.1. La traza que ve el usuario son 5 niveles de `while importing` de ficheros que él no ha escrito
  y no puede tocar, sin ninguna indicación de que el culpable es el paquete y no su documento. **No es un
  bug de la app** (reproducido idéntico con el binario a pelo, fuera de la aplicación) y la corrección de
  fondo corresponde al autor del paquete: `ctheorems 1.1.3` ya lo arregla, pero `springer-spaniel 0.1.0`
  lo fija a `1.1.2` y no tiene versión posterior publicada.
  **Trabajo propuesto:** reconocer este patrón de error y anteponer una pista, exactamente igual que ya se
  hace con "unknown font family" (Slice 26) y con la bibliografía ausente al previsualizar un capítulo
  suelto (Slice 27) — mismo mecanismo `preview__band-hint` ya existente, sin infraestructura nueva.
  Conviene además distinguir dos casos: paquete demasiado ANTIGUO para el Typst incluido (este) y paquete
  que exige uno más NUEVO. Ojo al redactarlo: el mensaje debe dejar claro que la solución no está en manos
  del usuario, para no mandarle a depurar un fichero ajeno.
- **Pista legible al abrir una plantilla de Pandoc** (decidido el 2026-09-07, ver `memory.md` →
  ADR-PANDOC-001). Detectar la sintaxis `$variable$` de Pandoc y explicar que el fichero pertenece a un
  flujo Markdown → Pandoc → Typst y **no está pensado para compilarse solo**. Deliberadamente NO se
  sustituyen las variables: en el caso medido eso habría producido un PDF vacío sin ninguna pista, peor
  que el error actual. **Mismo mecanismo y misma entrega que el punto anterior** — son tres casos del
  mismo patrón ("el fallo no es tuyo y no está en tu mano"), junto con "unknown font family" (Slice 26)
  y la bibliografía ausente (Slice 27); conviene implementarlos como una sola pasada sobre el
  reconocimiento de errores del compilador, no por separado.
- **Idea propuesta por el usuario (2026-09-10), no especificada todavía: pegar imagen del portapapeles.**
  Motivación de uso real: en reuniones o cursos, el usuario copia capturas de pantalla y hoy tiene que
  guardarlas manualmente a disco antes de poder arrastrarlas al proyecto. Propuesta: al pulsar `Ctrl+V` con
  una imagen (no texto) en el portapapeles, pedir un nombre (o generar uno por defecto), guardarla en
  `images/` del proyecto — reutilizando `copy_asset_into_project`/`find_existing_copy`, la misma
  infraestructura del Slice 19 (arrastrar y soltar) y el Slice 21 (selector manual) — e insertarla en la
  posición del cursor con `figureActionForPath()` si el editor tiene el foco. Candidato natural a un RF
  propio de la siguiente `/spec`, hermano de RF-17/RF-18.
- **Investigación abierta: importador Markdown → Typst.** Palanca de adquisición para el público que viene
  de Markdown/Pandoc/MkDocs, muy superior a soportar plantillas Pandoc (razonamiento en ADR-PANDOC-001).
  Aplicando la disciplina de ADR-LATEX-001, **se mide sobre proyectos reales antes de comprometer alcance**.
  Primer caso disponible: curso propio del usuario sobre Unreal Engine en Markdown; su motivación declarada
  para migrar es que **Markdown da muy poca libertad con las imágenes**, así que la conversión no debe
  limitarse a preservar sino a mejorar el tratamiento de figuras (tamaño, posición, pies, rejillas).

### 🧭 Pasos siguientes acordados (orden recomendado)

1. ~~**Crear el repositorio remoto y empujar.**~~ ✅ **HECHO el 2026-09-05.**
2. ~~**Fase `/test`.**~~ ✅ **CERRADA el 2026-09-05** — 33 tests de frontend nuevos; 3 puntos diferidos a propósito (ver arriba).
3. ~~**Fase `/code-simplify`.**~~ ✅ **CERRADA el 2026-09-05** — 0 hallazgos Críticos, 1 Importante corregido.
4. ~~**Fase `/ship` v0.1.0 (documentación).**~~ ✅ **CERRADA el 2026-09-05** — README, `walkthrough.md`, `CHANGELOG.md` con sección `[0.1.0]`.
5. ~~**Slice 11 (RF-13, v0.2) — barra de herramientas del editor.**~~ ✅ **CERRADA el 2026-09-05.**
6. ~~**Instancia única.**~~ ✅ **CERRADA el 2026-09-05 (Slice 23).**
7. ~~**Etiquetar una versión.**~~ ✅ **`v0.2.0` creado el 2026-09-06** (Fase 8) — se saltó directamente `v0.1.0` por decisión del usuario, ya muy superado por el contenido real. **Pendiente:** empujar el tag y publicar la Release de GitHub (con el instalador de Windows subido a mano) — decisión de publicación, no trabajo técnico.
8. **`docs/DESIGN.md`** — deuda documental consciente, sigue sin escribir (ver Fase 1). Buen candidato de bajo riesgo para cuando se retome.
9. **Sincronización editor ↔ vista previa** — **Spike S-2 CERRADO el 2026-09-08** (`spikes/preview-sync/README.md`). Pedido por un usuario (doble clic bidireccional). Veredicto: el mecanismo de anclas `#metadata` + `query` funciona y es barato, Typst **no** ofrece puente nativo render→fuente, y la raíz sombra en temporal resuelve los cambios sin guardar a través de un `#include` con un 2 % de sobrecoste. **Decidido con el usuario:** la vista previa pasará a compilar el documento raíz (con conmutador a "solo este fichero"), y el control de refresco automático/manual es **requisito**, no comodidad — compilar el documento completo cuesta ×9 y supera la pausa de tecleo. **`/spec` de v0.4.0 CERRADA el 2026-09-08**: RF-14 a RF-18 en `SPECIFICATIONS.md` §5b, con `ADR-SYNC-001` y la enmienda de la cláusula de §6/§11 que prometía sync "por posición real de fuente (no por anclas)", desmentida por el spike. **`/build` de v0.4.0 COMPLETADO el 2026-09-08** — los tres slices construidos y verdes (160 tests Rust, 152 JS, 8/8 verificaciones de frontend). ⚠️ **PENDIENTE: nada de esto se ha visto funcionando en la aplicación real** — no ha habido ejecución de la GUI en esta sesión; es lo primero que hay que hacer al retomar, antes de `/test`. **Tampoco hay ningún commit**: todo el trabajo de la sesión está en el árbol sin confirmar. **`/plan` cerrado el 2026-09-08** (`implementation_plan.md`, con Adversarial Architect Review de 6 objeciones y 3 slices). **Siguiente paso: aprobación del usuario y `/build` del Slice 28.** El informe del spike trae el informe trae recomendaciones concretas de granularidad, ordenación por (página, banda de x, y) y las dos limitaciones del camino inverso (columnas y flotantes).
10. **Migración LaTeX→Typst** — investigación cerrada con `ADR-LATEX-001` (aplazada como pilar de v1.0). Se retoma solo si se cumple su disparador explícito (ver `memory.md`), no por defecto en la siguiente sesión.

### 📜 Commits de la sesión del 2026-09-05

| Commit | Qué |
| --- | --- |
| `bc74407` | Decisión de WebView2 cerrada (ligero por defecto + variante offline) |
| `5c695d8` · `50e72a1` · `78fb3bc` | Slices 3, 4 y 5 |
| `dadb582` · `08bdf0e` · `d9800b6` | Slices 6, 7 y 8 |
| `d26b257` · `ccce34a` | Slices 9 y 10 |
| `9a660e7` | 3 defectos de la revisión propia (historial de deshacer, página visible, separador de ruta) |
| `2ca1328` | **Fallo de arranque** + `verify:frontend` |
| `1590fb9` | Ficheros de prueba en `testfiles/` |
| `36e5e7c` | `dialog.confirm` sin permiso + vista previa compilando el `.bib` |
| `2039359` · `8785298` · `a383df6` | Documentación: punto de retorno, validación manual, renombrado |

### 🛠️ Comandos útiles

```bash
npm install              # dependencias
npm run vendor:typst     # descarga el sidecar Typst (OBLIGATORIO tras clonar; no se versiona)
npm run dev              # arranca la app (start.cmd / start.sh equivalentes)
./stop.cmd               # cierra app + libera el puerto 1420 de Vite
npm run build            # instalador release (Windows: NSIS ~18 MB)
npm run build:win:offline # variante Windows con WebView2 embebido (~268 MB)
```

### 📌 Reglas vigentes

- **Priorizar software funcionando** sobre refinamiento arquitectónico (regla fijada por el usuario).
- **No reabrir decisiones congeladas** salvo bloqueante real, asunción demostrada falsa, o ADR explícito.
  En los tres casos: **ADR en `memory.md` primero, cambio después.**
- Un commit por slice o por unidad de trabajo cerrada, con la app arrancable al final de cada uno.
